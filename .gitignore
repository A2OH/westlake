node_modules/
dist/
.vite/
*.pyc
__pycache__/
.env
.DS_Store
dalvik-port/build/
dalvik-port/build-ohos-*/
dalvik-port/build-libffi-ohos/
dalvik-port/build-zlib-ohos/
dalvik-port/ohos-sysroot/
dalvik-port/*.jar
database/*.db
# Build artifacts
dalvik-port/*.class
dalvik-port/*.dex
dalvik-port/ohos-sysroot-arm32/
test-apps/build-local/
test-apps/javac.*.args
claude
.claire/
.clone/
skills/test/

# Gradle / Android build outputs that were never meaningful to track
multiproc-test-gradle/.gradle/
multiproc-test-gradle/app/build/
multiproc-test-gradle/local.properties
westlake-host-gradle/local.properties
westlake-host-gradle/gradlew.bat
test-apps/*/build/

# Untracked artifact subtrees with no committed contents (probes / scratch experiments).
# Note: artifacts/real-mcd/, artifacts/noice-1day/, artifacts/ohos-mvp/ are NOT ignored
# here because they contain tracked files that downstream tooling references.
artifacts/icu-probe/
artifacts/mcd-westlake/
artifacts/noice-westlake/

# Legacy shim snapshot from before the 2026-05-07 framework-jar dedupe.
shim.bak.before-dedupe-*/

# AOSP-port daemon build outputs and extracted sysroots — regenerated by
# each port's build.sh / Makefile.
aosp-libbinder-port/out/
aosp-libbinder-port/ohos-sysroot/
aosp-audio-daemon-port/out/
aosp-surface-daemon-port/out/

# Scratch directory created when a probe script's $(date) expansion gets
# captured literally as a directory name.
artifacts/real-mcd/current_phone_check_$(date*
