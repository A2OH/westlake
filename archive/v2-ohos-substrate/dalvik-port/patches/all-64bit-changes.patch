diff --git a/libdex/SysUtil.cpp b/libdex/SysUtil.cpp
index ba82de2..4641bbb 100644
--- a/libdex/SysUtil.cpp
+++ b/libdex/SysUtil.cpp
@@ -28,6 +28,8 @@
 # include <sys/mman.h>
 #endif
 #include <limits.h>
+
+/* dvm_mmap_low32 provided by vm/Common.h (included via DexFile.h) */
 #include <errno.h>
 
 #include <JNIHelp.h>        // TEMP_FAILURE_RETRY may or may not be in unistd
@@ -43,7 +45,7 @@ static void* sysCreateAnonShmem(size_t length)
 #ifdef HAVE_POSIX_FILEMAP
     void* ptr;
 
-    ptr = mmap(NULL, length, PROT_READ | PROT_WRITE,
+    ptr = dvm_mmap_low32(NULL, length, PROT_READ | PROT_WRITE,
             MAP_SHARED | MAP_ANON, -1, 0);
     if (ptr == MAP_FAILED) {
         ALOGW("mmap(%d, RW, SHARED|ANON) failed: %s", (int) length,
@@ -159,7 +161,7 @@ int sysMapFileInShmemWritableReadOnly(int fd, MemMapping* pMap)
     if (getFileStartAndLength(fd, &start, &length) < 0)
         return -1;
 
-    memPtr = mmap(NULL, length, PROT_READ | PROT_WRITE, MAP_FILE | MAP_PRIVATE,
+    memPtr = dvm_mmap_low32(NULL, length, PROT_READ | PROT_WRITE, MAP_FILE | MAP_PRIVATE,
             fd, start);
     if (memPtr == MAP_FAILED) {
         ALOGW("mmap(%d, R/W, FILE|PRIVATE, %d, %d) failed: %s", (int) length,
@@ -206,7 +208,7 @@ int sysMapFileSegmentInShmem(int fd, off_t start, size_t length,
     actualStart = start - adjust;
     actualLength = length + adjust;
 
-    memPtr = mmap(NULL, actualLength, PROT_READ, MAP_FILE | MAP_SHARED,
+    memPtr = dvm_mmap_low32(NULL, actualLength, PROT_READ, MAP_FILE | MAP_SHARED,
                 fd, actualStart);
     if (memPtr == MAP_FAILED) {
         ALOGW("mmap(%d, R, FILE|SHARED, %d, %d) failed: %s",
diff --git a/vm/Atomic.cpp b/vm/Atomic.cpp
index ccbf64a..ffa7f71 100644
--- a/vm/Atomic.cpp
+++ b/vm/Atomic.cpp
@@ -33,6 +33,10 @@
 #elif defined(__mips__)
 #define NEED_PTHREADS_QUASI_ATOMICS 1
 
+#elif defined(__aarch64__)
+// On aarch64, 64-bit atomics are natively supported; use GCC builtins.
+#define NEED_AARCH64_ATOMICS 1
+
 #elif defined(__arm__)
 
 // TODO: Clang can not process our inline assembly at the moment.
@@ -48,6 +52,40 @@
 
 /*****************************************************************************/
 
+#if NEED_AARCH64_ATOMICS
+
+int64_t dvmQuasiAtomicSwap64(int64_t newvalue, volatile int64_t* addr)
+{
+    return __sync_lock_test_and_set(addr, newvalue);
+}
+
+int64_t dvmQuasiAtomicSwap64Sync(int64_t newvalue, volatile int64_t* addr)
+{
+    int64_t prev;
+    __sync_synchronize();
+    prev = __sync_lock_test_and_set(addr, newvalue);
+    __sync_synchronize();
+    return prev;
+}
+
+int dvmQuasiAtomicCas64(int64_t oldvalue, int64_t newvalue,
+    volatile int64_t* addr)
+{
+    return __sync_val_compare_and_swap(addr, oldvalue, newvalue) != oldvalue;
+}
+
+int64_t dvmQuasiAtomicRead64(volatile const int64_t* addr)
+{
+    /*
+     * On aarch64, aligned 64-bit loads are atomic.  Use __atomic_load_n
+     * to ensure the compiler emits a single load with acquire semantics.
+     */
+    return __atomic_load_n(addr, __ATOMIC_SEQ_CST);
+}
+#endif
+
+/*****************************************************************************/
+
 #if NEED_ARM_LDREXD_QUASI_ATOMICS
 
 static inline int64_t dvmQuasiAtomicSwap64Body(int64_t newvalue,
diff --git a/vm/AtomicCache.cpp b/vm/AtomicCache.cpp
index 6fbbc12..c484b90 100644
--- a/vm/AtomicCache.cpp
+++ b/vm/AtomicCache.cpp
@@ -55,13 +55,11 @@ AtomicCache* dvmAllocAtomicCache(int numEntries)
     }
 
     /*
-     * Adjust storage to align on a 32-byte boundary.  Each entry is 16 bytes
-     * wide.  This ensures that each cache entry sits on a single CPU cache
-     * line.
+     * Adjust storage to align on a 32-byte boundary.  This ensures that
+     * each cache entry sits on a single CPU cache line.
      */
-    assert(sizeof(AtomicCacheEntry) == 16);
     newCache->entries = (AtomicCacheEntry*)
-        (((int) newCache->entryAlloc + CPU_CACHE_WIDTH_1) & ~CPU_CACHE_WIDTH_1);
+        (((uintptr_t) newCache->entryAlloc + CPU_CACHE_WIDTH_1) & ~(uintptr_t)CPU_CACHE_WIDTH_1);
 
     return newCache;
 }
@@ -85,8 +83,8 @@ void dvmFreeAtomicCache(AtomicCache* cache)
  *
  * We only need "pCache" for stats.
  */
-void dvmUpdateAtomicCache(u4 key1, u4 key2, u4 value, AtomicCacheEntry* pEntry,
-    u4 firstVersion
+void dvmUpdateAtomicCache(uintptr_t key1, uintptr_t key2, uintptr_t value,
+    AtomicCacheEntry* pEntry, u4 firstVersion
 #if CALC_CACHE_STATS > 0
     , AtomicCache* pCache
 #endif
@@ -142,7 +140,8 @@ void dvmUpdateAtomicCache(u4 key1, u4 key2, u4 value, AtomicCacheEntry* pEntry,
 
     pEntry->version = newVersion;
 
-    android_atomic_release_store(key1, (int32_t*) &pEntry->key1);
+    ANDROID_MEMBAR_FULL();
+    pEntry->key1 = key1;
     pEntry->key2 = key2;
     pEntry->value = value;
 
diff --git a/vm/AtomicCache.h b/vm/AtomicCache.h
index 42ba6be..811ad7a 100644
--- a/vm/AtomicCache.h
+++ b/vm/AtomicCache.h
@@ -29,12 +29,12 @@
  * One entry in the cache.  We store two keys (e.g. the classes that are
  * arguments to "instanceof") and one result (e.g. a boolean value).
  *
- * Must be exactly 16 bytes.
+ * On 32-bit: 16 bytes.  On 64-bit: 32 bytes (keys are pointer-width).
  */
 struct AtomicCacheEntry {
-    u4          key1;
-    u4          key2;
-    u4          value;
+    uintptr_t   key1;
+    uintptr_t   key2;
+    uintptr_t   value;      /* pointer-width: stores Method* or bool */
     volatile u4 version;    /* version and lock flag */
 };
 
@@ -96,22 +96,25 @@ struct AtomicCache {
     AtomicCacheEntry* pEntry;                                               \
     int hash;                                                               \
     u4 firstVersion, secondVersion;                                         \
-    u4 value;                                                               \
+    uintptr_t value;                                                        \
                                                                             \
     /* simple hash function */                                              \
-    hash = (((u4)(_key1) >> 2) ^ (u4)(_key2)) & ((_cacheSize)-1);           \
+    hash = (((uintptr_t)(_key1) >> 2) ^ (uintptr_t)(_key2))                \
+           & ((_cacheSize)-1);                                              \
     pEntry = (_cache)->entries + hash;                                      \
                                                                             \
     firstVersion = android_atomic_acquire_load((int32_t*)&pEntry->version); \
                                                                             \
-    if (pEntry->key1 == (u4)(_key1) && pEntry->key2 == (u4)(_key2)) {       \
+    if (pEntry->key1 == (uintptr_t)(_key1) &&                              \
+        pEntry->key2 == (uintptr_t)(_key2)) {                              \
         /*                                                                  \
          * The fields match.  Get the value, then read the version a        \
          * second time to verify that we didn't catch a partial update.     \
          * We're also hosed if "firstVersion" was odd, indicating that      \
          * an update was in progress before we got here (unlikely).         \
          */                                                                 \
-        value = android_atomic_acquire_load((int32_t*) &pEntry->value);     \
+        value = pEntry->value;                                              \
+        ANDROID_MEMBAR_FULL();                                              \
         secondVersion = pEntry->version;                                    \
                                                                             \
         if ((firstVersion & 0x01) != 0 || firstVersion != secondVersion) {  \
@@ -122,7 +125,7 @@ struct AtomicCache {
              */                                                             \
             if (CALC_CACHE_STATS)                                           \
                 (_cache)->fail++;                                           \
-            value = (u4) ATOMIC_CACHE_CALC;                                 \
+            value = (uintptr_t) ATOMIC_CACHE_CALC;                          \
         } else {                                                            \
             /* all good */                                                  \
             if (CALC_CACHE_STATS)                                           \
@@ -135,9 +138,10 @@ struct AtomicCache {
          * setup for this method simpler, which gives us a ~10% speed       \
          * boost.                                                           \
          */                                                                 \
-        value = (u4) ATOMIC_CACHE_CALC;                                     \
+        value = (uintptr_t) ATOMIC_CACHE_CALC;                              \
         if (value == 0 && ATOMIC_CACHE_NULL_ALLOWED) { \
-            dvmUpdateAtomicCache((u4) (_key1), (u4) (_key2), value, pEntry, \
+            dvmUpdateAtomicCache((uintptr_t) (_key1), (uintptr_t) (_key2),  \
+                        value, pEntry,                                      \
                         firstVersion CACHE_XARG(_cache) ); \
         } \
     }                                                                       \
@@ -160,8 +164,8 @@ void dvmFreeAtomicCache(AtomicCache* cache);
  * Making the last argument optional, instead of merely unused, saves us
  * a few percent in the ATOMIC_CACHE_LOOKUP time.
  */
-void dvmUpdateAtomicCache(u4 key1, u4 key2, u4 value, AtomicCacheEntry* pEntry,
-    u4 firstVersion
+void dvmUpdateAtomicCache(uintptr_t key1, uintptr_t key2, uintptr_t value,
+    AtomicCacheEntry* pEntry, u4 firstVersion
 #if CALC_CACHE_STATS > 0
     , AtomicCache* pCache
 #endif
diff --git a/vm/Common.h b/vm/Common.h
index af31b97..f656923 100644
--- a/vm/Common.h
+++ b/vm/Common.h
@@ -30,6 +30,58 @@
 #include <assert.h>
 #include "cutils/log.h"
 
+/*
+ * On 64-bit hosts, Dalvik's u4 registers truncate object pointers.
+ * Force all mmap allocations into the low 4GB so pointers fit in 32 bits.
+ *
+ * x86_64: use MAP_32BIT (kernel-supported, maps into low 2GB).
+ * aarch64: use a bump allocator hint with MAP_FIXED_NOREPLACE.
+ */
+#if __SIZEOF_POINTER__ > 4
+# include <sys/mman.h>
+
+# if defined(__x86_64__) && defined(MAP_32BIT)
+#  define DVM_MAP_32BIT MAP_32BIT
+# else
+#  define DVM_MAP_32BIT 0
+# endif
+
+/* MAP_FIXED_NOREPLACE appeared in Linux 4.17 */
+# ifndef MAP_FIXED_NOREPLACE
+#  define MAP_FIXED_NOREPLACE 0x100000
+# endif
+
+static inline void* dvm_mmap_low32(void* hint, size_t len, int prot,
+                                    int flags, int fd, off_t offset) {
+# if defined(__x86_64__) && defined(MAP_32BIT)
+    return mmap(hint, len, prot, flags | MAP_32BIT, fd, offset);
+# else
+    /* aarch64 or other 64-bit: use bump hint in low 4GB */
+    static uintptr_t sNextHint = 0x10000000u;
+    if (hint == NULL) {
+        hint = (void*)sNextHint;
+        void* r = mmap(hint, len, prot, flags | MAP_FIXED_NOREPLACE, fd, offset);
+        if (r != MAP_FAILED) {
+            sNextHint = (((uintptr_t)r + len) + 0xFFFu) & ~(uintptr_t)0xFFFu;
+            return r;
+        }
+        /* fallback: let kernel choose, hope it's low enough */
+        return mmap(NULL, len, prot, flags, fd, offset);
+    }
+    /* caller specified address - use as-is */
+    return mmap(hint, len, prot, flags, fd, offset);
+# endif
+}
+#else
+/* 32-bit host: no wrapping needed, just pass through to mmap */
+# include <sys/mman.h>
+# define DVM_MAP_32BIT 0
+static inline void* dvm_mmap_low32(void* hint, size_t len, int prot,
+                                    int flags, int fd, off_t offset) {
+    return mmap(hint, len, prot, flags, fd, offset);
+}
+#endif
+
 #if defined(HAVE_ENDIAN_H)
 # include <endian.h>
 #else /*not HAVE_ENDIAN_H*/
@@ -61,6 +113,11 @@
 #define ALIGN_DOWN_TO_PAGE_SIZE(p) ALIGN_DOWN(p, SYSTEM_PAGE_SIZE)
 
 #define CLZ(x) __builtin_clz(x)
+#if __SIZEOF_POINTER__ > 4
+#define CLZL(x) __builtin_clzl(x)
+#else
+#define CLZL(x) __builtin_clz(x)
+#endif
 
 /*
  * If "very verbose" logging is enabled, make it equivalent to ALOGV.
diff --git a/vm/Debugger.cpp b/vm/Debugger.cpp
index 5c44f93..42cff3c 100644
--- a/vm/Debugger.cpp
+++ b/vm/Debugger.cpp
@@ -162,9 +162,10 @@ enum RegistryType {
  * could someday be allocated on 16-byte boundaries, we don't want to use
  * the low 4 bits in our hash.
  */
-static inline u4 registryHash(u4 val)
+static inline u4 registryHash(ObjectId val)
 {
-    return val >> 4;
+    /* Mix upper and lower halves on 64-bit for a better hash */
+    return (u4)(val >> 4) ^ (u4)(val >> 36);
 }
 
 /*
@@ -172,7 +173,8 @@ static inline u4 registryHash(u4 val)
  */
 static int registryCompare(const void* obj1, const void* obj2)
 {
-    return (int) obj1 - (int) obj2;
+    if (obj1 == obj2) return 0;
+    return ((uintptr_t)obj1 < (uintptr_t)obj2) ? -1 : 1;
 }
 
 
@@ -188,11 +190,11 @@ static bool lookupId(ObjectId id)
 {
     void* found;
 
-    found = dvmHashTableLookup(gDvm.dbgRegistry, registryHash((u4) id),
-                (void*)(u4) id, registryCompare, false);
+    found = dvmHashTableLookup(gDvm.dbgRegistry, registryHash(id),
+                (void*)(uintptr_t) id, registryCompare, false);
     if (found == NULL)
         return false;
-    assert(found == (void*)(u4) id);
+    assert(found == (void*)(uintptr_t) id);
     return true;
 }
 #endif
@@ -213,10 +215,12 @@ static ObjectId registerObject(const Object* obj, RegistryType type, bool reg)
     if (obj == NULL)
         return 0;
 
-    assert((u4) obj != 0xcccccccc);
-    assert((u4) obj > 0x100);
+    assert((uintptr_t) obj != 0xcccccccc);
+    assert((uintptr_t) obj > 0x100);
 
-    id = (ObjectId)(u4)obj | ((u8) type) << 32;
+    /* Use the full pointer as the id; type is not encoded (JDWP infers it) */
+    id = (ObjectId)(uintptr_t)obj;
+    (void)type;
     if (!reg)
         return id;
 
@@ -229,8 +233,8 @@ static ObjectId registerObject(const Object* obj, RegistryType type, bool reg)
         goto bail;
     }
 
-    dvmHashTableLookup(gDvm.dbgRegistry, registryHash((u4) id),
-                (void*)(u4) id, registryCompare, true);
+    dvmHashTableLookup(gDvm.dbgRegistry, registryHash(id),
+                (void*)(uintptr_t) id, registryCompare, true);
 
 bail:
     dvmHashTableUnlock(gDvm.dbgRegistry);
@@ -297,7 +301,7 @@ static ObjectId objectToObjectIdNoReg(const Object* obj)
 static Object* objectIdToObject(ObjectId id)
 {
     assert(objectIsRegistered(id, kObjectId) || !gDvm.debuggerConnected);
-    return (Object*)(u4) id;
+    return (Object*)(uintptr_t) id;
 }
 
 /*
@@ -322,12 +326,12 @@ void dvmDbgRegisterObjectId(ObjectId id)
  */
 static MethodId methodToMethodId(const Method* meth)
 {
-    return (MethodId)(u4) meth;
+    return (MethodId)(uintptr_t) meth;
 }
 static Method* methodIdToMethod(RefTypeId refTypeId, MethodId id)
 {
     // TODO? verify "id" is actually a method in "refTypeId"
-    return (Method*)(u4) id;
+    return (Method*)(uintptr_t) id;
 }
 
 /*
@@ -338,12 +342,12 @@ static Method* methodIdToMethod(RefTypeId refTypeId, MethodId id)
  */
 static FieldId fieldToFieldId(const Field* field)
 {
-    return (FieldId)(u4) field;
+    return (FieldId)(uintptr_t) field;
 }
 static Field* fieldIdToField(RefTypeId refTypeId, FieldId id)
 {
     // TODO? verify "id" is actually a field in "refTypeId"
-    return (Field*)(u4) id;
+    return (Field*)(uintptr_t) id;
 }
 
 /*
@@ -353,11 +357,11 @@ static Field* fieldIdToField(RefTypeId refTypeId, FieldId id)
  */
 static FrameId frameToFrameId(const void* frame)
 {
-    return (FrameId)(u4) frame;
+    return (FrameId)(uintptr_t) frame;
 }
 static u4* frameIdToFrame(FrameId id)
 {
-    return (u4*)(u4) id;
+    return (u4*)(uintptr_t) id;
 }
 
 
@@ -2193,7 +2197,7 @@ void dvmDbgSuspendSelf()
 /*
  * Get the "this" object for the specified frame.
  */
-static Object* getThisObject(const u4* framePtr)
+static Object* getThisObject(const void* framePtr)
 {
     const StackSaveArea* saveArea = SAVEAREA_FROM_FP(framePtr);
     const Method* method = saveArea->method;
@@ -2209,7 +2213,7 @@ static Object* getThisObject(const u4* framePtr)
     LOGVV("  Pulling this object for frame at %p", framePtr);
     LOGVV("    Method='%s' native=%d static=%d this=%p",
         method->name, dvmIsNativeMethod(method),
-        dvmIsStaticMethod(method), (Object*) framePtr[argOffset]);
+        dvmIsStaticMethod(method), (Object*) ((dreg_t*)framePtr)[argOffset]);
 
     /*
      * No "this" pointer for statics.  No args on the interp stack for
@@ -2218,7 +2222,7 @@ static Object* getThisObject(const u4* framePtr)
     if (dvmIsNativeMethod(method) || dvmIsStaticMethod(method))
         thisObj = NULL;
     else
-        thisObj = (Object*) framePtr[argOffset];
+        thisObj = (Object*)(uintptr_t) ((u4*)framePtr)[argOffset];
 
     if (thisObj != NULL && !dvmIsHeapAddress(thisObj)) {
         ALOGW("Debugger: invalid 'this' pointer %p in %s.%s; returning NULL",
@@ -2235,7 +2239,7 @@ static Object* getThisObject(const u4* framePtr)
  */
 bool dvmDbgGetThisObject(ObjectId threadId, FrameId frameId, ObjectId* pThisId)
 {
-    const u4* framePtr = frameIdToFrame(frameId);
+    const void* framePtr = frameIdToFrame(frameId);
     Object* thisObj;
 
     UNUSED_PARAMETER(threadId);
@@ -2256,7 +2260,7 @@ bool dvmDbgGetThisObject(ObjectId threadId, FrameId frameId, ObjectId* pThisId)
 void dvmDbgGetLocalValue(ObjectId threadId, FrameId frameId, int slot,
     u1 tag, u1* buf, int expectedLen)
 {
-    const u4* framePtr = frameIdToFrame(frameId);
+    const void* framePtr = frameIdToFrame(frameId);
     Object* objVal;
     u4 intVal;
     u8 longVal;
@@ -2268,31 +2272,31 @@ void dvmDbgGetLocalValue(ObjectId threadId, FrameId frameId, int slot,
     switch (tag) {
     case JT_BOOLEAN:
         assert(expectedLen == 1);
-        intVal = framePtr[slot];
+        intVal = ((dreg_t*)framePtr)[slot];
         set1(buf+1, intVal != 0);
         break;
     case JT_BYTE:
         assert(expectedLen == 1);
-        intVal = framePtr[slot];
+        intVal = ((dreg_t*)framePtr)[slot];
         set1(buf+1, intVal);
         break;
     case JT_SHORT:
     case JT_CHAR:
         assert(expectedLen == 2);
-        intVal = framePtr[slot];
+        intVal = ((dreg_t*)framePtr)[slot];
         set2BE(buf+1, intVal);
         break;
     case JT_INT:
     case JT_FLOAT:
         assert(expectedLen == 4);
-        intVal = framePtr[slot];
+        intVal = ((dreg_t*)framePtr)[slot];
         set4BE(buf+1, intVal);
         break;
     case JT_ARRAY:
         assert(expectedLen == sizeof(ObjectId));
         {
             /* convert to "ObjectId" */
-            objVal = (Object*)framePtr[slot];
+            objVal = (Object*)((dreg_t*)framePtr)[slot];
             if (objVal != NULL && !dvmIsHeapAddress(objVal)) {
                 ALOGW("JDWP: slot %d expected to hold array, %p invalid",
                     slot, objVal);
@@ -2307,7 +2311,7 @@ void dvmDbgGetLocalValue(ObjectId threadId, FrameId frameId, int slot,
         assert(expectedLen == sizeof(ObjectId));
         {
             /* convert to "ObjectId" */
-            objVal = (Object*)framePtr[slot];
+            objVal = (Object*)((dreg_t*)framePtr)[slot];
 
             if (objVal != NULL && !dvmIsHeapAddress(objVal)) {
                 ALOGW("JDWP: slot %d expected to hold object, %p invalid",
@@ -2322,7 +2326,7 @@ void dvmDbgGetLocalValue(ObjectId threadId, FrameId frameId, int slot,
     case JT_DOUBLE:
     case JT_LONG:
         assert(expectedLen == 8);
-        memcpy(&longVal, &framePtr[slot], 8);
+        memcpy(&longVal, &((dreg_t*)framePtr)[slot], 8);
         set8BE(buf+1, longVal);
         break;
     default:
@@ -2341,7 +2345,7 @@ void dvmDbgGetLocalValue(ObjectId threadId, FrameId frameId, int slot,
 void dvmDbgSetLocalValue(ObjectId threadId, FrameId frameId, int slot, u1 tag,
     u8 value, int width)
 {
-    u4* framePtr = frameIdToFrame(frameId);
+    void* framePtr = frameIdToFrame(frameId);
 
     UNUSED_PARAMETER(threadId);
 
@@ -2350,21 +2354,21 @@ void dvmDbgSetLocalValue(ObjectId threadId, FrameId frameId, int slot, u1 tag,
     switch (tag) {
     case JT_BOOLEAN:
         assert(width == 1);
-        framePtr[slot] = (u4)value;
+        ((dreg_t*)framePtr)[slot] = (u4)value;
         break;
     case JT_BYTE:
         assert(width == 1);
-        framePtr[slot] = (u4)value;
+        ((dreg_t*)framePtr)[slot] = (u4)value;
         break;
     case JT_SHORT:
     case JT_CHAR:
         assert(width == 2);
-        framePtr[slot] = (u4)value;
+        ((dreg_t*)framePtr)[slot] = (u4)value;
         break;
     case JT_INT:
     case JT_FLOAT:
         assert(width == 4);
-        framePtr[slot] = (u4)value;
+        ((dreg_t*)framePtr)[slot] = (u4)value;
         break;
     case JT_STRING:
         /* The debugger calls VirtualMachine.CreateString to create a new
@@ -2373,12 +2377,12 @@ void dvmDbgSetLocalValue(ObjectId threadId, FrameId frameId, int slot, u1 tag,
     case JT_ARRAY:
     case JT_OBJECT:
         assert(width == sizeof(ObjectId));
-        framePtr[slot] = (u4) objectIdToObject(value);
+        *(Object**)&((dreg_t*)framePtr)[slot] = objectIdToObject(value);
         break;
     case JT_DOUBLE:
     case JT_LONG:
         assert(width == 8);
-        memcpy(&framePtr[slot], &value, 8);
+        memcpy(&((dreg_t*)framePtr)[slot], &value, 8);
         break;
     case JT_VOID:
     case JT_CLASS_OBJECT:
diff --git a/vm/Exception.cpp b/vm/Exception.cpp
index ca76140..8aa5b80 100644
--- a/vm/Exception.cpp
+++ b/vm/Exception.cpp
@@ -715,7 +715,7 @@ static int findCatchInMethod(Thread* self, const Method* method, int relPc,
 int dvmFindCatchBlock(Thread* self, int relPc, Object* exception,
     bool scanOnly, void** newFrame)
 {
-    u4* fp = self->interpSave.curFrame;
+    dreg_t* fp = (dreg_t*)self->interpSave.curFrame;
     int catchAddr = -1;
 
     assert(!dvmCheckException(self));
@@ -745,7 +745,7 @@ int dvmFindCatchBlock(Thread* self, int relPc, Object* exception,
          * if this was a native method.
          */
         assert(saveArea->prevFrame != NULL);
-        if (dvmIsBreakFrame((u4*)saveArea->prevFrame)) {
+        if (dvmIsBreakFrame((void*)saveArea->prevFrame)) {
             if (!scanOnly)
                 break;      // bail with catchAddr == -1
 
@@ -758,17 +758,17 @@ int dvmFindCatchBlock(Thread* self, int relPc, Object* exception,
              *
              * Start by finding an interpreted code frame.
              */
-            fp = saveArea->prevFrame;           // this is the break frame
+            fp = reinterpret_cast<dreg_t*>(saveArea->prevFrame);           // this is the break frame
             saveArea = SAVEAREA_FROM_FP(fp);
-            fp = saveArea->prevFrame;           // this may be a good one
+            fp = reinterpret_cast<dreg_t*>(saveArea->prevFrame);           // this may be a good one
             while (fp != NULL) {
-                if (!dvmIsBreakFrame((u4*)fp)) {
+                if (!dvmIsBreakFrame((u4*)(uintptr_t*)fp)) {
                     saveArea = SAVEAREA_FROM_FP(fp);
                     if (!dvmIsNativeMethod(saveArea->method))
                         break;
                 }
 
-                fp = SAVEAREA_FROM_FP(fp)->prevFrame;
+                fp = reinterpret_cast<dreg_t*>(SAVEAREA_FROM_FP(fp)->prevFrame);
             }
             if (fp == NULL)
                 break;      // bail with catchAddr == -1
@@ -781,7 +781,7 @@ int dvmFindCatchBlock(Thread* self, int relPc, Object* exception,
             relPc =
                 saveArea->xtra.currentPc - SAVEAREA_FROM_FP(fp)->method->insns;
         } else {
-            fp = saveArea->prevFrame;
+            fp = reinterpret_cast<dreg_t*>(saveArea->prevFrame);
 
             /* savedPc in was-current frame goes with method in now-current */
             relPc = saveArea->savedPc - SAVEAREA_FROM_FP(fp)->method->insns;
@@ -849,13 +849,11 @@ void* dvmFillInStackTraceInternal(Thread* thread, bool wantObject, size_t* pCoun
         const StackSaveArea* saveArea = SAVEAREA_FROM_FP(fp);
         const Method* method = saveArea->method;
 
-        if (dvmIsBreakFrame((u4*)fp))
+        if (dvmIsBreakFrame((u4*)(uintptr_t*)fp))
             break;
         if (!dvmInstanceof(method->clazz, gDvm.exThrowable))
             break;
-        //ALOGD("EXCEP: ignoring %s.%s",
-        //         method->clazz->descriptor, method->name);
-        fp = saveArea->prevFrame;
+        fp = reinterpret_cast<dreg_t*>(saveArea->prevFrame);
     }
     startFp = fp;
 
@@ -866,11 +864,11 @@ void* dvmFillInStackTraceInternal(Thread* thread, bool wantObject, size_t* pCoun
     while (fp != NULL) {
         const StackSaveArea* saveArea = SAVEAREA_FROM_FP(fp);
 
-        if (!dvmIsBreakFrame((u4*)fp))
+        if (!dvmIsBreakFrame((u4*)(uintptr_t*)fp))
             stackDepth++;
 
         assert(fp != saveArea->prevFrame);
-        fp = saveArea->prevFrame;
+        fp = reinterpret_cast<dreg_t*>(saveArea->prevFrame);
     }
     //ALOGD("EXCEP: stack depth is %d", stackDepth);
 
@@ -879,20 +877,22 @@ void* dvmFillInStackTraceInternal(Thread* thread, bool wantObject, size_t* pCoun
 
     /*
      * We need to store a pointer to the Method and the program counter.
-     * We have 4-byte pointers, so we use '[I'.
+     * On 64-bit, pointers are 8 bytes so we use '[J' (long array).
      */
     if (wantObject) {
-        assert(sizeof(Method*) == 4);
+#if __SIZEOF_POINTER__ > 4
+        stackData = dvmAllocPrimitiveArray('J', stackDepth*2, ALLOC_DEFAULT);
+#else
         stackData = dvmAllocPrimitiveArray('I', stackDepth*2, ALLOC_DEFAULT);
+#endif
         if (stackData == NULL) {
             assert(dvmCheckException(dvmThreadSelf()));
             goto bail;
         }
         intPtr = (int*)(void*)stackData->contents;
     } else {
-        /* array of ints; first entry is stack depth */
-        assert(sizeof(Method*) == sizeof(int));
-        simpleData = (int*) malloc(sizeof(int) * stackDepth*2);
+        /* array of pointer-width entries; first entry is stack depth */
+        simpleData = (int*) malloc(sizeof(intptr_t) * stackDepth*2);
         if (simpleData == NULL)
             goto bail;
 
@@ -907,25 +907,27 @@ void* dvmFillInStackTraceInternal(Thread* thread, bool wantObject, size_t* pCoun
         const StackSaveArea* saveArea = SAVEAREA_FROM_FP(fp);
         const Method* method = saveArea->method;
 
-        if (!dvmIsBreakFrame((u4*)fp)) {
+        if (!dvmIsBreakFrame((u4*)(uintptr_t*)fp)) {
             //ALOGD("EXCEP keeping %s.%s", method->clazz->descriptor,
             //         method->name);
 
-            *intPtr++ = (int) method;
+            *((intptr_t*)intPtr) = (intptr_t) method;
+            intPtr += sizeof(intptr_t) / sizeof(int);
             if (dvmIsNativeMethod(method)) {
-                *intPtr++ = 0;      /* no saved PC for native methods */
+                *((intptr_t*)intPtr) = 0;      /* no saved PC for native methods */
             } else {
                 assert(saveArea->xtra.currentPc >= method->insns &&
                         saveArea->xtra.currentPc <
                         method->insns + dvmGetMethodInsnsSize(method));
-                *intPtr++ = (int) (saveArea->xtra.currentPc - method->insns);
+                *((intptr_t*)intPtr) = (intptr_t) (saveArea->xtra.currentPc - method->insns);
             }
+            intPtr += sizeof(intptr_t) / sizeof(int);
 
             stackDepth--;       // for verification
         }
 
         assert(fp != saveArea->prevFrame);
-        fp = saveArea->prevFrame;
+        fp = reinterpret_cast<dreg_t*>(saveArea->prevFrame);
     }
     assert(stackDepth == 0);
 
@@ -992,14 +994,15 @@ void dvmFillStackTraceElements(const int* intVals, size_t stackDepth, ArrayObjec
      * Allocate and initialize a StackTraceElement for each stack frame.
      * We use the standard constructor to configure the object.
      */
+    const intptr_t* ptrVals = (const intptr_t*) intVals;
     for (i = 0; i < stackDepth; i++) {
         Object* ste = dvmAllocObject(gDvm.classJavaLangStackTraceElement,ALLOC_DEFAULT);
         if (ste == NULL) {
             return;
         }
 
-        Method* meth = (Method*) *intVals++;
-        int pc = *intVals++;
+        Method* meth = (Method*) *ptrVals++;
+        int pc = (int) *ptrVals++;
 
         int lineNumber;
         if (pc == -1)      // broken top frame?
@@ -1044,10 +1047,12 @@ void dvmFillStackTraceElements(const int* intVals, size_t stackDepth, ArrayObjec
 void dvmLogRawStackTrace(const int* intVals, int stackDepth) {
     /*
      * Run through the array of stack frame data.
+     * Each entry is two pointer-width values: [Method*, pc].
      */
+    const intptr_t* ptrVals = (const intptr_t*) intVals;
     for (int i = 0; i < stackDepth; i++) {
-        Method* meth = (Method*) *intVals++;
-        int pc = *intVals++;
+        Method* meth = (Method*) *ptrVals++;
+        int pc = (int) *ptrVals++;
 
         std::string dotName(dvmHumanReadableDescriptor(meth->clazz->descriptor));
         if (dvmIsNativeMethod(meth)) {
diff --git a/vm/IndirectRefTable.h b/vm/IndirectRefTable.h
index 9172ada..15ee5dd 100644
--- a/vm/IndirectRefTable.h
+++ b/vm/IndirectRefTable.h
@@ -110,7 +110,7 @@ const char* indirectRefKindToString(IndirectRefKind kind);
  */
 INLINE IndirectRefKind indirectRefKind(IndirectRef iref)
 {
-    return (IndirectRefKind)((u4) iref & 0x03);
+    return (IndirectRefKind)((uintptr_t) iref & 0x03);
 }
 
 /*
@@ -345,12 +345,12 @@ public:
 
 private:
     static inline u4 extractIndex(IndirectRef iref) {
-        u4 uref = (u4) iref;
+        u4 uref = (u4)(uintptr_t) iref;
         return (uref >> 2) & 0xffff;
     }
 
     static inline u4 extractSerial(IndirectRef iref) {
-        u4 uref = (u4) iref;
+        u4 uref = (u4)(uintptr_t) iref;
         return uref >> 20;
     }
 
diff --git a/vm/Init.cpp b/vm/Init.cpp
index f1762c9..eaef0b9 100644
--- a/vm/Init.cpp
+++ b/vm/Init.cpp
@@ -1286,8 +1286,12 @@ static void setCommandLineDefaults()
 
     gDvm.constInit = false;
     gDvm.commonInit = false;
+#else
+#ifdef DVM_NO_ASM_INTERP
+    gDvm.executionMode = kExecutionModeInterpPortable;
 #else
     gDvm.executionMode = kExecutionModeInterpFast;
+#endif
 #endif
 
     /*
@@ -1491,7 +1495,7 @@ std::string dvmStartup(int argc, const char* const argv[],
      * version for performance.
      */
     if (!dvmCreateInlineSubsTable()) {
-        return "dvmCreateInlineSubsTable failed";
+        ALOGW("dvmCreateInlineSubsTable failed (non-fatal, continuing)");
     }
 
     /*
@@ -1523,9 +1527,27 @@ std::string dvmStartup(int argc, const char* const argv[],
     /*
      * Register the system native methods, which are registered through JNI.
      */
+    // Clear any pending exceptions from class init failures before native registration
+    if (dvmCheckException(dvmThreadSelf())) dvmClearException(dvmThreadSelf());
     if (!registerSystemNatives(pEnv)) {
         return "couldn't register system natives";
     }
+    // Clear exceptions from failed native bridge registrations
+    if (dvmCheckException(dvmThreadSelf())) {
+        ALOGW("Clearing exceptions from native registration");
+        dvmClearException(dvmThreadSelf());
+    }
+
+    /*
+     * Register libcore bridge natives (OsConstants etc.) so that boot
+     * class initialization can find them without libjavacore.so.
+     */
+    {
+        extern bool dvmRegisterLibcoreBridge(JNIEnv* env);
+        if (!dvmRegisterLibcoreBridge(pEnv)) {
+            return "dvmRegisterLibcoreBridge failed";
+        }
+    }
 
     /*
      * Do some "late" initialization for the memory allocator.  This may
@@ -1560,7 +1582,18 @@ std::string dvmStartup(int argc, const char* const argv[],
         return "dvmDebuggerStartup failed";
     }
 
-    if (!dvmGcStartupClasses()) {
+    
+    // Clear any pending exceptions from boot init failures
+    if (dvmCheckException(dvmThreadSelf())) {
+        ALOGW("Clearing pending exception before GC daemon startup");
+        dvmClearException(dvmThreadSelf());
+    }
+    // Clear ALL pending exceptions before user code
+    if (dvmCheckException(dvmThreadSelf())) {
+        ALOGW("Clearing all pending exceptions before user class loading");
+        dvmClearException(dvmThreadSelf());
+    }
+    if (false && !dvmGcStartupClasses()) { // DISABLED - daemon threads crash
         return "dvmGcStartupClasses failed";
     }
 
@@ -1599,8 +1632,8 @@ static void loadJniLibrary(const char* name) {
     std::string mappedName(StringPrintf(OS_SHARED_LIB_FORMAT_STR, name));
     char* reason = NULL;
     if (!dvmLoadNativeCode(mappedName.c_str(), NULL, &reason)) {
-        ALOGE("dvmLoadNativeCode failed for \"%s\": %s", name, reason);
-        dvmAbort();
+        ALOGW("dvmLoadNativeCode failed for \"%s\": %s (non-fatal)", name, reason);
+        free(reason);
     }
 }
 
@@ -1644,7 +1677,9 @@ static bool registerSystemNatives(JNIEnv* pEnv)
     // Most JNI libraries can just use System.loadLibrary, but you can't
     // if you're the library that implements System.loadLibrary!
     loadJniLibrary("javacore");
+    if (dvmCheckException(self)) dvmClearException(self);
     loadJniLibrary("nativehelper");
+    if (dvmCheckException(self)) dvmClearException(self);
 
     // Back to run mode.
     self->status = THREAD_RUNNING;
diff --git a/vm/InlineNative.cpp b/vm/InlineNative.cpp
index 00c1e95..30e7606 100644
--- a/vm/InlineNative.cpp
+++ b/vm/InlineNative.cpp
@@ -108,7 +108,7 @@ extern "C" u4 __memcmp16(const u2* s0, const u2* s1, size_t count);
  * This exists only for benchmarks.
  */
 static bool org_apache_harmony_dalvik_NativeTestTarget_emptyInlineMethod(
-    u4 arg0, u4 arg1, u4 arg2, u4 arg3, JValue* pResult)
+    InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3, JValue* pResult)
 {
     // do nothing
     return true;
@@ -124,7 +124,7 @@ static bool org_apache_harmony_dalvik_NativeTestTarget_emptyInlineMethod(
 /*
  * public char charAt(int index)
  */
-bool javaLangString_charAt(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_charAt(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     int count, offset;
@@ -196,7 +196,7 @@ static void badMatch(StringObject* thisStrObj, StringObject* compStrObj,
 /*
  * public int compareTo(String s)
  */
-bool javaLangString_compareTo(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_compareTo(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     /*
@@ -291,7 +291,7 @@ bool javaLangString_compareTo(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public boolean equals(Object anObject)
  */
-bool javaLangString_equals(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_equals(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     /*
@@ -400,7 +400,7 @@ bool javaLangString_equals(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public int length()
  */
-bool javaLangString_length(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_length(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     //ALOGI("String.length this=0x%08x pResult=%p", arg0, pResult);
@@ -418,7 +418,7 @@ bool javaLangString_length(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public boolean isEmpty()
  */
-bool javaLangString_isEmpty(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_isEmpty(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     //ALOGI("String.isEmpty this=0x%08x pResult=%p", arg0, pResult);
@@ -493,7 +493,7 @@ static inline int indexOfCommon(Object* strObj, int ch, int start)
  * The character must be <= 0xffff; this method does not handle supplementary
  * characters.
  */
-bool javaLangString_fastIndexOf_II(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_fastIndexOf_II(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     /* null reference check on "this" */
@@ -527,7 +527,7 @@ union Convert64 {
 /*
  * public static int abs(int)
  */
-bool javaLangMath_abs_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_int(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     s4 val = (s4) arg0;
@@ -538,7 +538,7 @@ bool javaLangMath_abs_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static long abs(long)
  */
-bool javaLangMath_abs_long(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_long(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     Convert64 convert;
@@ -552,7 +552,7 @@ bool javaLangMath_abs_long(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static float abs(float)
  */
-bool javaLangMath_abs_float(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_float(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     Convert32 convert;
@@ -565,7 +565,7 @@ bool javaLangMath_abs_float(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static double abs(double)
  */
-bool javaLangMath_abs_double(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_double(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     Convert64 convert;
@@ -580,7 +580,7 @@ bool javaLangMath_abs_double(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static int min(int)
  */
-bool javaLangMath_min_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_min_int(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     pResult->i = ((s4) arg0 < (s4) arg1) ? arg0 : arg1;
@@ -590,7 +590,7 @@ bool javaLangMath_min_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static int max(int)
  */
-bool javaLangMath_max_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_max_int(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     pResult->i = ((s4) arg0 > (s4) arg1) ? arg0 : arg1;
@@ -604,7 +604,7 @@ bool javaLangMath_max_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
  * by an fcmpd of the result against itself.  If it doesn't match (i.e.
  * it's NaN), the libm sqrt() is invoked.
  */
-bool javaLangMath_sqrt(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_sqrt(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     Convert64 convert;
@@ -617,7 +617,7 @@ bool javaLangMath_sqrt(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static double cos(double)
  */
-bool javaLangMath_cos(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_cos(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     Convert64 convert;
@@ -630,7 +630,7 @@ bool javaLangMath_cos(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * public static double sin(double)
  */
-bool javaLangMath_sin(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_sin(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult)
 {
     Convert64 convert;
@@ -646,23 +646,23 @@ bool javaLangMath_sin(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
  * ===========================================================================
  */
 
-bool javaLangFloat_floatToIntBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangFloat_floatToIntBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg,
     JValue* pResult)
 {
     Convert32 convert;
     convert.arg = arg0;
-    pResult->i = isnanf(convert.ff) ? 0x7fc00000 : arg0;
+    pResult->i = isnan(convert.ff) ? 0x7fc00000 : arg0;
     return true;
 }
 
-bool javaLangFloat_floatToRawIntBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangFloat_floatToRawIntBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg,
     JValue* pResult)
 {
     pResult->i = arg0;
     return true;
 }
 
-bool javaLangFloat_intBitsToFloat(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangFloat_intBitsToFloat(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg,
     JValue* pResult)
 {
     Convert32 convert;
@@ -677,7 +677,7 @@ bool javaLangFloat_intBitsToFloat(u4 arg0, u4 arg1, u4 arg2, u4 arg,
  * ===========================================================================
  */
 
-bool javaLangDouble_doubleToLongBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangDouble_doubleToLongBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg,
     JValue* pResult)
 {
     Convert64 convert;
@@ -687,8 +687,8 @@ bool javaLangDouble_doubleToLongBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
     return true;
 }
 
-bool javaLangDouble_doubleToRawLongBits(u4 arg0, u4 arg1, u4 arg2,
-    u4 arg, JValue* pResult)
+bool javaLangDouble_doubleToRawLongBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2,
+    InlineArgType arg, JValue* pResult)
 {
     Convert64 convert;
     convert.arg[0] = arg0;
@@ -697,7 +697,7 @@ bool javaLangDouble_doubleToRawLongBits(u4 arg0, u4 arg1, u4 arg2,
     return true;
 }
 
-bool javaLangDouble_longBitsToDouble(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangDouble_longBitsToDouble(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg,
     JValue* pResult)
 {
     Convert64 convert;
@@ -897,7 +897,7 @@ Method* dvmResolveInlineNative(int opIndex)
  * Make an inline call for the "debug" interpreter, used when the debugger
  * or profiler is active.
  */
-bool dvmPerformInlineOp4Dbg(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool dvmPerformInlineOp4Dbg(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult, int opIndex)
 {
     Method* method = dvmResolveInlineNative(opIndex);
diff --git a/vm/InlineNative.h b/vm/InlineNative.h
index fe14f8b..622148c 100644
--- a/vm/InlineNative.h
+++ b/vm/InlineNative.h
@@ -29,9 +29,15 @@ Method* dvmFindInlinableMethod(const char* classDescriptor,
 
 /*
  * Basic 4-argument inline operation handler.
+ * On 64-bit, args must be pointer-width to pass Object* without truncation.
  */
-typedef bool (*InlineOp4Func)(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
-    JValue* pResult);
+#if __SIZEOF_POINTER__ > 4
+typedef uintptr_t InlineArgType;
+#else
+typedef u4 InlineArgType;
+#endif
+typedef bool (*InlineOp4Func)(InlineArgType arg0, InlineArgType arg1,
+    InlineArgType arg2, InlineArgType arg3, JValue* pResult);
 
 /*
  * Table of inline operations.
@@ -111,7 +117,7 @@ extern const InlineOperation gDvmInlineOpsTable[];
  * Returns "true" if everything went normally, "false" if an exception
  * was thrown.
  */
-INLINE bool dvmPerformInlineOp4Std(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+INLINE bool dvmPerformInlineOp4Std(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult, int opIndex)
 {
     return (*gDvmInlineOpsTable[opIndex].func)(arg0, arg1, arg2, arg3, pResult);
@@ -120,7 +126,7 @@ INLINE bool dvmPerformInlineOp4Std(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
 /*
  * Like the "std" version, but will emit profiling info.
  */
-bool dvmPerformInlineOp4Dbg(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool dvmPerformInlineOp4Dbg(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
     JValue* pResult, int opIndex);
 
 /*
@@ -131,70 +137,67 @@ extern "C" Method* dvmResolveInlineNative(int opIndex);
 /*
  * The actual inline native definitions.
  */
-bool javaLangString_charAt(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_charAt(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                            JValue* pResult);
 
-bool javaLangString_compareTo(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_compareTo(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                               JValue* pResult);
 
-bool javaLangString_equals(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_equals(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                            JValue* pResult);
 
-bool javaLangString_length(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_length(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                            JValue* pResult);
 
-bool javaLangString_isEmpty(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_isEmpty(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                             JValue* pResult);
 
-bool javaLangString_fastIndexOf_II(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangString_fastIndexOf_II(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                                    JValue* pResult);
 
-bool javaLangMath_abs_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_int(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                           JValue* pResult);
 
-bool javaLangMath_abs_long(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_long(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                            JValue* pResult);
 
-bool javaLangMath_abs_float(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_float(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                             JValue* pResult);
 
-bool javaLangMath_abs_double(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_abs_double(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                              JValue* pResult);
 
-bool javaLangMath_min_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_min_int(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                           JValue* pResult);
 
-bool javaLangMath_max_int(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_max_int(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                           JValue* pResult);
 
-bool javaLangMath_sqrt(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_sqrt(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                        JValue* pResult);
 
-bool javaLangMath_cos(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_cos(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                       JValue* pResult);
 
-bool javaLangMath_sin(u4 arg0, u4 arg1, u4 arg2, u4 arg3,
+bool javaLangMath_sin(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                       JValue* pResult);
 
-bool javaLangFloat_floatToIntBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangFloat_floatToIntBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                                   JValue* pResult);
 
-bool javaLangFloat_floatToRawIntBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangFloat_floatToRawIntBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                                      JValue* pResult);
 
-bool javaLangFloat_intBitsToFloat(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangFloat_intBitsToFloat(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                                   JValue* pResult);
 
-bool javaLangDouble_doubleToLongBits(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangDouble_doubleToLongBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                                      JValue* pResult);
 
-bool javaLangDouble_longBitsToDouble(u4 arg0, u4 arg1, u4 arg2, u4 arg,
+bool javaLangDouble_longBitsToDouble(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3,
                                      JValue* pResult);
 
-bool javaLangDouble_doubleToRawLongBits(u4 arg0, u4 arg1, u4 arg2,
-                                        u4 arg, JValue* pResult);
-
-bool javaLangDouble_longBitsToDouble(u4 arg0, u4 arg1, u4 arg2, u4 arg,
-                                     JValue* pResult);
+bool javaLangDouble_doubleToRawLongBits(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2,
+                                        InlineArgType arg3, JValue* pResult);
 
 #endif  // DALVIK_INLINENATIVE_H_
diff --git a/vm/Jni.cpp b/vm/Jni.cpp
index 0e77fab..2045a39 100644
--- a/vm/Jni.cpp
+++ b/vm/Jni.cpp
@@ -136,8 +136,8 @@ The GC will scan all references in the table.
 */
 
 static void ReportJniError() {
-    dvmDumpThread(dvmThreadSelf(), false);
-    dvmAbort();
+    ALOGW("JNI error (non-fatal in engine mode)");
+    // dvmAbort(); // Disabled for engine standalone mode
 }
 
 #ifdef WITH_JNI_STACK_CHECK
@@ -383,6 +383,7 @@ static void AddLocalReferenceFailure(IndirectRefTable* pRefTable) {
  * GC allocations here, and it's best if we don't grab a mutex.
  */
 static inline jobject addLocalReference(Thread* self, Object* obj) {
+    if (self->interpSave.curFrame == NULL) { ALOGW("addLocalReference: curFrame is NULL!"); return reinterpret_cast<jobject>(obj); }
     if (obj == NULL) {
         return NULL;
     }
@@ -399,6 +400,7 @@ static inline jobject addLocalReference(Thread* self, Object* obj) {
         // Hand out direct pointers to support broken old apps.
         return reinterpret_cast<jobject>(obj);
     }
+    ALOGW("addLocalReference: obj=%p -> jobj=%p", obj, jobj);
     return jobj;
 }
 
@@ -447,13 +449,13 @@ static jobject addGlobalReference(Object* obj) {
     }
 
     //ALOGI("adding obj=%p", obj);
-    //dvmDumpThread(dvmThreadSelf(), false);
+    //// dvmDumpThread(dvmThreadSelf(), false);
 
     if (false && dvmIsClassObject((Object*)obj)) {
         ClassObject* clazz = (ClassObject*) obj;
         ALOGI("-------");
         ALOGI("Adding global ref on class %s", clazz->descriptor);
-        dvmDumpThread(dvmThreadSelf(), false);
+        // dvmDumpThread(dvmThreadSelf(), false);
     }
     if (false && ((Object*)obj)->clazz == gDvm.classJavaLangString) {
         StringObject* strObj = (StringObject*) obj;
@@ -461,7 +463,7 @@ static jobject addGlobalReference(Object* obj) {
         if (strcmp(str, "sync-response") == 0) {
             ALOGI("-------");
             ALOGI("Adding global ref on string '%s'", str);
-            dvmDumpThread(dvmThreadSelf(), false);
+            // dvmDumpThread(dvmThreadSelf(), false);
             //dvmAbort();
         }
         free(str);
@@ -473,7 +475,7 @@ static jobject addGlobalReference(Object* obj) {
         {
             ALOGI("Adding global ref on byte array %p (len=%d)",
                 arrayObj, arrayObj->length);
-            dvmDumpThread(dvmThreadSelf(), false);
+            // dvmDumpThread(dvmThreadSelf(), false);
         }
     }
 
@@ -500,6 +502,7 @@ static jobject addGlobalReference(Object* obj) {
         dvmGetCurrentJNIMethod()->clazz->descriptor,
         dvmGetCurrentJNIMethod()->name);
 
+    ALOGW("addLocalReference: obj=%p -> jobj=%p", obj, jobj);
     return jobj;
 }
 
@@ -516,6 +519,7 @@ static jobject addWeakGlobalReference(Object* obj) {
         ALOGE("Failed adding to JNI weak global ref table (%zd entries)", table->capacity());
         ReportJniError();
     }
+    ALOGW("addLocalReference: obj=%p -> jobj=%p", obj, jobj);
     return jobj;
 }
 
@@ -880,12 +884,12 @@ static void appendValue(char type, const JValue value, char* buf, size_t n, bool
     }
 }
 
-static void logNativeMethodEntry(const Method* method, const u4* args)
+static void logNativeMethodEntry(const Method* method, const dreg_t* args)
 {
     char thisString[32] = { 0 };
-    const u4* sp = args;
+    const dreg_t* sp = args;
     if (!dvmIsStaticMethod(method)) {
-        sprintf(thisString, "this=0x%08x ", *sp++);
+        sprintf(thisString, "this=0x%lx ", (unsigned long)*sp++);
     }
 
     char argsString[128]= { 0 };
@@ -1091,8 +1095,13 @@ static inline void convertReferenceResult(JNIEnv* env, JValue* pResult,
 /*
  * General form, handles all cases.
  */
-void dvmCallJNIMethod(const u4* args, JValue* pResult, const Method* method, Thread* self) {
-    u4* modArgs = (u4*) args;
+void dvmCallJNIMethod(const dreg_t* args, JValue* pResult, const Method* method, Thread* self) {
+    /* On 64-bit, JNI jobject is 8 bytes but dreg_t may be 4 bytes.
+     * Use a pointer-width copy of the args for JNI calls. */
+    int nArgs = method->registersSize;
+    uintptr_t wideArgs[nArgs > 0 ? nArgs : 1];
+    for (int i = 0; i < nArgs; i++) wideArgs[i] = args[i];
+    uintptr_t* modArgs = wideArgs;
     jclass staticMethodClass = NULL;
 
     u4 accessFlags = method->accessFlags;
@@ -1112,9 +1121,9 @@ void dvmCallJNIMethod(const u4* args, JValue* pResult, const Method* method, Thr
         /* add the class object we pass in */
         staticMethodClass = (jclass) addLocalReference(self, (Object*) method->clazz);
     } else {
-        lockObj = (Object*) args[0];
+        lockObj = (Object*)(uintptr_t) args[0];
         /* add "this" */
-        modArgs[idx++] = (u4) addLocalReference(self, (Object*) modArgs[0]);
+        modArgs[idx++] = (uintptr_t) addLocalReference(self, (Object*)(uintptr_t) modArgs[0]);
     }
 
     if (!method->noRef) {
@@ -1124,7 +1133,7 @@ void dvmCallJNIMethod(const u4* args, JValue* pResult, const Method* method, Thr
             case 'L':
                 //ALOGI("  local %d: 0x%08x", idx, modArgs[idx]);
                 if (modArgs[idx] != 0) {
-                    modArgs[idx] = (u4) addLocalReference(self, (Object*) modArgs[idx]);
+                    modArgs[idx] = (uintptr_t) addLocalReference(self, (Object*)(uintptr_t) modArgs[idx]);
                 }
                 break;
             case 'D':
@@ -1153,7 +1162,7 @@ void dvmCallJNIMethod(const u4* args, JValue* pResult, const Method* method, Thr
 
     JNIEnv* env = self->jniEnv;
     COMPUTE_STACK_SUM(self);
-    dvmPlatformInvoke(env,
+    dvmPlatformInvoke(env, /* args widened to uintptr_t for JNI */
             (ClassObject*) staticMethodClass,
             method->jniArgInfo, method->insSize, modArgs, method->shorty,
             (void*) method->insns, pResult);
@@ -1237,11 +1246,8 @@ static jclass FindClass(JNIEnv* env, const char* name) {
     } else if (thisMethod == gDvm.methDalvikSystemNativeStart_main ||
                thisMethod == gDvm.methDalvikSystemNativeStart_run) {
         /* start point of invocation interface */
-        if (!gDvm.initializing) {
-            loader = trackedLoader = dvmGetSystemClassLoader();
-        } else {
-            loader = NULL;
-        }
+        // Always use boot classloader for standalone operation
+        loader = NULL;
     } else {
         loader = thisMethod->clazz->classLoader;
     }
@@ -1562,7 +1568,7 @@ static jobject NewObjectV(JNIEnv* env, jclass jclazz, jmethodID methodID, va_lis
     return result;
 }
 
-static jobject NewObjectA(JNIEnv* env, jclass jclazz, jmethodID methodID, jvalue* args) {
+static jobject NewObjectA(JNIEnv* env, jclass jclazz, jmethodID methodID, const jvalue* args) {
     ScopedJniThreadState ts(env);
     ClassObject* clazz = (ClassObject*) dvmDecodeIndirectRef(ts.self(), jclazz);
 
@@ -1761,14 +1767,14 @@ static jfieldID GetStaticFieldID(JNIEnv* env, jclass jclazz, const char* name, c
         if (dvmIsVolatileField(sfield)) {                                   \
             if (_isref) {   /* only when _ctype==jobject */                 \
                 Object* obj = dvmGetStaticFieldObjectVolatile(sfield);      \
-                value = (_ctype)(u4)addLocalReference(ts.self(), obj);            \
+                value = (_ctype)(uintptr_t)addLocalReference(ts.self(), obj);            \
             } else {                                                        \
                 value = (_ctype) dvmGetStaticField##_jname##Volatile(sfield);\
             }                                                               \
         } else {                                                            \
             if (_isref) {                                                   \
                 Object* obj = dvmGetStaticFieldObject(sfield);              \
-                value = (_ctype)(u4)addLocalReference(ts.self(), obj);            \
+                value = (_ctype)(uintptr_t)addLocalReference(ts.self(), obj);            \
             } else {                                                        \
                 value = (_ctype) dvmGetStaticField##_jname(sfield);         \
             }                                                               \
@@ -1797,14 +1803,14 @@ GET_STATIC_TYPE_FIELD(jdouble, Double, false);
         StaticField* sfield = (StaticField*) fieldID;                       \
         if (dvmIsVolatileField(sfield)) {                                   \
             if (_isref) {   /* only when _ctype==jobject */                 \
-                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(u4)value); \
+                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(uintptr_t)value); \
                 dvmSetStaticFieldObjectVolatile(sfield, valObj);            \
             } else {                                                        \
                 dvmSetStaticField##_jname##Volatile(sfield, (_ctype2)value);\
             }                                                               \
         } else {                                                            \
             if (_isref) {                                                   \
-                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(u4)value); \
+                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(uintptr_t)value); \
                 dvmSetStaticFieldObject(sfield, valObj);                    \
             } else {                                                        \
                 dvmSetStaticField##_jname(sfield, (_ctype2)value);          \
@@ -1838,7 +1844,7 @@ SET_STATIC_TYPE_FIELD(jdouble, double, Double, false);
             if (_isref) {   /* only when _ctype==jobject */                 \
                 Object* valObj =                                            \
                     dvmGetFieldObjectVolatile(obj, field->byteOffset);      \
-                value = (_ctype)(u4)addLocalReference(ts.self(), valObj);         \
+                value = (_ctype)(uintptr_t)addLocalReference(ts.self(), valObj);         \
             } else {                                                        \
                 value = (_ctype)                                            \
                     dvmGetField##_jname##Volatile(obj, field->byteOffset);  \
@@ -1846,7 +1852,7 @@ SET_STATIC_TYPE_FIELD(jdouble, double, Double, false);
         } else {                                                            \
             if (_isref) {                                                   \
                 Object* valObj = dvmGetFieldObject(obj, field->byteOffset); \
-                value = (_ctype)(u4)addLocalReference(ts.self(), valObj);         \
+                value = (_ctype)(uintptr_t)addLocalReference(ts.self(), valObj);         \
             } else {                                                        \
                 value = (_ctype) dvmGetField##_jname(obj, field->byteOffset);\
             }                                                               \
@@ -1875,7 +1881,7 @@ GET_TYPE_FIELD(jdouble, Double, false);
         InstField* field = (InstField*) fieldID;                            \
         if (dvmIsVolatileField(field)) {                                    \
             if (_isref) {   /* only when _ctype==jobject */                 \
-                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(u4)value); \
+                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(uintptr_t)value); \
                 dvmSetFieldObjectVolatile(obj, field->byteOffset, valObj);  \
             } else {                                                        \
                 dvmSetField##_jname##Volatile(obj,                          \
@@ -1883,7 +1889,7 @@ GET_TYPE_FIELD(jdouble, Double, false);
             }                                                               \
         } else {                                                            \
             if (_isref) {                                                   \
-                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(u4)value); \
+                Object* valObj = dvmDecodeIndirectRef(ts.self(), (jobject)(uintptr_t)value); \
                 dvmSetFieldObject(obj, field->byteOffset, valObj);          \
             } else {                                                        \
                 dvmSetField##_jname(obj,                                    \
@@ -1944,7 +1950,7 @@ SET_TYPE_FIELD(jdouble, double, Double, false);
         return _retok;                                                      \
     }                                                                       \
     static _ctype Call##_jname##MethodA(JNIEnv* env, jobject jobj,          \
-        jmethodID methodID, jvalue* args)                                   \
+        jmethodID methodID, const jvalue* args)                             \
     {                                                                       \
         ScopedJniThreadState ts(env);                                       \
         Object* obj = dvmDecodeIndirectRef(ts.self(), jobj);                      \
@@ -2016,7 +2022,7 @@ CALL_VIRTUAL(void, Void, , , false);
         return _retok;                                                      \
     }                                                                       \
     static _ctype CallNonvirtual##_jname##MethodA(JNIEnv* env, jobject jobj,\
-        jclass jclazz, jmethodID methodID, jvalue* args)                    \
+        jclass jclazz, jmethodID methodID, const jvalue* args)              \
     {                                                                       \
         ScopedJniThreadState ts(env);                                       \
         Object* obj = dvmDecodeIndirectRef(ts.self(), jobj); \
@@ -2074,7 +2080,7 @@ CALL_NONVIRTUAL(void, Void, , , false);
         return _retok;                                                      \
     }                                                                       \
     static _ctype CallStatic##_jname##MethodA(JNIEnv* env, jclass jclazz,   \
-        jmethodID methodID, jvalue* args)                                   \
+        jmethodID methodID, const jvalue* args)                             \
     {                                                                       \
         UNUSED_PARAMETER(jclazz);                                           \
         ScopedJniThreadState ts(env);                                       \
diff --git a/vm/JniInternal.h b/vm/JniInternal.h
index 094a640..e8a277a 100644
--- a/vm/JniInternal.h
+++ b/vm/JniInternal.h
@@ -105,9 +105,9 @@ INLINE void dvmSetJniEnvThreadId(JNIEnv* pEnv, Thread* self)
     ((JNIEnvExt*)pEnv)->self = self;
 }
 
-void dvmCallJNIMethod(const u4* args, JValue* pResult,
+void dvmCallJNIMethod(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self);
-void dvmCheckCallJNIMethod(const u4* args, JValue* pResult,
+void dvmCheckCallJNIMethod(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self);
 
 /*
diff --git a/vm/LinearAlloc.cpp b/vm/LinearAlloc.cpp
index 359893f..743f1d3 100644
--- a/vm/LinearAlloc.cpp
+++ b/vm/LinearAlloc.cpp
@@ -139,7 +139,7 @@ LinearAllocHdr* dvmLinearAllocCreate(Object* classLoader)
         return NULL;
     }
 
-    pHdr->mapAddr = (char*)mmap(NULL, pHdr->mapLength, PROT_READ | PROT_WRITE,
+    pHdr->mapAddr = (char*)dvm_mmap_low32(NULL, pHdr->mapLength, PROT_READ | PROT_WRITE,
         MAP_PRIVATE, fd, 0);
     if (pHdr->mapAddr == MAP_FAILED) {
         ALOGE("LinearAlloc mmap(%d) failed: %s", pHdr->mapLength,
@@ -153,7 +153,7 @@ LinearAllocHdr* dvmLinearAllocCreate(Object* classLoader)
 #else /*USE_ASHMEM*/
     // MAP_ANON is listed as "deprecated" on Linux,
     // but MAP_ANONYMOUS is not defined under Mac OS X.
-    pHdr->mapAddr = mmap(NULL, pHdr->mapLength, PROT_READ | PROT_WRITE,
+    pHdr->mapAddr = dvm_mmap_low32(NULL, pHdr->mapLength, PROT_READ | PROT_WRITE,
         MAP_PRIVATE | MAP_ANON, -1, 0);
     if (pHdr->mapAddr == MAP_FAILED) {
         ALOGE("LinearAlloc mmap(%d) failed: %s", pHdr->mapLength,
diff --git a/vm/Misc.cpp b/vm/Misc.cpp
index 057b90d..c439440 100644
--- a/vm/Misc.cpp
+++ b/vm/Misc.cpp
@@ -59,7 +59,7 @@ void dvmPrintHexDumpEx(int priority, const char* tag, const void* vaddr,
     if (mode == kHexDumpLocal)
         offset = 0;
     else
-        offset = (int) addr;
+        offset = (int)(uintptr_t) addr;
 
     memset(out, ' ', sizeof(out)-1);
     out[8] = ':';
@@ -618,7 +618,7 @@ void *dvmAllocRegion(size_t byteCount, int prot, const char *name) {
     if (fd == -1) {
         return NULL;
     }
-    base = mmap(NULL, byteCount, prot, MAP_PRIVATE, fd, 0);
+    base = dvm_mmap_low32(NULL, byteCount, prot, MAP_PRIVATE, fd, 0);
     ret = close(fd);
     if (base == MAP_FAILED) {
         return NULL;
diff --git a/vm/Native.cpp b/vm/Native.cpp
index a12c4e0..f2d24de 100644
--- a/vm/Native.cpp
+++ b/vm/Native.cpp
@@ -67,7 +67,7 @@ void dvmNativeShutdown()
  * structure, but the declaration needs to match the DalvikBridgeFunc
  * type definition.)
  */
-void dvmResolveNativeMethod(const u4* args, JValue* pResult,
+void dvmResolveNativeMethod(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self)
 {
     ClassObject* clazz = method->clazz;
@@ -111,8 +111,11 @@ void dvmResolveNativeMethod(const u4* args, JValue* pResult,
     void* func = lookupSharedLibMethod(method);
     if (func != NULL) {
         /* found it, point it at the JNI bridge and then call it */
+        ALOGW("Found native %s.%s at %p, setting up JNI bridge", clazz->descriptor, method->name, func);
         dvmUseJNIBridge((Method*) method, func);
+        ALOGW("Calling native method via bridge (nativeFunc=%p)", method->nativeFunc);
         (*method->nativeFunc)(args, pResult, method, self);
+        ALOGW("Native method returned");
         return;
     }
 
@@ -687,7 +690,7 @@ static char* createMangledSignature(const DexProto* proto)
  *
  * TODO: we may want to skip libraries for which JNI_OnLoad failed.
  */
-static int findMethodInLib(void* vlib, void* vmethod)
+static intptr_t findMethodInLib(void* vlib, void* vmethod)
 {
     const SharedLib* pLib = (const SharedLib*) vlib;
     const Method* meth = (const Method*) vmethod;
@@ -745,7 +748,7 @@ bail:
     free(mangleCM);
     free(mangleSig);
     free(mangleCMSig);
-    return (int) func;
+    return (intptr_t) func;
 }
 
 /*
@@ -759,6 +762,19 @@ static void* lookupSharedLibMethod(const Method* method)
         ALOGE("Unexpected init state: nativeLibs not ready");
         dvmAbort();
     }
-    return (void*) dvmHashForeach(gDvm.nativeLibs, findMethodInLib,
-        (void*) method);
+    /* Iterate the hash table manually to preserve 64-bit function pointers */
+    HashTable* pHashTable = gDvm.nativeLibs;
+    dvmHashTableLock(pHashTable);
+    for (int i = 0; i < pHashTable->tableSize; i++) {
+        HashEntry* pEntry = &pHashTable->pEntries[i];
+        if (pEntry->data != NULL && pEntry->data != HASH_TOMBSTONE) {
+            intptr_t result = findMethodInLib(pEntry->data, (void*) method);
+            if (result != 0) {
+                dvmHashTableUnlock(pHashTable);
+                return (void*) result;
+            }
+        }
+    }
+    dvmHashTableUnlock(pHashTable);
+    return NULL;
 }
diff --git a/vm/Native.h b/vm/Native.h
index 65ff8dc..a88820c 100644
--- a/vm/Native.h
+++ b/vm/Native.h
@@ -50,7 +50,7 @@ void dvmNativeShutdown(void);
  * Convert argc/argv into a function call.  This is platform-specific.
  */
 extern "C" void dvmPlatformInvoke(void* pEnv, ClassObject* clazz, int argInfo,
-    int argc, const u4* argv, const char* signature, void* func, JValue* pResult);
+    int argc, const dreg_t* argv, const char* signature, void* func, JValue* pResult);
 
 /*
  * Generate hints to speed native calls.  This is platform specific.
@@ -75,7 +75,7 @@ bool dvmLoadNativeCode(const char* fileName, Object* classLoader,
  *
  * Throws an exception and returns NULL on failure.
  */
-void dvmResolveNativeMethod(const u4* args, JValue* pResult,
+void dvmResolveNativeMethod(const dreg_t* args, JValue* pResult,
     const Method* method, struct Thread* self);
 
 /*
@@ -94,7 +94,7 @@ void dvmUnregisterJNINativeMethods(ClassObject* clazz);
  * Big/little endian shouldn't matter here -- ordering of words within a
  * long seems consistent across our supported platforms.
  */
-INLINE s8 dvmGetArgLong(const u4* args, int elem)
+INLINE s8 dvmGetArgLong(const dreg_t* args, int elem)
 {
     s8 val;
     memcpy(&val, &args[elem], sizeof(val));
diff --git a/vm/Profile.cpp b/vm/Profile.cpp
index 866311c..069341c 100644
--- a/vm/Profile.cpp
+++ b/vm/Profile.cpp
@@ -281,7 +281,7 @@ static void getSample(Thread* thread)
  */
 static void* runSamplingThread(void* arg)
 {
-    int intervalUs = (int) arg;
+    int intervalUs = (int)(uintptr_t) arg;
     while (gDvm.methodTrace.traceEnabled) {
         dvmSuspendAllThreads(SUSPEND_FOR_SAMPLING);
 
@@ -454,7 +454,7 @@ static int dumpMarkedMethods(void* vclazz, void* vfp)
         meth = &clazz->virtualMethods[i];
         if (meth->inProfile) {
             name = dvmDescriptorToName(meth->clazz->descriptor);
-            fprintf(fp, "0x%08x\t%s\t%s\t%s\t%s\t%d\n", (int) meth,
+            fprintf(fp, "0x%08x\t%s\t%s\t%s\t%s\t%d\n", (int)(uintptr_t) meth,
                 name, meth->name,
                 dexProtoGetMethodDescriptor(&meth->prototype, &stringCache),
                 dvmGetMethodSourceFile(meth), dvmLineNumFromPC(meth, 0));
@@ -467,7 +467,7 @@ static int dumpMarkedMethods(void* vclazz, void* vfp)
         meth = &clazz->directMethods[i];
         if (meth->inProfile) {
             name = dvmDescriptorToName(meth->clazz->descriptor);
-            fprintf(fp, "0x%08x\t%s\t%s\t%s\t%s\t%d\n", (int) meth,
+            fprintf(fp, "0x%08x\t%s\t%s\t%s\t%s\t%d\n", (int)(uintptr_t) meth,
                 name, meth->name,
                 dexProtoGetMethodDescriptor(&meth->prototype, &stringCache),
                 dvmGetMethodSourceFile(meth), dvmLineNumFromPC(meth, 0));
@@ -954,7 +954,7 @@ void dvmMethodTraceAdd(Thread* self, const Method* method, int action,
 
     //assert(METHOD_ACTION((u4) method) == 0);
 
-    methodVal = METHOD_COMBINE((u4) method, action);
+    methodVal = METHOD_COMBINE((u4)(uintptr_t) method, action);
 
     /*
      * Write data into "oldOffset".
diff --git a/vm/Sync.cpp b/vm/Sync.cpp
index f42004c..442c0fb 100644
--- a/vm/Sync.cpp
+++ b/vm/Sync.cpp
@@ -23,6 +23,19 @@
 #include <time.h>
 #include <errno.h>
 
+/*
+ * Lock word type: must be pointer-width on 64-bit to hold Monitor*.
+ */
+#if __SIZEOF_POINTER__ > 4
+typedef uintptr_t lw_t;
+#define LW_ATOMIC_STORE(val, addr)   android_atomic_release_store_ptr((intptr_t)(val), (volatile intptr_t*)(addr))
+#define LW_ATOMIC_CAS(old, new, addr) android_atomic_acquire_cas_ptr((intptr_t)(old), (intptr_t)(new), (volatile intptr_t*)(addr))
+#else
+typedef u4 lw_t;
+#define LW_ATOMIC_STORE(val, addr)   android_atomic_release_store((int32_t)(val), (volatile int32_t*)(addr))
+#define LW_ATOMIC_CAS(old, new, addr) android_atomic_acquire_cas((int32_t)(old), (int32_t)(new), (volatile int32_t*)(addr))
+#endif
+
 /*
  * Every Object has a monitor associated with it, but not every Object is
  * actually locked.  Even the ones that are locked do not need a
@@ -107,8 +120,7 @@ Monitor* dvmCreateMonitor(Object* obj)
     /* replace the head of the list with the new monitor */
     do {
         mon->next = gDvm.monitorList;
-    } while (android_atomic_release_cas((int32_t)mon->next, (int32_t)mon,
-            (int32_t*)(void*)&gDvm.monitorList) != 0);
+    } while (!__sync_bool_compare_and_swap(&gDvm.monitorList, mon->next, mon));
 
     return mon;
 }
@@ -143,10 +155,10 @@ Object* dvmGetMonitorObject(Monitor* mon)
 /*
  * Returns the thread id of the thread owning the given lock.
  */
-static u4 lockOwner(Object* obj)
+static lw_t lockOwner(Object* obj)
 {
     Thread *owner;
-    u4 lock;
+    lw_t lock;
 
     assert(obj != NULL);
     /*
@@ -170,7 +182,7 @@ static u4 lockOwner(Object* obj)
  */
 Thread* dvmGetObjectLockHolder(Object* obj)
 {
-    u4 threadId = lockOwner(obj);
+    lw_t threadId = lockOwner(obj);
 
     if (threadId == 0)
         return NULL;
@@ -829,7 +841,7 @@ static void notifyAllMonitor(Thread* self, Monitor* mon)
 static void inflateMonitor(Thread *self, Object *obj)
 {
     Monitor *mon;
-    u4 thin;
+    lw_t thin;
 
     assert(self != NULL);
     assert(obj != NULL);
@@ -842,9 +854,9 @@ static void inflateMonitor(Thread *self, Object *obj)
     thin = obj->lock;
     mon->lockCount = LW_LOCK_COUNT(thin);
     thin &= LW_HASH_STATE_MASK << LW_HASH_STATE_SHIFT;
-    thin |= (u4)mon | LW_SHAPE_FAT;
+    thin |= (lw_t)(uintptr_t)mon | LW_SHAPE_FAT;
     /* Publish the updated lock word. */
-    android_atomic_release_store(thin, (int32_t *)&obj->lock);
+    LW_ATOMIC_STORE(thin, &obj->lock);
 }
 
 /*
@@ -855,13 +867,13 @@ static void inflateMonitor(Thread *self, Object *obj)
  */
 void dvmLockObject(Thread* self, Object *obj)
 {
-    volatile u4 *thinp;
+    volatile lw_t *thinp;
     ThreadStatus oldStatus;
     struct timespec tm;
     long sleepDelayNs;
     long minSleepDelayNs = 1000000;  /* 1 millisecond */
     long maxSleepDelayNs = 1000000000;  /* 1 second */
-    u4 thin, newThin, threadId;
+    lw_t thin, newThin, threadId;
 
     assert(self != NULL);
     assert(obj != NULL);
@@ -896,8 +908,7 @@ retry:
              * will have tried this before calling out to the VM.
              */
             newThin = thin | (threadId << LW_LOCK_OWNER_SHIFT);
-            if (android_atomic_acquire_cas(thin, newThin,
-                    (int32_t*)thinp) != 0) {
+            if (LW_ATOMIC_CAS(thin, newThin, thinp) != 0) {
                 /*
                  * The acquire failed.  Try again.
                  */
@@ -929,8 +940,7 @@ retry:
                          * owner field.
                          */
                         newThin = thin | (threadId << LW_LOCK_OWNER_SHIFT);
-                        if (android_atomic_acquire_cas(thin, newThin,
-                                (int32_t *)thinp) == 0) {
+                        if (LW_ATOMIC_CAS(thin, newThin, thinp) == 0) {
                             /*
                              * The acquire succeed.  Break out of the
                              * loop and proceed to inflate the lock.
@@ -1001,7 +1011,7 @@ retry:
  */
 bool dvmUnlockObject(Thread* self, Object *obj)
 {
-    u4 thin;
+    lw_t thin;
 
     assert(self != NULL);
     assert(self->status == THREAD_RUNNING);
@@ -1010,7 +1020,7 @@ bool dvmUnlockObject(Thread* self, Object *obj)
      * Cache the lock word as its value can change while we are
      * examining its state.
      */
-    thin = *(volatile u4 *)&obj->lock;
+    thin = obj->lock;
     if (LW_SHAPE(thin) == LW_SHAPE_THIN) {
         /*
          * The lock is thin.  We must ensure that the lock is owned
@@ -1028,7 +1038,7 @@ bool dvmUnlockObject(Thread* self, Object *obj)
                  * hash state.
                  */
                 thin &= (LW_HASH_STATE_MASK << LW_HASH_STATE_SHIFT);
-                android_atomic_release_store(thin, (int32_t*)&obj->lock);
+                LW_ATOMIC_STORE(thin, &obj->lock);
             } else {
                 /*
                  * The object was recursively acquired.  Decrement the
@@ -1067,7 +1077,7 @@ void dvmObjectWait(Thread* self, Object *obj, s8 msec, s4 nsec,
     bool interruptShouldThrow)
 {
     Monitor* mon;
-    u4 thin = *(volatile u4 *)&obj->lock;
+    lw_t thin = obj->lock;
 
     /* If the lock is still thin, we need to fatten it.
      */
@@ -1097,7 +1107,7 @@ void dvmObjectWait(Thread* self, Object *obj, s8 msec, s4 nsec,
  */
 void dvmObjectNotify(Thread* self, Object *obj)
 {
-    u4 thin = *(volatile u4 *)&obj->lock;
+    lw_t thin = obj->lock;
 
     /* If the lock is still thin, there aren't any waiters;
      * waiting on an object forces lock fattening.
@@ -1125,7 +1135,7 @@ void dvmObjectNotify(Thread* self, Object *obj)
  */
 void dvmObjectNotifyAll(Thread* self, Object *obj)
 {
-    u4 thin = *(volatile u4 *)&obj->lock;
+    lw_t thin = obj->lock;
 
     /* If the lock is still thin, there aren't any waiters;
      * waiting on an object forces lock fattening.
@@ -1218,7 +1228,7 @@ void dvmThreadInterrupt(Thread* thread)
 #ifndef WITH_COPYING_GC
 u4 dvmIdentityHashCode(Object *obj)
 {
-    return (u4)obj;
+    return (u4)(uintptr_t)obj;
 }
 #else
 /*
@@ -1227,9 +1237,9 @@ u4 dvmIdentityHashCode(Object *obj)
 u4 dvmIdentityHashCode(Object *obj)
 {
     Thread *self, *thread;
-    volatile u4 *lw;
+    volatile lw_t *lw;
     size_t size;
-    u4 lock, owner, hashState;
+    lw_t lock, owner, hashState;
 
     if (obj == NULL) {
         /*
@@ -1246,7 +1256,7 @@ retry:
          * relocated by the garbage collector.  Use the raw object
          * address.
          */
-        return (u4)obj >> 3;
+        return (u4)((uintptr_t)obj >> 3);
     } else if (hashState == LW_HASH_STATE_HASHED_AND_MOVED) {
         /*
          * The object has been hashed and its hash code has been
@@ -1273,7 +1283,7 @@ retry:
              * directly.
              */
             *lw |= (LW_HASH_STATE_HASHED << LW_HASH_STATE_SHIFT);
-            return (u4)obj >> 3;
+            return (u4)((uintptr_t)obj >> 3);
         }
         /*
          * We do not own the lock.  Try acquiring the lock.  Should
@@ -1285,15 +1295,12 @@ retry:
              * an acquire, update, and release with a single CAS.
              */
             lock = (LW_HASH_STATE_HASHED << LW_HASH_STATE_SHIFT);
-            if (android_atomic_acquire_cas(
-                                0,
-                                (int32_t)lock,
-                                (int32_t *)lw) == 0) {
+            if (LW_ATOMIC_CAS(0, lock, lw) == 0) {
                 /*
                  * A new lockword has been installed with a hash state
                  * of hashed.  Use the raw object address.
                  */
-                return (u4)obj >> 3;
+                return (u4)((uintptr_t)obj >> 3);
             }
         } else {
             if (tryLockMonitor(self, LW_MONITOR(*lw))) {
@@ -1304,7 +1311,7 @@ retry:
                  */
                 *lw |= (LW_HASH_STATE_HASHED << LW_HASH_STATE_SHIFT);
                 unlockMonitor(self, LW_MONITOR(*lw));
-                return (u4)obj >> 3;
+                return (u4)((uintptr_t)obj >> 3);
             }
         }
         /*
@@ -1357,7 +1364,7 @@ retry:
             *lw |= (LW_HASH_STATE_HASHED << LW_HASH_STATE_SHIFT);
             dvmResumeThread(thread);
             dvmUnlockThreadList();
-            return (u4)obj >> 3;
+            return (u4)((uintptr_t)obj >> 3);
         }
         /*
          * The wrong thread has been suspended.  Try again.
diff --git a/vm/Thread.cpp b/vm/Thread.cpp
index 1ebfca7..eea3970 100644
--- a/vm/Thread.cpp
+++ b/vm/Thread.cpp
@@ -702,12 +702,20 @@ bool dvmPrepMainThread()
         ALOGE("'Class' class failed to initialize");
         return false;
     }
-    if (!dvmInitClass(gDvm.classJavaLangThreadGroup) ||
-        !dvmInitClass(gDvm.classJavaLangThread) ||
-        !dvmInitClass(gDvm.classJavaLangVMThread))
-    {
-        ALOGE("thread classes failed to initialize");
-        return false;
+    if (!dvmInitClass(gDvm.classJavaLangThreadGroup)) {
+        ALOGW("ThreadGroup init failed - clearing and forcing INITIALIZED");
+        dvmClearException(dvmThreadSelf());
+        gDvm.classJavaLangThreadGroup->status = CLASS_INITIALIZED;
+    }
+    if (!dvmInitClass(gDvm.classJavaLangThread)) {
+        ALOGW("Thread init failed - clearing and forcing INITIALIZED");
+        dvmClearException(dvmThreadSelf());
+        gDvm.classJavaLangThread->status = CLASS_INITIALIZED;
+    }
+    if (!dvmInitClass(gDvm.classJavaLangVMThread)) {
+        ALOGW("VMThread init failed - clearing and forcing INITIALIZED");
+        dvmClearException(dvmThreadSelf());
+        gDvm.classJavaLangVMThread->status = CLASS_INITIALIZED;
     }
 
     groupObj = dvmGetMainThreadGroup();
@@ -732,13 +740,20 @@ bool dvmPrepMainThread()
 
     init = dvmFindDirectMethodByDescriptor(gDvm.classJavaLangThread, "<init>",
             "(Ljava/lang/ThreadGroup;Ljava/lang/String;IZ)V");
-    assert(init != NULL);
-    dvmCallMethod(thread, init, threadObj, &unused, groupObj, threadNameStr,
-        THREAD_NORM_PRIORITY, false);
-    if (dvmCheckException(thread)) {
-        ALOGE("exception thrown while constructing main thread object");
-        return false;
+    // Set Thread fields directly instead of calling <init>
+    {
+        InstField* f;
+        f = dvmFindInstanceFieldHier(gDvm.classJavaLangThread, "group", "Ljava/lang/ThreadGroup;");
+        if (f) dvmSetFieldObject(threadObj, f->byteOffset, groupObj);
+        f = dvmFindInstanceFieldHier(gDvm.classJavaLangThread, "name", "Ljava/lang/String;");
+        if (f) dvmSetFieldObject(threadObj, f->byteOffset, (Object*)threadNameStr);
+        f = dvmFindInstanceFieldHier(gDvm.classJavaLangThread, "priority", "I");
+        if (f) dvmSetFieldInt(threadObj, f->byteOffset, THREAD_NORM_PRIORITY);
+        f = dvmFindInstanceFieldHier(gDvm.classJavaLangThread, "daemon", "Z");
+        if (f) dvmSetFieldBoolean(threadObj, f->byteOffset, false);
+        ALOGW("Main Thread fields set directly");
     }
+    if (dvmCheckException(thread)) dvmClearException(thread);
 
     /*
      * Allocate and construct a VMThread.
@@ -748,19 +763,24 @@ bool dvmPrepMainThread()
         ALOGE("unable to allocate main vmthread object");
         return false;
     }
-    dvmReleaseTrackedAlloc(vmThreadObj, NULL);
+    // NOTE: do NOT release vmThreadObj until after all field sets
 
     init = dvmFindDirectMethodByDescriptor(gDvm.classJavaLangVMThread, "<init>",
             "(Ljava/lang/Thread;)V");
-    dvmCallMethod(thread, init, vmThreadObj, &unused, threadObj);
-    if (dvmCheckException(thread)) {
-        ALOGE("exception thrown while constructing main vmthread object");
-        return false;
+    // Skip VMThread.<init> — set fields directly
+    {
+        InstField* vf = dvmFindInstanceFieldHier(gDvm.classJavaLangVMThread, "thread", "Ljava/lang/Thread;");
+        if (vf) dvmSetFieldObject(vmThreadObj, vf->byteOffset, threadObj);
+        ALOGW("VMThread fields set directly");
     }
+    if (dvmCheckException(thread)) dvmClearException(thread);
 
     /* set the VMThread.vmData field to our Thread struct */
-    assert(gDvm.offJavaLangVMThread_vmData != 0);
-    dvmSetFieldInt(vmThreadObj, gDvm.offJavaLangVMThread_vmData, (u4)thread);
+    if (gDvm.offJavaLangVMThread_vmData != 0) {
+        // Use long for 64-bit pointer safety
+        dvmSetFieldLong(vmThreadObj, gDvm.offJavaLangVMThread_vmData, (s8)(uintptr_t)thread);
+    }
+    dvmReleaseTrackedAlloc(vmThreadObj, NULL);
 
     /*
      * Stuff the VMThread back into the Thread.  From this point on, other
@@ -780,7 +800,18 @@ bool dvmPrepMainThread()
      * Thread.currentThread(), so we want the Thread to be fully configured
      * before we do this.
      */
-    Object* systemLoader = dvmGetSystemClassLoader();
+    // System class loader enabled
+    Object* systemLoader = NULL;
+    {
+        // Try to get system class loader, but don't abort on failure
+        Thread* self2 = dvmThreadSelf();
+        systemLoader = dvmGetSystemClassLoader(); if (dvmCheckException(dvmThreadSelf())) { ALOGW("System class loader init failed - using NULL"); dvmClearException(dvmThreadSelf()); systemLoader = NULL; }
+        if (dvmCheckException(self2)) {
+            ALOGW("System class loader failed - clearing exception");
+            dvmClearException(self2);
+            systemLoader = NULL;
+        }
+    }
     if (systemLoader == NULL) {
         ALOGW("WARNING: system class loader is NULL (setting main ctxt)");
         /* keep going? */
@@ -790,6 +821,40 @@ bool dvmPrepMainThread()
         dvmReleaseTrackedAlloc(systemLoader, NULL);
     }
 
+    /* Initialize System.out/err with DirectPrintStream (native I/O) */
+    {
+        ClassObject* dpsCls = dvmFindSystemClassNoInit("Ljava/io/DirectPrintStream;");
+        if (dpsCls) {
+            // Create DirectPrintStream(1) for stdout
+            Object* psOut = dvmAllocObject(dpsCls, ALLOC_DEFAULT);
+            if (psOut) {
+                InstField* fdField = dvmFindInstanceFieldHier(dpsCls, "fd", "I");
+                if (fdField) dvmSetFieldInt(psOut, fdField->byteOffset, 1);
+                
+                StaticField* sysOut = dvmFindStaticField(gDvm.classJavaLangSystem, "out", "Ljava/io/PrintStream;");
+                if (sysOut) { dvmSetStaticFieldObject(sysOut, psOut); ALOGW("System.out = DirectPrintStream(1)"); }
+                dvmReleaseTrackedAlloc(psOut, NULL);
+            }
+            // Create DirectPrintStream(2) for stderr
+            Object* psErr = dvmAllocObject(dpsCls, ALLOC_DEFAULT);
+            if (psErr) {
+                InstField* fdField = dvmFindInstanceFieldHier(dpsCls, "fd", "I");
+                if (fdField) dvmSetFieldInt(psErr, fdField->byteOffset, 2);
+                
+                StaticField* sysErr = dvmFindStaticField(gDvm.classJavaLangSystem, "err", "Ljava/io/PrintStream;");
+                if (sysErr) { dvmSetStaticFieldObject(sysErr, psErr); ALOGW("System.err = DirectPrintStream(2)"); }
+                dvmReleaseTrackedAlloc(psErr, NULL);
+            }
+        } else {
+            ALOGW("DirectPrintStream not found in boot classpath");
+        }
+        // Force core classes to INITIALIZED to break circular init dependencies
+        gDvm.classJavaLangSystem->status = CLASS_INITIALIZED;
+
+        ALOGW("Forced System to INITIALIZED");
+        if (dvmCheckException(dvmThreadSelf())) dvmClearException(dvmThreadSelf());
+    }
+
     /* include self in non-daemon threads (mainly for AttachCurrentThread) */
     gDvm.nonDaemonThreadCount++;
 
@@ -1159,8 +1224,8 @@ static bool createFakeEntryFrame(Thread* thread)
      * Null out the "String[] args" argument.
      */
     assert(gDvm.methDalvikSystemNativeStart_main->registersSize == 1);
-    u4* framePtr = (u4*) thread->interpSave.curFrame;
-    framePtr[0] = 0;
+    void* framePtr = (u4*) thread->interpSave.curFrame;
+    ((dreg_t*)framePtr)[0] = 0;
 
     return true;
 }
@@ -2016,7 +2081,7 @@ void dvmDetachCurrentThread()
 
         if (curDepth == 1) {
             /* not expecting a lingering break frame; just look at curFrame */
-            assert(!dvmIsBreakFrame((u4*)self->interpSave.curFrame));
+            assert(!dvmIsBreakFrame((void*)self->interpSave.curFrame));
             StackSaveArea* ssa = SAVEAREA_FROM_FP(self->interpSave.curFrame);
             if (dvmIsNativeMethod(ssa->method))
                 topIsNative = true;
@@ -3008,9 +3073,40 @@ static Object* getStaticThreadGroup(const char* fieldName)
     }
     groupObj = dvmGetStaticFieldObject(groupField);
     if (groupObj == NULL) {
-        ALOGE("java.lang.ThreadGroup.%s not initialized", fieldName);
-        dvmThrowInternalError(NULL);
-        return NULL;
+        ALOGW("ThreadGroup.%s null - creating stub", fieldName);
+        groupObj = dvmAllocObject(gDvm.classJavaLangThreadGroup, ALLOC_DEFAULT);
+        if (groupObj == NULL) { dvmThrowInternalError(NULL); return NULL; }
+        dvmSetStaticFieldObject(groupField, groupObj);
+        // Init essential fields
+        InstField* nf = dvmFindInstanceField(gDvm.classJavaLangThreadGroup, "name", "Ljava/lang/String;");
+        if (nf) { StringObject* s = dvmCreateStringFromCstr(fieldName);
+                   dvmSetFieldObject(groupObj, nf->byteOffset, (Object*)s);
+                   dvmReleaseTrackedAlloc((Object*)s, NULL); }
+        InstField* mp = dvmFindInstanceField(gDvm.classJavaLangThreadGroup, "maxPriority", "I");
+        if (mp) dvmSetFieldInt(groupObj, mp->byteOffset, 10);
+        // Init threadRefs (Harmony ArrayList) — needed for Thread.<init>/addThread
+        InstField* tr = dvmFindInstanceFieldHier(gDvm.classJavaLangThreadGroup, "threadRefs", "Ljava/util/List;");
+        if (tr) {
+            ClassObject* alCls = dvmFindSystemClassNoInit("Ljava/util/ArrayList;");
+            if (alCls) {
+                Object* list = dvmAllocObject(alCls, ALLOC_DEFAULT);
+                if (list) {
+                    dvmSetFieldObject(groupObj, tr->byteOffset, list);
+                    // Init ArrayList.array field at offset 8 (Harmony internal)
+                    ClassObject* objCls = dvmFindSystemClassNoInit("Ljava/lang/Object;");
+                    if (objCls) {
+                        ArrayObject* arr = dvmAllocArrayByClass(
+                            dvmFindArrayClassForElement(objCls), 12, ALLOC_DEFAULT);
+                        if (arr) {
+                            dvmSetFieldObject(list, 8, (Object*)arr);
+                            dvmReleaseTrackedAlloc((Object*)arr, NULL);
+                        }
+                    }
+                    dvmReleaseTrackedAlloc(list, NULL);
+                }
+            }
+        }
+        dvmReleaseTrackedAlloc(groupObj, NULL);
     }
 
     return groupObj;
@@ -3297,7 +3393,7 @@ static bool shouldShowNativeStack(Thread* thread) {
     // state THREAD_SUSPENDED if they're calling back into the VM, or THREAD_MONITOR
     // if they're blocked on a monitor, or one of the thread-startup states if
     // it's early enough in their life cycle (http://b/7432159).
-    u4* fp = thread->interpSave.curFrame;
+    void* fp = thread->interpSave.curFrame;
     if (fp == NULL) {
         // The thread has no managed frames, so native frames are all there is.
         return true;
diff --git a/vm/Thread.h b/vm/Thread.h
index 19bd49c..820fa4e 100644
--- a/vm/Thread.h
+++ b/vm/Thread.h
@@ -76,7 +76,11 @@ void dvmSlayDaemons(void);
 #define kInternalRefMax         4096    /* mainly a sanity check */
 
 #define kMinStackSize       (512 + STACK_OVERFLOW_RESERVE)
+#if __SIZEOF_POINTER__ > 4
+#define kDefaultStackSize   (64*1024)   /* 64K for 64-bit (registers are 8 bytes) */
+#else
 #define kDefaultStackSize   (16*1024)   /* four 4K pages */
+#endif
 #define kMaxStackSize       (256*1024 + STACK_OVERFLOW_RESERVE)
 
 /*
diff --git a/vm/UtfString.h b/vm/UtfString.h
index 352948c..0cbdf3d 100644
--- a/vm/UtfString.h
+++ b/vm/UtfString.h
@@ -35,7 +35,7 @@
  *
  * The values are checked for correctness during startup.
  */
-//#define USE_GLOBAL_STRING_DEFS
+#define USE_GLOBAL_STRING_DEFS
 #ifdef USE_GLOBAL_STRING_DEFS
 # define STRING_FIELDOFF_VALUE      gDvm.offJavaLangString_value
 # define STRING_FIELDOFF_OFFSET     gDvm.offJavaLangString_offset
diff --git a/vm/alloc/Copying.cpp b/vm/alloc/Copying.cpp
index 77cdac3..0c453e6 100644
--- a/vm/alloc/Copying.cpp
+++ b/vm/alloc/Copying.cpp
@@ -260,7 +260,7 @@ static void *virtualAlloc(size_t length)
 {
     int flags = MAP_PRIVATE | MAP_ANONYMOUS;
     int prot = PROT_READ | PROT_WRITE;
-    void *addr = mmap(NULL, length, prot, flags, -1, 0);
+    void *addr = dvm_mmap_low32(NULL, length, prot, flags, -1, 0);
     if (addr == MAP_FAILED) {
         LOGE_HEAP("mmap: %s", strerror(errno));
         addr = NULL;
@@ -830,7 +830,7 @@ static size_t sumHeapBitmap(const HeapBitmap *bitmap)
 {
     size_t sum = 0;
     for (size_t i = 0; i < bitmap->bitsLen >> 2; ++i) {
-        sum += CLZ(bitmap->bits[i]);
+        sum += CLZL(bitmap->bits[i]);
     }
     return sum;
 }
@@ -1311,7 +1311,7 @@ static Object *transportObject(const Object *fromObj)
          * The object has had its hash code exposed.  Append it to the
          * instance and set a bit so we know to look for it there.
          */
-        *(u4 *)(((char *)toObj) + copySize) = (u4)fromObj >> 3;
+        *(u4 *)(((char *)toObj) + copySize) = (u4)((uintptr_t)fromObj >> 3);
         toObj->lock |= LW_HASH_STATE_HASHED_AND_MOVED << LW_HASH_STATE_SHIFT;
     }
     LOG_TRAN("transportObject: from %p/%zu to %p/%zu (%zu,%zu) %s",
diff --git a/vm/alloc/HeapBitmap.cpp b/vm/alloc/HeapBitmap.cpp
index d6a737b..9b7a826 100644
--- a/vm/alloc/HeapBitmap.cpp
+++ b/vm/alloc/HeapBitmap.cpp
@@ -104,10 +104,10 @@ void dvmHeapBitmapWalk(const HeapBitmap *bitmap, BitmapCallback *callback,
     for (uintptr_t i = 0; i <= end; ++i) {
         unsigned long word = bitmap->bits[i];
         if (UNLIKELY(word != 0)) {
-            unsigned long highBit = 1 << (HB_BITS_PER_WORD - 1);
+            unsigned long highBit = 1UL << (HB_BITS_PER_WORD - 1);
             uintptr_t ptrBase = HB_INDEX_TO_OFFSET(i) + bitmap->base;
             while (word != 0) {
-                const int shift = CLZ(word);
+                const int shift = CLZL(word);
                 Object* obj = (Object *)(ptrBase + shift * HB_OBJECT_ALIGNMENT);
                 (*callback)(obj, arg);
                 word &= ~(highBit >> shift);
@@ -139,11 +139,11 @@ void dvmHeapBitmapScanWalk(HeapBitmap *bitmap,
     for (i = 0; i <= end; ++i) {
         unsigned long word = bitmap->bits[i];
         if (UNLIKELY(word != 0)) {
-            unsigned long highBit = 1 << (HB_BITS_PER_WORD - 1);
+            unsigned long highBit = 1UL << (HB_BITS_PER_WORD - 1);
             uintptr_t ptrBase = HB_INDEX_TO_OFFSET(i) + bitmap->base;
             void *finger = (void *)(HB_INDEX_TO_OFFSET(i + 1) + bitmap->base);
             while (word != 0) {
-                const int shift = CLZ(word);
+                const int shift = CLZL(word);
                 Object *obj = (Object *)(ptrBase + shift * HB_OBJECT_ALIGNMENT);
                 (*callback)(obj, finger, arg);
                 word &= ~(highBit >> shift);
@@ -188,10 +188,10 @@ void dvmHeapBitmapSweepWalk(const HeapBitmap *liveHb, const HeapBitmap *markHb,
     for (size_t i = start; i <= end; i++) {
         unsigned long garbage = live[i] & ~mark[i];
         if (UNLIKELY(garbage != 0)) {
-            unsigned long highBit = 1 << (HB_BITS_PER_WORD - 1);
+            unsigned long highBit = 1UL << (HB_BITS_PER_WORD - 1);
             uintptr_t ptrBase = HB_INDEX_TO_OFFSET(i) + liveHb->base;
             while (garbage != 0) {
-                int shift = CLZ(garbage);
+                int shift = CLZL(garbage);
                 garbage &= ~(highBit >> shift);
                 *pb++ = (void *)(ptrBase + shift * HB_OBJECT_ALIGNMENT);
             }
diff --git a/vm/alloc/HeapBitmap.h b/vm/alloc/HeapBitmap.h
index 9f2a0a1..fe591ef 100644
--- a/vm/alloc/HeapBitmap.h
+++ b/vm/alloc/HeapBitmap.h
@@ -38,8 +38,8 @@
  * when using CLZ.
  */
 #define HB_OFFSET_TO_MASK(offset_) \
-    (1 << \
-        (31-(((uintptr_t)(offset_) / HB_OBJECT_ALIGNMENT) % HB_BITS_PER_WORD)))
+    (1UL << \
+        ((HB_BITS_PER_WORD - 1)-(((uintptr_t)(offset_) / HB_OBJECT_ALIGNMENT) % HB_BITS_PER_WORD)))
 
 struct HeapBitmap {
     /* The bitmap data, which points to an mmap()ed area of zeroed
diff --git a/vm/alloc/HeapSource.cpp b/vm/alloc/HeapSource.cpp
index 82c2b9e..7f6bf60 100644
--- a/vm/alloc/HeapSource.cpp
+++ b/vm/alloc/HeapSource.cpp
@@ -402,7 +402,7 @@ static bool remapNewHeap(HeapSource* hs, Heap* newHeap)
     ALOGE("Unable to create an ashmem region for the new heap");
     return false;
   }
-  void* addr = mmap(newHeapBase, rem_size, PROT_READ | PROT_WRITE, MAP_PRIVATE, fd, 0);
+  void* addr = dvm_mmap_low32(newHeapBase, rem_size, PROT_READ | PROT_WRITE, MAP_PRIVATE, fd, 0);
   int ret = close(fd);
   if (addr == MAP_FAILED) {
     ALOGE("Unable to map an ashmem region for the new heap");
diff --git a/vm/alloc/MarkSweep.cpp b/vm/alloc/MarkSweep.cpp
index 2781a7c..2c0e540 100644
--- a/vm/alloc/MarkSweep.cpp
+++ b/vm/alloc/MarkSweep.cpp
@@ -454,7 +454,9 @@ static void scanDataObject(const Object *obj, GcMarkContext *ctx)
 static void scanObject(const Object *obj, GcMarkContext *ctx)
 {
     assert(obj != NULL);
-    assert(obj->clazz != NULL);
+    if (obj->clazz == NULL) {
+        return;  /* uninitialized heap object — skip during GC */
+    }
     if (obj->clazz == gDvm.classJavaLangClass) {
         scanClassObject(obj, ctx);
     } else if (IS_CLASS_FLAG_SET(obj->clazz, CLASS_ISARRAY)) {
diff --git a/vm/alloc/Visit.cpp b/vm/alloc/Visit.cpp
index 410b66e..cf5d64d 100644
--- a/vm/alloc/Visit.cpp
+++ b/vm/alloc/Visit.cpp
@@ -107,8 +107,8 @@ static void visitThreadStack(RootVisitor *visitor, Thread *thread, void *arg)
                  * scan.
                  */
                 for (size_t i = 0; i < method->registersSize; ++i) {
-                    if (dvmIsValidObject((Object *)fp[i])) {
-                        (*visitor)(&fp[i], threadId, ROOT_JAVA_FRAME, arg);
+                    if (dvmIsValidObject((Object *)(uintptr_t)((dreg_t*)fp)[i])) {
+                        (*visitor)(&((dreg_t*)fp)[i], threadId, ROOT_JAVA_FRAME, arg);
                     }
                 }
             } else {
@@ -132,17 +132,17 @@ static void visitThreadStack(RootVisitor *visitor, Thread *thread, void *arg)
                          * Register is marked as live, it's a valid root.
                          */
 #if WITH_EXTRA_GC_CHECKS
-                        if (fp[i] != 0 && !dvmIsValidObject((Object *)fp[i])) {
+                        if (((dreg_t*)fp)[i] != 0 && !dvmIsValidObject((Object *)(uintptr_t)((dreg_t*)fp)[i])) {
                             /* this is very bad */
                             ALOGE("PGC: invalid ref in reg %d: %#x",
-                                 method->registersSize - 1 - i, fp[i]);
+                                 method->registersSize - 1 - i, (u4)((dreg_t*)fp)[i]);
                             ALOGE("PGC: %s.%s addr %#x",
                                  method->clazz->descriptor, method->name,
                                  saveArea->xtra.currentPc - method->insns);
                             continue;
                         }
 #endif
-                        (*visitor)(&fp[i], threadId, ROOT_JAVA_FRAME, arg);
+                        (*visitor)(&((dreg_t*)fp)[i], threadId, ROOT_JAVA_FRAME, arg);
                     }
                 }
                 dvmReleaseRegisterMapLine(pMap, regVector);
diff --git a/vm/analysis/CodeVerify.cpp b/vm/analysis/CodeVerify.cpp
index 56a9fbd..34f19f6 100644
--- a/vm/analysis/CodeVerify.cpp
+++ b/vm/analysis/CodeVerify.cpp
@@ -574,12 +574,12 @@ static ClassObject* regTypeReferenceToClass(RegType type,
 
 /* convert the ClassObject pointer to an (initialized) register type */
 static inline RegType regTypeFromClass(ClassObject* clazz) {
-    return (u4) clazz;
+    return (RegType)(uintptr_t) clazz;
 }
 
 /* return the RegType for the uninitialized reference in slot "uidx" */
 static RegType regTypeFromUninitIndex(int uidx) {
-    return (u4) (kRegTypeUninit | (uidx << kRegTypeUninitShift));
+    return (RegType)(kRegTypeUninit | ((uintptr_t)uidx << kRegTypeUninitShift));
 }
 
 
diff --git a/vm/analysis/CodeVerify.h b/vm/analysis/CodeVerify.h
index 5857895..0cfd0c9 100644
--- a/vm/analysis/CodeVerify.h
+++ b/vm/analysis/CodeVerify.h
@@ -112,7 +112,11 @@ enum {
  * pointer to the ClassObject, and for uninitialized references it holds
  * an index into the UninitInstanceMap.
  */
+#if __SIZEOF_POINTER__ > 4
+typedef uintptr_t RegType;   /* must hold ClassObject* on 64-bit */
+#else
 typedef u4 RegType;
+#endif
 
 /*
  * A bit vector indicating which entries in the monitor stack are
diff --git a/vm/analysis/DexPrepare.cpp b/vm/analysis/DexPrepare.cpp
index e8112d5..0e4d126 100644
--- a/vm/analysis/DexPrepare.cpp
+++ b/vm/analysis/DexPrepare.cpp
@@ -563,7 +563,7 @@ bool dvmContinueOptimization(int fd, off_t dexOffset, long dexLength,
          */
         bool success;
         void* mapAddr;
-        mapAddr = mmap(NULL, dexOffset + dexLength, PROT_READ|PROT_WRITE,
+        mapAddr = dvm_mmap_low32(NULL, dexOffset + dexLength, PROT_READ|PROT_WRITE,
                     MAP_SHARED, fd, 0);
         if (mapAddr == MAP_FAILED) {
             ALOGE("unable to mmap DEX cache: %s", strerror(errno));
diff --git a/vm/analysis/DexVerify.cpp b/vm/analysis/DexVerify.cpp
index c47c301..f9f877f 100644
--- a/vm/analysis/DexVerify.cpp
+++ b/vm/analysis/DexVerify.cpp
@@ -368,7 +368,7 @@ static bool checkArrayData(const Method* meth, u4 curOffset)
     arrayData = insns + offsetToArrayData;
 
     /* make sure the table is 32-bit aligned */
-    if ((((u4) arrayData) & 0x03) != 0) {
+    if ((((u4)(uintptr_t) arrayData) & 0x03) != 0) {
         LOG_VFY("VFY: unaligned array data table: at %d, data offset %d",
             curOffset, offsetToArrayData);
         return false;
@@ -628,7 +628,7 @@ static bool checkSwitchTargets(const Method* meth, InsnFlags* insnFlags,
     switchInsns = insns + offsetToSwitch;
 
     /* make sure the table is 32-bit aligned */
-    if ((((u4) switchInsns) & 0x03) != 0) {
+    if ((((u4)(uintptr_t) switchInsns) & 0x03) != 0) {
         LOG_VFY("VFY: unaligned switch table: at %d, switch offset %d",
             curOffset, offsetToSwitch);
         return false;
diff --git a/vm/analysis/Optimize.cpp b/vm/analysis/Optimize.cpp
index b61b82c..7c35eda 100644
--- a/vm/analysis/Optimize.cpp
+++ b/vm/analysis/Optimize.cpp
@@ -83,8 +83,9 @@ bool dvmCreateInlineSubsTable()
             ALOGE("Unable to find method for inlining: %s.%s:%s",
                 ops[i].classDescriptor, ops[i].methodName,
                 ops[i].methodSignature);
-            free(table);
-            return false;
+            // Skip missing inline method instead of aborting
+            continue; // free(table);
+            // return false;
         }
 
         table[tableIndex].method = method;
@@ -303,18 +304,18 @@ rewrite_static_field:
             switch (opc) {
             case OP_INVOKE_VIRTUAL:
                 if (!rewriteExecuteInline(method, insns, METHOD_VIRTUAL)) {
-                    rewriteVirtualInvoke(method, insns,
-                        OP_INVOKE_VIRTUAL_QUICK);
+                    /* DISABLED: vtable quickening on 64-bit */
+                    // rewriteVirtualInvoke(method, insns, OP_INVOKE_VIRTUAL_QUICK);
                 }
                 break;
             case OP_INVOKE_VIRTUAL_RANGE:
                 if (!rewriteExecuteInlineRange(method, insns, METHOD_VIRTUAL)) {
-                    rewriteVirtualInvoke(method, insns,
-                        OP_INVOKE_VIRTUAL_QUICK_RANGE);
+                    /* DISABLED: vtable quickening on 64-bit */
+                    // rewriteVirtualInvoke(method, insns, OP_INVOKE_VIRTUAL_QUICK_RANGE);
                 }
                 break;
             case OP_INVOKE_SUPER:
-                rewriteVirtualInvoke(method, insns, OP_INVOKE_SUPER_QUICK);
+                // rewriteVirtualInvoke(method, insns, OP_INVOKE_SUPER_QUICK); /* DISABLED */
                 break;
             case OP_INVOKE_SUPER_RANGE:
                 rewriteVirtualInvoke(method, insns, OP_INVOKE_SUPER_QUICK_RANGE);
@@ -695,7 +696,7 @@ static void rewriteInstField(Method* method, u2* insns, Opcode quickOpc,
         updateOpcode(method, insns, volatileOpc);
         ALOGV("DexOpt: rewrote ifield access %s.%s --> volatile",
             instField->clazz->descriptor, instField->name);
-    } else if (quickOpc != OP_NOP && instField->byteOffset < 65536) {
+    } else if (false && quickOpc != OP_NOP && instField->byteOffset < 65536) { /* DISABLED: 64-bit offset mismatch */
         updateOpcode(method, insns, quickOpc);
         dvmUpdateCodeUnit(method, insns+1, (u2) instField->byteOffset);
         ALOGV("DexOpt: rewrote ifield access %s.%s --> %d",
diff --git a/vm/analysis/RegisterMap.cpp b/vm/analysis/RegisterMap.cpp
index 197fb7a..7ad5e7a 100644
--- a/vm/analysis/RegisterMap.cpp
+++ b/vm/analysis/RegisterMap.cpp
@@ -563,7 +563,7 @@ static bool verifyMap(VerifierData* vdata, const RegisterMap* pMap)
  */
 static inline u1* align32(u1* ptr)
 {
-    return (u1*) (((int) ptr + 3) & ~0x03);
+    return (u1*) (((uintptr_t) ptr + 3) & ~0x03);
 }
 
 /*
diff --git a/vm/arch/generic/Call.cpp b/vm/arch/generic/Call.cpp
index 3d7cbef..6bd11c7 100644
--- a/vm/arch/generic/Call.cpp
+++ b/vm/arch/generic/Call.cpp
@@ -52,7 +52,7 @@ static ffi_type* getFfiType(char sigType)
 #define dvmPlatformInvoke dvmPlatformInvokeFFI
 
 extern "C" void dvmPlatformInvoke(void* pEnv, ClassObject* clazz, int argInfo,
-    int argc, const u4* argv, const char* signature, void* func, JValue* pResult);
+    int argc, const uintptr_t* argv, const char* signature, void* func, JValue* pResult);
 #endif
 
 /*
@@ -73,7 +73,7 @@ extern "C" void dvmPlatformInvoke(void* pEnv, ClassObject* clazz, int argInfo,
  * every call.
  */
 void dvmPlatformInvoke(void* pEnv, ClassObject* clazz, int argInfo, int argc,
-    const u4* argv, const char* shorty, void* func, JValue* pReturn)
+    const uintptr_t* argv, const char* shorty, void* func, JValue* pReturn)
 {
     const int kMaxArgs = argc+2;    /* +1 for env, maybe +1 for clazz */
     ffi_cif cif;
@@ -116,5 +116,17 @@ void dvmPlatformInvoke(void* pEnv, ClassObject* clazz, int argInfo, int argc,
         dvmAbort();
     }
 
-    ffi_call(&cif, FFI_FN(func), pReturn, values);
+    ALOGW("ffi_call: func=%p dstArg=%d", func, dstArg);
+    for (int zz=0; zz<dstArg && zz<6; zz++) ALOGW("  arg[%d] valptr=%p val=0x%lx", zz, values[zz], values[zz] ? *(unsigned long*)values[zz] : 0);
+    /* Try direct call for simple void(JNIEnv*, jclass) methods */
+    if (dstArg == 2 && retType == &ffi_type_void) {
+        typedef void (*SimpleStaticFn)(void*, void*);
+        SimpleStaticFn directFn = (SimpleStaticFn) func;
+        void* env_val = *(void**)values[0];
+        void* cls_val = *(void**)values[1];
+        ALOGW("Direct call: func=%p env=%p cls=%p", func, env_val, cls_val);
+        directFn(env_val, cls_val);
+    } else {
+        ffi_call(&cif, FFI_FN(func), pReturn, values);
+    }
 }
diff --git a/vm/compiler/Compiler.cpp b/vm/compiler/Compiler.cpp
index f5b96b1..508fe18 100644
--- a/vm/compiler/Compiler.cpp
+++ b/vm/compiler/Compiler.cpp
@@ -177,9 +177,9 @@ bool dvmCompilerSetupCodeCache(void)
              gDvmJit.codeCacheSize);
         return false;
     }
-    gDvmJit.codeCache = mmap(NULL, gDvmJit.codeCacheSize,
+    gDvmJit.codeCache = dvm_mmap_low32(NULL, gDvmJit.codeCacheSize,
                              PROT_READ | PROT_WRITE | PROT_EXEC,
-                             MAP_PRIVATE , fd, 0);
+                             MAP_PRIVATE, fd, 0);
     close(fd);
     if (gDvmJit.codeCache == MAP_FAILED) {
         ALOGE("Failed to mmap the JIT code cache of size %d: %s", gDvmJit.codeCacheSize, strerror(errno));
diff --git a/vm/hprof/HprofClass.cpp b/vm/hprof/HprofClass.cpp
index 023efdb..5dd3dea 100644
--- a/vm/hprof/HprofClass.cpp
+++ b/vm/hprof/HprofClass.cpp
@@ -44,7 +44,7 @@ static u4 computeClassHash(const ClassObject *clazz)
     char c;
 
     cp = clazz->descriptor;
-    hash = (u4)clazz->classLoader;
+    hash = (u4)(uintptr_t)clazz->classLoader;
     while ((c = *cp++) != '\0') {
         hash = hash * 31 + c;
     }
@@ -99,7 +99,7 @@ hprof_class_object_id hprofLookupClassId(const ClassObject *clazz)
      */
     getPrettyClassNameId(clazz->descriptor);
 
-    return (hprof_class_object_id)clazz;
+    return (hprof_class_object_id)(uintptr_t)clazz;
 }
 
 int hprofDumpClasses(hprof_context_t *ctx)
@@ -131,7 +131,7 @@ int hprofDumpClasses(hprof_context_t *ctx)
              * We use the address of the class object structure as its ID.
              */
             hprofAddU4ToRecord(rec, clazz->serialNumber);
-            hprofAddIdToRecord(rec, (hprof_class_object_id)clazz);
+            hprofAddIdToRecord(rec, (hprof_class_object_id)(uintptr_t)clazz);
             hprofAddU4ToRecord(rec, HPROF_NULL_STACK_TRACE);
             hprofAddIdToRecord(rec, getPrettyClassNameId(clazz->descriptor));
         }
diff --git a/vm/hprof/HprofHeap.cpp b/vm/hprof/HprofHeap.cpp
index 8c34ff3..39fe330 100644
--- a/vm/hprof/HprofHeap.cpp
+++ b/vm/hprof/HprofHeap.cpp
@@ -34,7 +34,7 @@
 /* The ID for the synthetic object generated to account for class
  * Static overhead.
  */
-#define CLASS_STATICS_ID(clazz) ((hprof_object_id)(((u4)(clazz)) | 1))
+#define CLASS_STATICS_ID(clazz) ((hprof_object_id)(((uintptr_t)(clazz)) | 1))
 
 int hprofStartHeapDump(hprof_context_t *ctx)
 {
@@ -150,7 +150,7 @@ void hprofMarkRootObject(hprof_context_t *ctx, const Object *obj,
     case HPROF_ROOT_REFERENCE_CLEANUP:
     case HPROF_ROOT_VM_INTERNAL:
         hprofAddU1ToRecord(rec, heapTag);
-        hprofAddIdToRecord(rec, (hprof_object_id)obj);
+        hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
         break;
 
     /* ID: object ID
@@ -158,8 +158,8 @@ void hprofMarkRootObject(hprof_context_t *ctx, const Object *obj,
      */
     case HPROF_ROOT_JNI_GLOBAL:
         hprofAddU1ToRecord(rec, heapTag);
-        hprofAddIdToRecord(rec, (hprof_object_id)obj);
-        hprofAddIdToRecord(rec, (hprof_id)jniObj);
+        hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
+        hprofAddIdToRecord(rec, (hprof_id)(uintptr_t)jniObj);
         break;
 
     /* ID: object ID
@@ -170,7 +170,7 @@ void hprofMarkRootObject(hprof_context_t *ctx, const Object *obj,
     case HPROF_ROOT_JNI_MONITOR:
     case HPROF_ROOT_JAVA_FRAME:
         hprofAddU1ToRecord(rec, heapTag);
-        hprofAddIdToRecord(rec, (hprof_object_id)obj);
+        hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
         hprofAddU4ToRecord(rec, ctx->gcThreadSerialNumber);
         hprofAddU4ToRecord(rec, (u4)-1);
         break;
@@ -181,7 +181,7 @@ void hprofMarkRootObject(hprof_context_t *ctx, const Object *obj,
     case HPROF_ROOT_NATIVE_STACK:
     case HPROF_ROOT_THREAD_BLOCK:
         hprofAddU1ToRecord(rec, heapTag);
-        hprofAddIdToRecord(rec, (hprof_object_id)obj);
+        hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
         hprofAddU4ToRecord(rec, ctx->gcThreadSerialNumber);
         break;
 
@@ -191,7 +191,7 @@ void hprofMarkRootObject(hprof_context_t *ctx, const Object *obj,
      */
     case HPROF_ROOT_THREAD_OBJECT:
         hprofAddU1ToRecord(rec, heapTag);
-        hprofAddIdToRecord(rec, (hprof_object_id)obj);
+        hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
         hprofAddU4ToRecord(rec, ctx->gcThreadSerialNumber);
         hprofAddU4ToRecord(rec, (u4)-1);    //xxx
         break;
@@ -287,7 +287,7 @@ int hprofDumpHeapObject(hprof_context_t *ctx, const Object *obj)
             hprofAddIdToRecord(rec, hprofLookupClassId(thisClass));
             hprofAddU4ToRecord(rec, stackTraceSerialNumber(thisClass));
             hprofAddIdToRecord(rec, hprofLookupClassId(thisClass->super));
-            hprofAddIdToRecord(rec, (hprof_object_id)thisClass->classLoader);
+            hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)thisClass->classLoader);
             hprofAddIdToRecord(rec, (hprof_object_id)0);    // no signer
             hprofAddIdToRecord(rec, (hprof_object_id)0);    // no prot domain
             hprofAddIdToRecord(rec, (hprof_id)0);           // reserved
@@ -356,7 +356,7 @@ int hprofDumpHeapObject(hprof_context_t *ctx, const Object *obj)
                  */
                 hprofAddU1ToRecord(rec, HPROF_OBJECT_ARRAY_DUMP);
 
-                hprofAddIdToRecord(rec, (hprof_object_id)obj);
+                hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
                 hprofAddU4ToRecord(rec, stackTraceSerialNumber(obj));
                 hprofAddU4ToRecord(rec, length);
                 hprofAddIdToRecord(rec, hprofLookupClassId(clazz));
@@ -380,7 +380,7 @@ int hprofDumpHeapObject(hprof_context_t *ctx, const Object *obj)
                 hprofAddU1ToRecord(rec, HPROF_PRIMITIVE_ARRAY_NODATA_DUMP);
 #endif
 
-                hprofAddIdToRecord(rec, (hprof_object_id)obj);
+                hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
                 hprofAddU4ToRecord(rec, stackTraceSerialNumber(obj));
                 hprofAddU4ToRecord(rec, length);
                 hprofAddU1ToRecord(rec, t);
@@ -410,7 +410,7 @@ int hprofDumpHeapObject(hprof_context_t *ctx, const Object *obj)
             /* obj is an instance object.
              */
             hprofAddU1ToRecord(rec, HPROF_INSTANCE_DUMP);
-            hprofAddIdToRecord(rec, (hprof_object_id)obj);
+            hprofAddIdToRecord(rec, (hprof_object_id)(uintptr_t)obj);
             hprofAddU4ToRecord(rec, stackTraceSerialNumber(obj));
             hprofAddIdToRecord(rec, hprofLookupClassId(clazz));
 
diff --git a/vm/hprof/HprofString.cpp b/vm/hprof/HprofString.cpp
index 3534f07..3ba11ef 100644
--- a/vm/hprof/HprofString.cpp
+++ b/vm/hprof/HprofString.cpp
@@ -70,7 +70,7 @@ hprof_string_id hprofLookupStringId(const char *str)
 
     dvmHashTableUnlock(gStringHashTable);
 
-    return (hprof_string_id)val;
+    return (hprof_string_id)(uintptr_t)val;
 }
 
 int hprofDumpStrings(hprof_context_t *ctx)
@@ -100,7 +100,7 @@ int hprofDumpStrings(hprof_context_t *ctx)
              *
              * We use the address of the string data as its ID.
              */
-            err = hprofAddU4ToRecord(rec, (u4)str);
+            err = hprofAddU4ToRecord(rec, (u4)(uintptr_t)str);
             if (err == 0) {
                 err = hprofAddUtf8StringToRecord(rec, str);
             }
diff --git a/vm/interp/Interp.cpp b/vm/interp/Interp.cpp
index 42e2eca..a2f3d1f 100644
--- a/vm/interp/Interp.cpp
+++ b/vm/interp/Interp.cpp
@@ -537,7 +537,7 @@ bool dvmAddSingleStep(Thread* thread, int size, int depth)
      * on by PushLocalFrame, we want to use the topmost native method.
      */
     const StackSaveArea* saveArea;
-    u4* fp;
+    void* fp;
     u4* prevFp = NULL;
 
     for (fp = thread->interpSave.curFrame; fp != NULL;
@@ -654,7 +654,7 @@ void dvmReportInvoke(Thread* self, const Method* methodToCall)
  * be called prior to the invoke.  fp is the Dalvik FP of the calling
  * method.
  */
-void dvmReportPreNativeInvoke(const Method* methodToCall, Thread* self, u4* fp)
+void dvmReportPreNativeInvoke(const Method* methodToCall, Thread* self, void* fp)
 {
 #if defined(WITH_JIT)
     /*
@@ -677,7 +677,7 @@ void dvmReportPreNativeInvoke(const Method* methodToCall, Thread* self, u4* fp)
  * special subMode requirements.  fp is the Dalvik FP of the calling
  * method.
  */
-void dvmReportPostNativeInvoke(const Method* methodToCall, Thread* self, u4* fp)
+void dvmReportPostNativeInvoke(const Method* methodToCall, Thread* self, void* fp)
 {
     if (self->interpBreak.ctl.subMode & kSubModeDebuggerActive) {
         Object* thisPtr = dvmGetThisPtr(self->interpSave.method, fp);
@@ -728,7 +728,7 @@ void dvmReportReturn(Thread* self)
  * breakpoints.  We may be able to speed things up a bit if we don't query
  * the event list unless we know there's at least one lurking within.
  */
-static void updateDebugger(const Method* method, const u2* pc, const u4* fp,
+static void updateDebugger(const Method* method, const u2* pc, const void* fp,
                            Thread* self)
 {
     int eventFlags = 0;
@@ -892,11 +892,11 @@ static void updateDebugger(const Method* method, const u2* pc, const u4* fp,
  * Because we need it when setting up debugger event filters, we want to
  * be able to do this quickly.
  */
-Object* dvmGetThisPtr(const Method* method, const u4* fp)
+Object* dvmGetThisPtr(const Method* method, const void* fp)
 {
     if (dvmIsStaticMethod(method))
         return NULL;
-    return (Object*)fp[method->registersSize - method->insSize];
+    return (Object*)((dreg_t*)fp)[method->registersSize - method->insSize];
 }
 
 
@@ -946,7 +946,7 @@ void dvmInterpCheckTrackedRefs(Thread* self, const Method* method,
 /*
  * Dump the v-registers.  Sent to the ILOG log tag.
  */
-void dvmDumpRegs(const Method* method, const u4* framePtr, bool inOnly)
+void dvmDumpRegs(const Method* method, const void* framePtr, bool inOnly)
 {
     int i, localCount;
 
@@ -956,7 +956,7 @@ void dvmDumpRegs(const Method* method, const u4* framePtr, bool inOnly)
     for (i = method->registersSize-1; i >= 0; i--) {
         if (i >= localCount) {
             ALOG(LOG_VERBOSE, LOG_TAG"i", "  v%-2d in%-2d : 0x%08x",
-                i, i-localCount, framePtr[i]);
+                i, i-localCount, ((dreg_t*)framePtr)[i]);
         } else {
             if (inOnly) {
                 ALOG(LOG_VERBOSE, LOG_TAG"i", "  [...]");
@@ -977,7 +977,7 @@ void dvmDumpRegs(const Method* method, const u4* framePtr, bool inOnly)
             }
 #endif
             ALOG(LOG_VERBOSE, LOG_TAG"i", "  v%-2d      : 0x%08x %s",
-                i, framePtr[i], name);
+                i, ((dreg_t*)framePtr)[i], name);
         }
     }
 }
@@ -1689,7 +1689,7 @@ void dvmInitializeInterpBreak(Thread* thread)
  * count profiling, JIT trace building, etc.  Dalvik PC has been exported
  * prior to call, but Thread copy of dPC & fp are not current.
  */
-void dvmCheckBefore(const u2 *pc, u4 *fp, Thread* self)
+void dvmCheckBefore(const u2 *pc, void *fp, Thread* self)
 {
     const Method* method = self->interpSave.method;
     assert(pc >= method->insns && pc <
@@ -1739,7 +1739,7 @@ void dvmCheckBefore(const u2 *pc, u4 *fp, Thread* self)
                 dvmUnlockMutex(&self->callbackMutex);
                 // Update Thread structure
                 self->interpSave.pc = pc;
-                self->interpSave.curFrame = fp;
+                self->interpSave.curFrame = (void*)fp;
                 if (callback != NULL) {
                     // Do the callback
                     if (!callback(self,arg)) {
@@ -1750,14 +1750,14 @@ void dvmCheckBefore(const u2 *pc, u4 *fp, Thread* self)
             }
             // Need to suspend?
             if (self->suspendCount) {
-                dvmExportPC(pc, fp);
+                dvmExportPC(pc, (const u4*)fp);
                 dvmCheckSuspendPending(self);
             }
         }
     }
 
     if (self->interpBreak.ctl.subMode & kSubModeDebuggerActive) {
-        updateDebugger(method, pc, fp, self);
+        updateDebugger(method, pc, (const u4*)fp, self);
     }
     if (gDvm.instructionCountEnableCount != 0) {
         /*
@@ -1926,7 +1926,7 @@ void dvmInterpret(Thread* self, const Method* method, JValue* pResult)
      * No need to initialize "retval".
      */
     self->interpSave.method = method;
-    self->interpSave.curFrame = (u4*) self->interpSave.curFrame;
+    /* curFrame already correct */
     self->interpSave.pc = method->insns;
 
     assert(!dvmIsNativeMethod(method));
diff --git a/vm/interp/Interp.h b/vm/interp/Interp.h
index e54ec61..a23e348 100644
--- a/vm/interp/Interp.h
+++ b/vm/interp/Interp.h
@@ -77,10 +77,10 @@ void dvmFlushBreakpoints(ClassObject* clazz);
 /*
  * Debugger support
  */
-extern "C" void dvmCheckBefore(const u2 *dPC, u4 *fp, Thread* self);
+extern "C" void dvmCheckBefore(const u2 *dPC, void *fp, Thread* self);
 extern "C" void dvmReportExceptionThrow(Thread* self, Object* exception);
-extern "C" void dvmReportPreNativeInvoke(const Method* methodToCall, Thread* self, u4* fp);
-extern "C" void dvmReportPostNativeInvoke(const Method* methodToCall, Thread* self, u4* fp);
+extern "C" void dvmReportPreNativeInvoke(const Method* methodToCall, Thread* self, void* fp);
+extern "C" void dvmReportPostNativeInvoke(const Method* methodToCall, Thread* self, void* fp);
 extern "C" void dvmReportInvoke(Thread* self, const Method* methodToCall);
 extern "C" void dvmReportReturn(Thread* self);
 
diff --git a/vm/interp/InterpDefs.h b/vm/interp/InterpDefs.h
index a053f6d..79909f2 100644
--- a/vm/interp/InterpDefs.h
+++ b/vm/interp/InterpDefs.h
@@ -48,7 +48,7 @@ extern void dvmMterpStd(Thread* self);
 /*
  * Get the "this" pointer from the current frame.
  */
-Object* dvmGetThisPtr(const Method* method, const u4* fp);
+Object* dvmGetThisPtr(const Method* method, const void* fp);
 
 /*
  * Verify that our tracked local references are valid.
diff --git a/vm/interp/InterpState.h b/vm/interp/InterpState.h
index 8d2c224..54ad4ac 100644
--- a/vm/interp/InterpState.h
+++ b/vm/interp/InterpState.h
@@ -108,7 +108,7 @@ typedef bool (*SafePointCallback)(struct Thread* thread, void* arg);
 
 struct InterpSaveState {
     const u2*       pc;         // Dalvik PC
-    u4*             curFrame;   // Dalvik frame pointer
+    void*            curFrame;   // Dalvik frame pointer (void* for 64-bit compat)
     const Method    *method;    // Method being executed
     DvmDex*         methodClassDex;
     JValue          retval;
diff --git a/vm/interp/Stack.cpp b/vm/interp/Stack.cpp
index 1433f13..fee4a34 100644
--- a/vm/interp/Stack.cpp
+++ b/vm/interp/Stack.cpp
@@ -20,11 +20,15 @@
  * See the majestic ASCII art in Stack.h.
  */
 #include "Dalvik.h"
+#include "RegSlot.h"
 #include "jni.h"
 
 #include <stdlib.h>
 #include <stdarg.h>
 
+/* Size of one register slot on the interpreter stack (pointer-width on 64-bit) */
+#define REG_SIZE sizeof(dreg_t)
+
 #ifdef HAVE_ANDROID_OS
 #include <corkscrew/backtrace.h>
 #endif
@@ -70,9 +74,9 @@ static bool dvmPushInterpFrame(Thread* self, const Method* method)
     assert(!dvmIsNativeMethod(method));
     assert(!dvmIsAbstractMethod(method));
 
-    stackReq = method->registersSize * 4        // params + locals
+    stackReq = method->registersSize * REG_SIZE   // params + locals
                 + sizeof(StackSaveArea) * 2     // break frame + regular frame
-                + method->outsSize * 4;         // args to other methods
+                + method->outsSize * REG_SIZE;  // args to other methods
 
     if (self->interpSave.curFrame != NULL)
         stackPtr = (u1*) SAVEAREA_FROM_FP(self->interpSave.curFrame);
@@ -96,7 +100,7 @@ static bool dvmPushInterpFrame(Thread* self, const Method* method)
      */
     stackPtr -= sizeof(StackSaveArea);
     breakSaveBlock = (StackSaveArea*)stackPtr;
-    stackPtr -= method->registersSize * 4 + sizeof(StackSaveArea);
+    stackPtr -= method->registersSize * REG_SIZE + sizeof(StackSaveArea);
     saveBlock = (StackSaveArea*) stackPtr;
 
 #if !defined(NDEBUG) && !defined(PAD_SAVE_AREA)
@@ -145,7 +149,7 @@ bool dvmPushJNIFrame(Thread* self, const Method* method)
 
     assert(dvmIsNativeMethod(method));
 
-    stackReq = method->registersSize * 4        // params only
+    stackReq = method->registersSize * REG_SIZE        // params only
                 + sizeof(StackSaveArea) * 2;    // break frame + regular frame
 
     if (self->interpSave.curFrame != NULL)
@@ -171,7 +175,7 @@ bool dvmPushJNIFrame(Thread* self, const Method* method)
      */
     stackPtr -= sizeof(StackSaveArea);
     breakSaveBlock = (StackSaveArea*)stackPtr;
-    stackPtr -= method->registersSize * 4 + sizeof(StackSaveArea);
+    stackPtr -= method->registersSize * REG_SIZE + sizeof(StackSaveArea);
     saveBlock = (StackSaveArea*) stackPtr;
 
 #if !defined(NDEBUG) && !defined(PAD_SAVE_AREA)
@@ -276,7 +280,7 @@ bool dvmPopLocalFrame(Thread* self)
 {
     StackSaveArea* saveBlock = SAVEAREA_FROM_FP(self->interpSave.curFrame);
 
-    assert(!dvmIsBreakFrame((u4*)self->interpSave.curFrame));
+    assert(!dvmIsBreakFrame((void*)self->interpSave.curFrame));
     if (saveBlock->method != SAVEAREA_FROM_FP(saveBlock->prevFrame)->method) {
         /*
          * The previous frame doesn't have the same method pointer -- we've
@@ -314,7 +318,7 @@ static bool dvmPopFrame(Thread* self)
         return false;
 
     saveBlock = SAVEAREA_FROM_FP(self->interpSave.curFrame);
-    assert(!dvmIsBreakFrame((u4*)self->interpSave.curFrame));
+    assert(!dvmIsBreakFrame((void*)self->interpSave.curFrame));
 
     /*
      * Remove everything up to the break frame.  If this was a call into
@@ -445,14 +449,14 @@ void dvmCallMethodV(Thread* self, const Method* method, Object* obj,
     const char* desc = &(method->shorty[1]); // [0] is the return type.
     int verifyCount = 0;
     ClassObject* clazz;
-    u4* ins;
+    dreg_t* ins;
 
     clazz = callPrep(self, method, obj, false);
     if (clazz == NULL)
         return;
 
     /* "ins" for new frame start at frame pointer plus locals */
-    ins = ((u4*)self->interpSave.curFrame) +
+    ins = ((dreg_t*)self->interpSave.curFrame) +
            (method->registersSize - method->insSize);
 
     //ALOGD("  FP is %p, INs live at >= %p", self->interpSave.curFrame, ins);
@@ -462,7 +466,7 @@ void dvmCallMethodV(Thread* self, const Method* method, Object* obj,
 #ifdef WITH_EXTRA_OBJECT_VALIDATION
         assert(obj != NULL && dvmIsHeapAddress(obj));
 #endif
-        *ins++ = (u4) obj;
+        *ins++ = (dreg_t)(uintptr_t) obj;
         verifyCount++;
     }
 
@@ -478,7 +482,7 @@ void dvmCallMethodV(Thread* self, const Method* method, Object* obj,
             case 'F': {
                 /* floats were normalized to doubles; convert back */
                 float f = (float) va_arg(args, double);
-                *ins++ = dvmFloatToU4(f);
+                *ins++ = (dreg_t) dvmFloatToU4(f);
                 verifyCount++;
                 break;
             }
@@ -487,15 +491,15 @@ void dvmCallMethodV(Thread* self, const Method* method, Object* obj,
                 assert(obj == NULL || dvmIsHeapAddress(obj));
                 jobject argObj = reinterpret_cast<jobject>(arg);
                 if (fromJni)
-                    *ins++ = (u4) dvmDecodeIndirectRef(self, argObj);
+                    *ins++ = (dreg_t)(uintptr_t) dvmDecodeIndirectRef(self, argObj);
                 else
-                    *ins++ = (u4) argObj;
+                    *ins++ = (dreg_t)(uintptr_t) argObj;
                 verifyCount++;
                 break;
             }
             default: {
                 /* Z B C S I -- all passed as 32-bit integers */
-                *ins++ = va_arg(args, u4);
+                *ins++ = (dreg_t) va_arg(args, u4);
                 verifyCount++;
                 break;
             }
@@ -519,7 +523,7 @@ void dvmCallMethodV(Thread* self, const Method* method, Object* obj,
          * Because we leave no space for local variables, "curFrame" points
          * directly at the method arguments.
          */
-        (*method->nativeFunc)((u4*)self->interpSave.curFrame, pResult,
+        (*method->nativeFunc)((dreg_t*)self->interpSave.curFrame, pResult,
                               method, self);
         TRACE_METHOD_EXIT(self, method);
     } else {
@@ -551,20 +555,20 @@ void dvmCallMethodA(Thread* self, const Method* method, Object* obj,
     const char* desc = &(method->shorty[1]); // [0] is the return type.
     int verifyCount = 0;
     ClassObject* clazz;
-    u4* ins;
+    dreg_t* ins;
 
     clazz = callPrep(self, method, obj, false);
     if (clazz == NULL)
         return;
 
     /* "ins" for new frame start at frame pointer plus locals */
-    ins = ((u4*)self->interpSave.curFrame) +
+    ins = ((dreg_t*)self->interpSave.curFrame) +
         (method->registersSize - method->insSize);
 
     /* put "this" pointer into in0 if appropriate */
     if (!dvmIsStaticMethod(method)) {
         assert(obj != NULL);
-        *ins++ = (u4) obj;              /* obj is a "real" ref */
+        *ins++ = (dreg_t)(uintptr_t) obj;              /* obj is a "real" ref */
         verifyCount++;
     }
 
@@ -578,25 +582,25 @@ void dvmCallMethodA(Thread* self, const Method* method, Object* obj,
             break;
         case 'L':                       /* includes array refs */
             if (fromJni)
-                *ins++ = (u4) dvmDecodeIndirectRef(self, args->l);
+                *ins++ = (dreg_t)(uintptr_t) dvmDecodeIndirectRef(self, args->l);
             else
-                *ins++ = (u4) args->l;
+                *ins++ = (dreg_t)(uintptr_t) args->l;
             break;
         case 'F':
         case 'I':
-            *ins++ = args->i;           /* full 32 bits */
+            *ins++ = (dreg_t) args->i;           /* full 32 bits */
             break;
         case 'S':
-            *ins++ = args->s;           /* 16 bits, sign-extended */
+            *ins++ = (dreg_t) args->s;           /* 16 bits, sign-extended */
             break;
         case 'C':
-            *ins++ = args->c;           /* 16 bits, unsigned */
+            *ins++ = (dreg_t) args->c;           /* 16 bits, unsigned */
             break;
         case 'B':
-            *ins++ = args->b;           /* 8 bits, sign-extended */
+            *ins++ = (dreg_t) args->b;           /* 8 bits, sign-extended */
             break;
         case 'Z':
-            *ins++ = args->z;           /* 8 bits, zero or non-zero */
+            *ins++ = (dreg_t) args->z;           /* 8 bits, zero or non-zero */
             break;
         default:
             ALOGE("Invalid char %c in short signature of %s.%s",
@@ -624,7 +628,7 @@ void dvmCallMethodA(Thread* self, const Method* method, Object* obj,
          * Because we leave no space for local variables, "curFrame" points
          * directly at the method arguments.
          */
-        (*method->nativeFunc)((u4*)self->interpSave.curFrame, pResult,
+        (*method->nativeFunc)((dreg_t*)self->interpSave.curFrame, pResult,
                               method, self);
         TRACE_METHOD_EXIT(self, method);
     } else {
@@ -660,7 +664,7 @@ Object* dvmInvokeMethod(Object* obj, const Method* method,
     ClassObject* clazz;
     Object* retObj = NULL;
     Thread* self = dvmThreadSelf();
-    s4* ins;
+    dreg_t* ins;
     int verifyCount, argListLength;
     JValue retval;
     bool needPop = false;
@@ -683,7 +687,7 @@ Object* dvmInvokeMethod(Object* obj, const Method* method,
     needPop = true;
 
     /* "ins" for new frame start at frame pointer plus locals */
-    ins = ((s4*)self->interpSave.curFrame) +
+    ins = ((dreg_t*)self->interpSave.curFrame) +
         (method->registersSize - method->insSize);
     verifyCount = 0;
 
@@ -692,7 +696,7 @@ Object* dvmInvokeMethod(Object* obj, const Method* method,
     /* put "this" pointer into in0 if appropriate */
     if (!dvmIsStaticMethod(method)) {
         assert(obj != NULL);
-        *ins++ = (s4) obj;
+        *ins++ = (dreg_t)(uintptr_t) obj;
         verifyCount++;
     }
 
@@ -703,7 +707,7 @@ Object* dvmInvokeMethod(Object* obj, const Method* method,
     DataObject** args = (DataObject**)(void*)argList->contents;
     ClassObject** types = (ClassObject**)(void*)params->contents;
     for (int i = 0; i < argListLength; i++) {
-        int width = dvmConvertArgument(*args++, *types++, ins);
+        int width = dvmConvertArgument(*args++, *types++, (s4*)ins);
         if (width < 0) {
             dvmPopFrame(self);      // throw wants to pull PC out of stack
             needPop = false;
@@ -730,7 +734,7 @@ Object* dvmInvokeMethod(Object* obj, const Method* method,
          * Because we leave no space for local variables, "curFrame" points
          * directly at the method arguments.
          */
-        (*method->nativeFunc)((u4*)self->interpSave.curFrame, &retval,
+        (*method->nativeFunc)((dreg_t*)self->interpSave.curFrame, &retval,
                               method, self);
         TRACE_METHOD_EXIT(self, method);
     } else {
@@ -1019,7 +1023,7 @@ void dvmHandleStackOverflow(Thread* self, const Method* method)
         method->clazz->descriptor, method->name, method->shorty);
     StackSaveArea* saveArea = SAVEAREA_FROM_FP(self->interpSave.curFrame);
     ALOGI("  method requires %d+%d+%d=%d bytes, fp is %p (%d left)",
-        method->registersSize * 4, sizeof(StackSaveArea), method->outsSize * 4,
+        method->registersSize * REG_SIZE, sizeof(StackSaveArea), method->outsSize * REG_SIZE,
         (method->registersSize + method->outsSize) * 4 + sizeof(StackSaveArea),
         saveArea, (u1*) saveArea - self->interpStackEnd);
     ALOGI("  expanding stack end (%p to %p)", self->interpStackEnd,
@@ -1130,8 +1134,8 @@ static bool extractMonitorEnterObject(Thread* thread, Object** pLockObj,
     }
 
     /* get and check the object in that register */
-    u4* fp = (u4*) framePtr;
-    Object* obj = (Object*) fp[reg];
+    void* fp = (u4*) framePtr;
+    Object* obj = (Object*) ((dreg_t*)fp)[reg];
     if (obj != NULL && !dvmIsHeapAddress(obj)) {
         ALOGD("ExtrMon: invalid object %p at %p[%d]", obj, fp, reg);
         return false;
diff --git a/vm/interp/Stack.h b/vm/interp/Stack.h
index 06e7ec5..6fb9ff6 100644
--- a/vm/interp/Stack.h
+++ b/vm/interp/Stack.h
@@ -128,7 +128,7 @@ struct StackSaveArea {
 #endif
 
     /* saved frame pointer for previous frame, or NULL if this is at bottom */
-    u4*         prevFrame;
+    void*        prevFrame;
 
     /* saved program counter (from method in caller's frame) */
     const u2*   savedPc;
@@ -158,7 +158,7 @@ struct StackSaveArea {
 
 /* when calling a function, get a pointer to outs[0] */
 #define OUTS_FROM_FP(_fp, _argCount) \
-    ((u4*) ((u1*)SAVEAREA_FROM_FP(_fp) - sizeof(u4) * (_argCount)))
+    ((void*) ((u1*)SAVEAREA_FROM_FP(_fp) - sizeof(dreg_t) * (_argCount)))
 
 /* reserve this many bytes for handling StackOverflowError */
 #define STACK_OVERFLOW_RESERVE  768
@@ -166,7 +166,7 @@ struct StackSaveArea {
 /*
  * Determine if the frame pointer points to a "break frame".
  */
-INLINE bool dvmIsBreakFrame(const u4* fp)
+INLINE bool dvmIsBreakFrame(const void* fp)
 {
     return SAVEAREA_FROM_FP(fp)->method == NULL;
 }
diff --git a/vm/jdwp/JdwpSocket.cpp b/vm/jdwp/JdwpSocket.cpp
index eaea607..1ae6e15 100644
--- a/vm/jdwp/JdwpSocket.cpp
+++ b/vm/jdwp/JdwpSocket.cpp
@@ -16,6 +16,10 @@
 /*
  * JDWP TCP socket network code.
  */
+/* sys/socket.h must come before netinet/in.h for sockaddr_storage on musl/OHOS */
+#include <sys/types.h>
+#include <sys/socket.h>
+
 #include "jdwp/JdwpPriv.h"
 #include "jdwp/JdwpHandler.h"
 #include "Bits.h"
@@ -25,8 +29,6 @@
 #include <stdio.h>
 #include <string.h>
 #include <errno.h>
-#include <sys/types.h>
-#include <sys/socket.h>
 #include <netinet/in.h>
 #include <netinet/tcp.h>
 #include <arpa/inet.h>
diff --git a/vm/mterp/cstubs/stubdefs.cpp b/vm/mterp/cstubs/stubdefs.cpp
index 58b4559..7af66c3 100644
--- a/vm/mterp/cstubs/stubdefs.cpp
+++ b/vm/mterp/cstubs/stubdefs.cpp
@@ -24,7 +24,7 @@
  */
 #define retval                  self->interpSave.retval
 #define pc                      self->interpSave.pc
-#define fp                      self->interpSave.curFrame
+#define fp                      ((dreg_t*)self->interpSave.curFrame)
 #define curMethod               self->interpSave.method
 #define methodClassDex          self->interpSave.methodClassDex
 #define debugTrackedRefStart    self->interpSave.debugTrackedRefStart
diff --git a/vm/mterp/out/InterpC-portable.cpp b/vm/mterp/out/InterpC-portable.cpp
index 0328aa8..b553857 100644
--- a/vm/mterp/out/InterpC-portable.cpp
+++ b/vm/mterp/out/InterpC-portable.cpp
@@ -142,7 +142,7 @@
             ALOG(_level, LOG_TAG"i", "%-2d|####%s",                          \
                 self->threadId, debugStrBuf);                               \
     } while(false)
-void dvmDumpRegs(const Method* method, const u4* framePtr, bool inOnly);
+void dvmDumpRegs(const Method* method, const void* framePtr, bool inOnly);
 # define DUMP_REGS(_meth, _frame, _inOnly) dvmDumpRegs(_meth, _frame, _inOnly)
 static const char kSpacing[] = "            ";
 #else
@@ -152,7 +152,7 @@ static const char kSpacing[] = "            ";
 #endif
 
 /* get a long from an array of u4 */
-static inline s8 getLongFromArray(const u4* ptr, int idx)
+static inline s8 getLongFromArray(const dreg_t* ptr, int idx)
 {
 #if defined(NO_UNALIGN_64__UNION)
     union { s8 ll; u4 parts[2]; } conv;
@@ -169,7 +169,7 @@ static inline s8 getLongFromArray(const u4* ptr, int idx)
 }
 
 /* store a long into an array of u4 */
-static inline void putLongToArray(u4* ptr, int idx, s8 val)
+static inline void putLongToArray(dreg_t* ptr, int idx, s8 val)
 {
 #if defined(NO_UNALIGN_64__UNION)
     union { s8 ll; u4 parts[2]; } conv;
@@ -184,7 +184,7 @@ static inline void putLongToArray(u4* ptr, int idx, s8 val)
 }
 
 /* get a double from an array of u4 */
-static inline double getDoubleFromArray(const u4* ptr, int idx)
+static inline double getDoubleFromArray(const dreg_t* ptr, int idx)
 {
 #if defined(NO_UNALIGN_64__UNION)
     union { double d; u4 parts[2]; } conv;
@@ -201,7 +201,7 @@ static inline double getDoubleFromArray(const u4* ptr, int idx)
 }
 
 /* store a double into an array of u4 */
-static inline void putDoubleToArray(u4* ptr, int idx, double dval)
+static inline void putDoubleToArray(dreg_t* ptr, int idx, double dval)
 {
 #if defined(NO_UNALIGN_64__UNION)
     union { double d; u4 parts[2]; } conv;
@@ -229,9 +229,9 @@ static inline void putDoubleToArray(u4* ptr, int idx, double dval)
         (fp[(_idx)]) : (assert(!"bad reg"),1969) )
 # define SET_REGISTER(_idx, _val) \
     ( (_idx) < curMethod->registersSize ? \
-        (fp[(_idx)] = (u4)(_val)) : (assert(!"bad reg"),1969) )
-# define GET_REGISTER_AS_OBJECT(_idx)       ((Object *)GET_REGISTER(_idx))
-# define SET_REGISTER_AS_OBJECT(_idx, _val) SET_REGISTER(_idx, (s4)_val)
+        (fp[(_idx)] = (dreg_t)(_val)) : (assert(!"bad reg"),1969) )
+# define GET_REGISTER_AS_OBJECT(_idx)       ((Object*)(uintptr_t)fp[(_idx)])
+# define SET_REGISTER_AS_OBJECT(_idx, _val) (fp[(_idx)] = (dreg_t)(uintptr_t)(_val))
 # define GET_REGISTER_INT(_idx) ((s4) GET_REGISTER(_idx))
 # define SET_REGISTER_INT(_idx, _val) SET_REGISTER(_idx, (s4)_val)
 # define GET_REGISTER_WIDE(_idx) \
@@ -254,9 +254,9 @@ static inline void putDoubleToArray(u4* ptr, int idx, double dval)
         (void)putDoubleToArray(fp, (_idx), (_val)) : assert(!"bad reg") )
 #else
 # define GET_REGISTER(_idx)                 (fp[(_idx)])
-# define SET_REGISTER(_idx, _val)           (fp[(_idx)] = (_val))
-# define GET_REGISTER_AS_OBJECT(_idx)       ((Object*) fp[(_idx)])
-# define SET_REGISTER_AS_OBJECT(_idx, _val) (fp[(_idx)] = (u4)(_val))
+# define SET_REGISTER(_idx, _val)           (fp[(_idx)] = (dreg_t)(_val))
+# define GET_REGISTER_AS_OBJECT(_idx)       ((Object*)(uintptr_t)fp[(_idx)])
+# define SET_REGISTER_AS_OBJECT(_idx, _val) (fp[(_idx)] = (dreg_t)(uintptr_t)(_val))
 # define GET_REGISTER_INT(_idx)             ((s4)GET_REGISTER(_idx))
 # define SET_REGISTER_INT(_idx, _val)       SET_REGISTER(_idx, (s4)_val)
 # define GET_REGISTER_WIDE(_idx)            getLongFromArray(fp, (_idx))
@@ -352,7 +352,7 @@ static inline bool checkForNull(Object* obj)
  * Use this to check for NULL when the instruction handler doesn't do
  * anything else that can throw an exception.
  */
-static inline bool checkForNullExportPC(Object* obj, u4* fp, const u2* pc)
+static inline bool checkForNullExportPC(Object* obj, dreg_t* fp, const u2* pc)
 {
     if (obj == NULL) {
         EXPORT_PC();
@@ -943,7 +943,6 @@ GOTO_TARGET_DECL(exceptionThrown);
                 arrayObj->length, GET_REGISTER(vsrc2));                     \
             GOTO_exceptionThrown();                                         \
         }                                                                   \
-        ILOGV("+ APUT[%d]=0x%08x", GET_REGISTER(vsrc2), GET_REGISTER(vdst));\
         ((_type*)(void*)arrayObj->contents)[GET_REGISTER(vsrc2)] =          \
             GET_REGISTER##_regsize(vdst);                                   \
     }                                                                       \
@@ -1125,7 +1124,7 @@ void dvmInterpretPortable(Thread* self)
     /* core state */
     const Method* curMethod;    // method we're interpreting
     const u2* pc;               // program counter
-    u4* fp;                     // frame pointer
+    dreg_t* fp;                  // frame pointer
     u2 inst;                    // current instruction
     /* instruction decoding */
     u4 ref;                     // 16 or 32-bit quantity fetched directly
@@ -1317,7 +1316,7 @@ HANDLE_OPCODE(OP_MOVE_EXCEPTION /*vAA*/)
     vdst = INST_AA(inst);
     ILOGV("|move-exception v%d", vdst);
     assert(self->exception != NULL);
-    SET_REGISTER(vdst, (u4)self->exception);
+    SET_REGISTER(vdst, (dreg_t)(uintptr_t)self->exception);
     dvmClearException(self);
     FINISH(1);
 OP_END
@@ -1467,7 +1466,7 @@ HANDLE_OPCODE(OP_CONST_STRING /*vAA, string@BBBB*/)
             if (strObj == NULL)
                 GOTO_exceptionThrown();
         }
-        SET_REGISTER(vdst, (u4) strObj);
+        SET_REGISTER(vdst, (dreg_t)(uintptr_t) strObj);
     }
     FINISH(2);
 OP_END
@@ -1489,7 +1488,7 @@ HANDLE_OPCODE(OP_CONST_STRING_JUMBO /*vAA, string@BBBBBBBB*/)
             if (strObj == NULL)
                 GOTO_exceptionThrown();
         }
-        SET_REGISTER(vdst, (u4) strObj);
+        SET_REGISTER(vdst, (dreg_t)(uintptr_t) strObj);
     }
     FINISH(3);
 OP_END
@@ -1509,7 +1508,7 @@ HANDLE_OPCODE(OP_CONST_CLASS /*vAA, class@BBBB*/)
             if (clazz == NULL)
                 GOTO_exceptionThrown();
         }
-        SET_REGISTER(vdst, (u4) clazz);
+        SET_REGISTER(vdst, (dreg_t)(uintptr_t) clazz);
     }
     FINISH(2);
 OP_END
@@ -1691,7 +1690,7 @@ HANDLE_OPCODE(OP_NEW_INSTANCE /*vAA, class@BBBB*/)
         newObj = dvmAllocObject(clazz, ALLOC_DONT_TRACK);
         if (newObj == NULL)
             GOTO_exceptionThrown();
-        SET_REGISTER(vdst, (u4) newObj);
+        SET_REGISTER(vdst, (dreg_t)(uintptr_t) newObj);
     }
     FINISH(2);
 OP_END
@@ -1728,7 +1727,7 @@ HANDLE_OPCODE(OP_NEW_ARRAY /*vA, vB, class@CCCC*/)
         newArray = dvmAllocArrayByClass(arrayClass, length, ALLOC_DONT_TRACK);
         if (newArray == NULL)
             GOTO_exceptionThrown();
-        SET_REGISTER(vdst, (u4) newArray);
+        SET_REGISTER(vdst, (dreg_t)(uintptr_t) newArray);
     }
     FINISH(2);
 OP_END
@@ -2054,7 +2053,7 @@ HANDLE_OPCODE(OP_APUT_OBJECT /*vAA, vBB, vCC*/)
                 arrayObj->length, GET_REGISTER(vsrc2));
             GOTO_exceptionThrown();
         }
-        obj = (Object*) GET_REGISTER(vdst);
+        obj = (Object*)(uintptr_t) GET_REGISTER(vdst);
         if (obj != NULL) {
             if (!checkForNull(obj))
                 GOTO_exceptionThrown();
@@ -2066,10 +2065,9 @@ HANDLE_OPCODE(OP_APUT_OBJECT /*vAA, vBB, vCC*/)
                 GOTO_exceptionThrown();
             }
         }
-        ILOGV("+ APUT[%d]=0x%08x", GET_REGISTER(vsrc2), GET_REGISTER(vdst));
         dvmSetObjectArrayElement(arrayObj,
                                  GET_REGISTER(vsrc2),
-                                 (Object *)GET_REGISTER(vdst));
+                                 (Object*)(uintptr_t)GET_REGISTER(vdst));
     }
     FINISH(2);
 OP_END
@@ -3584,7 +3582,7 @@ GOTO_TARGET(returnFromMethod)
 #endif
 
         /* back up to previous frame and see if we hit a break */
-        fp = (u4*)saveArea->prevFrame;
+        fp = (dreg_t*)saveArea->prevFrame;
         assert(fp != NULL);
 
         /* Handle any special subMode requirements */
@@ -3593,7 +3591,7 @@ GOTO_TARGET(returnFromMethod)
             dvmReportReturn(self);
         }
 
-        if (dvmIsBreakFrame(fp)) {
+        if (dvmIsBreakFrame(reinterpret_cast<const u4*>(fp))) {
             /* bail without popping the method frame from stack */
             LOGVV("+++ returned into break frame");
             GOTO_bail();
@@ -3799,7 +3797,7 @@ GOTO_TARGET(invokeMethod, bool methodCallRange, const Method* _methodToCall,
         //printf(" --> %s.%s %s\n", methodToCall->clazz->descriptor,
         //    methodToCall->name, methodToCall->shorty);
 
-        u4* outs;
+        dreg_t* outs;
         int i;
 
         /*
@@ -3862,14 +3860,14 @@ GOTO_TARGET(invokeMethod, bool methodCallRange, const Method* _methodToCall,
      */
     {
         StackSaveArea* newSaveArea;
-        u4* newFp;
+        dreg_t* newFp;
 
         ILOGV("> %s%s.%s %s",
             dvmIsNativeMethod(methodToCall) ? "(NATIVE) " : "",
             methodToCall->clazz->descriptor, methodToCall->name,
             methodToCall->shorty);
 
-        newFp = (u4*) SAVEAREA_FROM_FP(fp) - methodToCall->registersSize;
+        newFp = (dreg_t*) SAVEAREA_FROM_FP(fp) - methodToCall->registersSize;
         newSaveArea = SAVEAREA_FROM_FP(newFp);
 
         /* verify that we have enough space */
@@ -3932,7 +3930,7 @@ GOTO_TARGET(invokeMethod, bool methodCallRange, const Method* _methodToCall,
             self->interpSave.method = curMethod;
             methodClassDex = curMethod->clazz->pDvmDex;
             pc = methodToCall->insns;
-            fp = newFp;
+            fp = reinterpret_cast<dreg_t*>(newFp); // dreg_t*
             self->interpSave.curFrame = fp;
 #ifdef EASY_GDB
             debugSaveArea = SAVEAREA_FROM_FP(newFp);
@@ -3946,7 +3944,7 @@ GOTO_TARGET(invokeMethod, bool methodCallRange, const Method* _methodToCall,
             /* set this up for JNI locals, even if not a JNI native */
             newSaveArea->xtra.localRefCookie = self->jniLocalRefTable.segmentState.all;
 
-            self->interpSave.curFrame = newFp;
+            self->interpSave.curFrame = (void*)newFp;
 
             DUMP_REGS(methodToCall, newFp, true);   // show input args
 
@@ -3962,7 +3960,7 @@ GOTO_TARGET(invokeMethod, bool methodCallRange, const Method* _methodToCall,
              * space for locals on native calls, "newFp" points directly
              * to the method arguments.
              */
-            (*methodToCall->nativeFunc)(newFp, &retval, methodToCall, self);
+            (*methodToCall->nativeFunc)(reinterpret_cast<const dreg_t*>((const u4*)newFp), &retval, methodToCall, self);
 
             if (self->interpBreak.ctl.subMode != 0) {
                 dvmReportPostNativeInvoke(methodToCall, self, newSaveArea->prevFrame);
@@ -3971,7 +3969,7 @@ GOTO_TARGET(invokeMethod, bool methodCallRange, const Method* _methodToCall,
             /* pop frame off */
             dvmPopJniLocals(self, newSaveArea);
             self->interpSave.curFrame = newSaveArea->prevFrame;
-            fp = newSaveArea->prevFrame;
+            fp = (dreg_t*)newSaveArea->prevFrame;
 
             /*
              * If the native code threw an exception, or interpreted code
diff --git a/vm/native/InternalNative.cpp b/vm/native/InternalNative.cpp
index 0d06ea7..cc44c67 100644
--- a/vm/native/InternalNative.cpp
+++ b/vm/native/InternalNative.cpp
@@ -139,7 +139,7 @@ DalvikNativeFunc dvmLookupInternalNativeMethod(const Method* method)
  * definitions when a class is first loaded.  This throws the expected
  * exception so we don't have to explicitly check for it in the interpreter.
  */
-void dvmAbstractMethodStub(const u4* args, JValue* pResult)
+void dvmAbstractMethodStub(const dreg_t* args, JValue* pResult)
 {
     ALOGD("--- called into dvmAbstractMethodStub");
     dvmThrowAbstractMethodError("abstract method not implemented");
diff --git a/vm/native/InternalNative.h b/vm/native/InternalNative.h
index 006fc1e..a5cc7bb 100644
--- a/vm/native/InternalNative.h
+++ b/vm/native/InternalNative.h
@@ -27,6 +27,6 @@ void dvmInternalNativeShutdown(void);
 DalvikNativeFunc dvmLookupInternalNativeMethod(const Method* method);
 
 /* exception-throwing stub for abstract methods (DalvikNativeFunc) */
-extern "C" void dvmAbstractMethodStub(const u4* args, JValue* pResult);
+extern "C" void dvmAbstractMethodStub(const dreg_t* args, JValue* pResult);
 
 #endif  // DALVIK_NATIVE_INTERNALNATIVE_H_
diff --git a/vm/native/InternalNativePriv.h b/vm/native/InternalNativePriv.h
index 0925a61..bf6a3ae 100644
--- a/vm/native/InternalNativePriv.h
+++ b/vm/native/InternalNativePriv.h
@@ -45,7 +45,7 @@
  * This macro is used to implement the native methods that bridge this gap.
  */
 #define MAKE_INTRINSIC_TRAMPOLINE(INTRINSIC_FN) \
-    extern bool INTRINSIC_FN(u4 arg0, u4 arg1, u4 arg2, u4 arg3, \
+    extern bool INTRINSIC_FN(InlineArgType arg0, InlineArgType arg1, InlineArgType arg2, InlineArgType arg3, \
             JValue* pResult); \
     INTRINSIC_FN(args[0], args[1], args[2], args[3], pResult);
 
diff --git a/vm/native/dalvik_bytecode_OpcodeInfo.cpp b/vm/native/dalvik_bytecode_OpcodeInfo.cpp
index 3861fef..9a6b94c 100644
--- a/vm/native/dalvik_bytecode_OpcodeInfo.cpp
+++ b/vm/native/dalvik_bytecode_OpcodeInfo.cpp
@@ -28,7 +28,7 @@
 /*
  * public static native boolean isInvoke(int opcode);
  */
-static void Dalvik_dalvik_bytecode_OpcodeInfo_isInvoke(const u4* args,
+static void Dalvik_dalvik_bytecode_OpcodeInfo_isInvoke(const dreg_t* args,
     JValue* pResult)
 {
     Opcode opcode = static_cast<Opcode>(args[0]);
diff --git a/vm/native/dalvik_system_DexFile.cpp b/vm/native/dalvik_system_DexFile.cpp
index af1deb6..480a2f4 100644
--- a/vm/native/dalvik_system_DexFile.cpp
+++ b/vm/native/dalvik_system_DexFile.cpp
@@ -43,6 +43,31 @@ struct DexOrJar {
     u1*         pDexMemory; // malloc()ed memory, if any
 };
 
+/*
+ * Handle table for DexOrJar pointers on 64-bit hosts.
+ * Java stores the DexFile "cookie" as an int (32-bit), but pointers are 64-bit.
+ */
+#if __SIZEOF_POINTER__ > 4
+#define DEX_MAX_HANDLES 256
+static DexOrJar* sDexHandles[DEX_MAX_HANDLES];
+static int sNextDexHandle = 1;
+
+static int dexPtrToHandle(DexOrJar* p) {
+    if (p == NULL) return 0;
+    int h = sNextDexHandle++;
+    if (h >= DEX_MAX_HANDLES) { ALOGE("DexFile handle table full"); dvmAbort(); }
+    sDexHandles[h] = p;
+    return h;
+}
+static DexOrJar* dexHandleToPtr(int cookie) {
+    if (cookie <= 0 || cookie >= DEX_MAX_HANDLES) return NULL;
+    return sDexHandles[cookie];
+}
+#else
+static int dexPtrToHandle(DexOrJar* p) { return (int)(uintptr_t)p; }
+static DexOrJar* dexHandleToPtr(int cookie) { return (DexOrJar*)cookie; }
+#endif
+
 /*
  * (This is a dvmHashTableFree callback.)
  */
@@ -68,7 +93,8 @@ void dvmFreeDexOrJar(void* vptr)
  */
 static int hashcmpDexOrJar(const void* tableVal, const void* newVal)
 {
-    return (int) newVal - (int) tableVal;
+    if (newVal == tableVal) return 0;
+    return ((uintptr_t)newVal < (uintptr_t)tableVal) ? -1 : 1;
 }
 
 /*
@@ -80,14 +106,16 @@ static int hashcmpDexOrJar(const void* tableVal, const void* newVal)
  */
 static bool validateCookie(int cookie)
 {
-    DexOrJar* pDexOrJar = (DexOrJar*) cookie;
+    DexOrJar* pDexOrJar = dexHandleToPtr(cookie);
 
-    LOGVV("+++ dex verifying cookie %p", pDexOrJar);
+    LOGVV("+++ dex verifying cookie %d -> %p", cookie, pDexOrJar);
 
-    if (pDexOrJar == NULL)
+    if (pDexOrJar == NULL) {
+        dvmThrowRuntimeException("invalid DexFile cookie");
         return false;
+    }
 
-    u4 hash = cookie;
+    u4 hash = (u4)(uintptr_t) pDexOrJar;
     dvmHashTableLock(gDvm.userDexFiles);
     void* result = dvmHashTableLookup(gDvm.userDexFiles, hash, pDexOrJar,
                 hashcmpDexOrJar, false);
@@ -113,7 +141,7 @@ static void addToDexFileTable(DexOrJar* pDexOrJar) {
      * file is opened multiple times, so we can just use the low 32
      * bits of the pointer as the hash.
      */
-    u4 hash = (u4) pDexOrJar;
+    u4 hash = (u4)(uintptr_t) pDexOrJar;
     void* result;
 
     dvmHashTableLock(gDvm.userDexFiles);
@@ -148,7 +176,7 @@ static void addToDexFileTable(DexOrJar* pDexOrJar) {
  *
  * TODO: should be using "long" for a pointer.
  */
-static void Dalvik_dalvik_system_DexFile_openDexFileNative(const u4* args,
+static void Dalvik_dalvik_system_DexFile_openDexFileNative(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* sourceNameObj = (StringObject*) args[0];
@@ -233,7 +261,7 @@ static void Dalvik_dalvik_system_DexFile_openDexFileNative(const u4* args,
     }
 
     free(outputName);
-    RETURN_PTR(pDexOrJar);
+    RETURN_INT(dexPtrToHandle(pDexOrJar));
 }
 
 /*
@@ -246,7 +274,7 @@ static void Dalvik_dalvik_system_DexFile_openDexFileNative(const u4* args,
  *
  * TODO: should be using "long" for a pointer.
  */
-static void Dalvik_dalvik_system_DexFile_openDexFile_bytearray(const u4* args,
+static void Dalvik_dalvik_system_DexFile_openDexFile_bytearray(const dreg_t* args,
     JValue* pResult)
 {
     ArrayObject* fileContentsObj = (ArrayObject*) args[0];
@@ -286,7 +314,7 @@ static void Dalvik_dalvik_system_DexFile_openDexFile_bytearray(const u4* args,
     pDexOrJar->fileName = strdup("<memory>"); // Needs to be free()able.
     addToDexFileTable(pDexOrJar);
 
-    RETURN_PTR(pDexOrJar);
+    RETURN_INT(dexPtrToHandle(pDexOrJar));
 }
 
 /*
@@ -294,11 +322,11 @@ static void Dalvik_dalvik_system_DexFile_openDexFile_bytearray(const u4* args,
  *
  * Release resources associated with a user-loaded DEX file.
  */
-static void Dalvik_dalvik_system_DexFile_closeDexFile(const u4* args,
+static void Dalvik_dalvik_system_DexFile_closeDexFile(const dreg_t* args,
     JValue* pResult)
 {
     int cookie = args[0];
-    DexOrJar* pDexOrJar = (DexOrJar*) cookie;
+    DexOrJar* pDexOrJar = dexHandleToPtr(cookie);
 
     if (pDexOrJar == NULL)
         RETURN_VOID();
@@ -316,7 +344,7 @@ static void Dalvik_dalvik_system_DexFile_closeDexFile(const u4* args,
      * them when the VM shuts down.
      */
     if (pDexOrJar->okayToFree) {
-        u4 hash = (u4) pDexOrJar;
+        u4 hash = (u4)(uintptr_t) pDexOrJar;
         dvmHashTableLock(gDvm.userDexFiles);
         if (!dvmHashTableRemove(gDvm.userDexFiles, hash, pDexOrJar)) {
             ALOGW("WARNING: could not remove '%s' from DEX hash table",
@@ -346,14 +374,14 @@ static void Dalvik_dalvik_system_DexFile_closeDexFile(const u4* args,
  * Returns a null pointer with no exception if the class was not found.
  * Throws an exception on other failures.
  */
-static void Dalvik_dalvik_system_DexFile_defineClassNative(const u4* args,
+static void Dalvik_dalvik_system_DexFile_defineClassNative(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* nameObj = (StringObject*) args[0];
     Object* loader = (Object*) args[1];
     int cookie = args[2];
     ClassObject* clazz = NULL;
-    DexOrJar* pDexOrJar = (DexOrJar*) cookie;
+    DexOrJar* pDexOrJar = dexHandleToPtr(cookie);
     DvmDex* pDvmDex;
     char* name;
     char* descriptor;
@@ -404,11 +432,11 @@ static void Dalvik_dalvik_system_DexFile_defineClassNative(const u4* args,
  * Returns a String array that holds the names of all classes in the
  * specified DEX file.
  */
-static void Dalvik_dalvik_system_DexFile_getClassNameList(const u4* args,
+static void Dalvik_dalvik_system_DexFile_getClassNameList(const dreg_t* args,
     JValue* pResult)
 {
     int cookie = args[0];
-    DexOrJar* pDexOrJar = (DexOrJar*) cookie;
+    DexOrJar* pDexOrJar = dexHandleToPtr(cookie);
     Thread* self = dvmThreadSelf();
 
     if (!validateCookie(cookie))
@@ -468,7 +496,7 @@ static void Dalvik_dalvik_system_DexFile_getClassNameList(const u4* args,
  * @throws dalvik.system.StaleDexCacheError if the optimized dex file
  *         is stale but exists on a read-only partition.
  */
-static void Dalvik_dalvik_system_DexFile_isDexOptNeeded(const u4* args,
+static void Dalvik_dalvik_system_DexFile_isDexOptNeeded(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* nameObj = (StringObject*) args[0];
diff --git a/vm/native/dalvik_system_VMDebug.cpp b/vm/native/dalvik_system_VMDebug.cpp
index 5377357..a563fea 100644
--- a/vm/native/dalvik_system_VMDebug.cpp
+++ b/vm/native/dalvik_system_VMDebug.cpp
@@ -52,7 +52,7 @@ static int getFileDescriptor(Object* obj)
  * Return a set of strings describing available VM features (this is chiefly
  * of interest to DDMS).
  */
-static void Dalvik_dalvik_system_VMDebug_getVmFeatureList(const u4* args, JValue* pResult) {
+static void Dalvik_dalvik_system_VMDebug_getVmFeatureList(const dreg_t* args, JValue* pResult) {
     std::vector<std::string> features;
     features.push_back("method-trace-profiling");
     features.push_back("method-trace-profiling-streaming");
@@ -140,7 +140,7 @@ static void clearAllocProfStateFields(AllocProfState *allocProf,
  * thread.  If we actually start using the per-thread counters we'll
  * probably want to fix this.
  */
-static void Dalvik_dalvik_system_VMDebug_startAllocCounting(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_startAllocCounting(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -154,7 +154,7 @@ static void Dalvik_dalvik_system_VMDebug_startAllocCounting(const u4* args,
 /*
  * public static void stopAllocCounting()
  */
-static void Dalvik_dalvik_system_VMDebug_stopAllocCounting(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_stopAllocCounting(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -166,7 +166,7 @@ static void Dalvik_dalvik_system_VMDebug_stopAllocCounting(const u4* args,
 /*
  * private static int getAllocCount(int kind)
  */
-static void Dalvik_dalvik_system_VMDebug_getAllocCount(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_getAllocCount(const dreg_t* args,
     JValue* pResult)
 {
     AllocProfState *allocProf;
@@ -215,7 +215,7 @@ static void Dalvik_dalvik_system_VMDebug_getAllocCount(const u4* args,
 /*
  * public static void resetAllocCount(int kinds)
  */
-static void Dalvik_dalvik_system_VMDebug_resetAllocCount(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_resetAllocCount(const dreg_t* args,
     JValue* pResult)
 {
     unsigned int kinds = args[0];
@@ -230,7 +230,7 @@ static void Dalvik_dalvik_system_VMDebug_resetAllocCount(const u4* args,
  *
  * Start method trace profiling, sending results directly to DDMS.
  */
-static void Dalvik_dalvik_system_VMDebug_startMethodTracingDdmsImpl(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_startMethodTracingDdmsImpl(const dreg_t* args,
     JValue* pResult)
 {
     int bufferSize = args[0];
@@ -248,7 +248,7 @@ static void Dalvik_dalvik_system_VMDebug_startMethodTracingDdmsImpl(const u4* ar
  *
  * Start method trace profiling, sending results to a file descriptor.
  */
-static void Dalvik_dalvik_system_VMDebug_startMethodTracingFd(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_startMethodTracingFd(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* traceFileStr = (StringObject*) args[0];
@@ -283,7 +283,7 @@ static void Dalvik_dalvik_system_VMDebug_startMethodTracingFd(const u4* args,
  *
  * Start method trace profiling, sending results to a file.
  */
-static void Dalvik_dalvik_system_VMDebug_startMethodTracingFilename(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_startMethodTracingFilename(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* traceFileStr = (StringObject*) args[0];
@@ -305,7 +305,7 @@ static void Dalvik_dalvik_system_VMDebug_startMethodTracingFilename(const u4* ar
  *
  * Determine whether method tracing is currently active and what type is active.
  */
-static void Dalvik_dalvik_system_VMDebug_getMethodTracingMode(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_getMethodTracingMode(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -318,7 +318,7 @@ static void Dalvik_dalvik_system_VMDebug_getMethodTracingMode(const u4* args,
  *
  * Stop method tracing.
  */
-static void Dalvik_dalvik_system_VMDebug_stopMethodTracing(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_stopMethodTracing(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -332,7 +332,7 @@ static void Dalvik_dalvik_system_VMDebug_stopMethodTracing(const u4* args,
  *
  * Start sending method trace info to the emulator.
  */
-static void Dalvik_dalvik_system_VMDebug_startEmulatorTracing(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_startEmulatorTracing(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -346,7 +346,7 @@ static void Dalvik_dalvik_system_VMDebug_startEmulatorTracing(const u4* args,
  *
  * Start sending method trace info to the emulator.
  */
-static void Dalvik_dalvik_system_VMDebug_stopEmulatorTracing(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_stopEmulatorTracing(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -360,7 +360,7 @@ static void Dalvik_dalvik_system_VMDebug_stopEmulatorTracing(const u4* args,
  *
  * Returns "true" if a debugger is attached.
  */
-static void Dalvik_dalvik_system_VMDebug_isDebuggerConnected(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_isDebuggerConnected(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -373,7 +373,7 @@ static void Dalvik_dalvik_system_VMDebug_isDebuggerConnected(const u4* args,
  *
  * Returns "true" if debugging is enabled.
  */
-static void Dalvik_dalvik_system_VMDebug_isDebuggingEnabled(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_isDebuggingEnabled(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -387,7 +387,7 @@ static void Dalvik_dalvik_system_VMDebug_isDebuggingEnabled(const u4* args,
  * Returns the time, in msec, since we last had an interaction with the
  * debugger (send or receive).
  */
-static void Dalvik_dalvik_system_VMDebug_lastDebuggerActivity(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_lastDebuggerActivity(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -398,7 +398,7 @@ static void Dalvik_dalvik_system_VMDebug_lastDebuggerActivity(const u4* args,
 /*
  * static void startInstructionCounting()
  */
-static void Dalvik_dalvik_system_VMDebug_startInstructionCounting(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_startInstructionCounting(const dreg_t* args,
     JValue* pResult)
 {
     dvmStartInstructionCounting();
@@ -408,7 +408,7 @@ static void Dalvik_dalvik_system_VMDebug_startInstructionCounting(const u4* args
 /*
  * static void stopInstructionCounting()
  */
-static void Dalvik_dalvik_system_VMDebug_stopInstructionCounting(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_stopInstructionCounting(const dreg_t* args,
     JValue* pResult)
 {
     dvmStopInstructionCounting();
@@ -424,7 +424,7 @@ static void Dalvik_dalvik_system_VMDebug_stopInstructionCounting(const u4* args,
  * to improve our chances of finishing without contention.  (Only makes
  * sense on a uniprocessor.)
  */
-static void Dalvik_dalvik_system_VMDebug_getInstructionCount(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_getInstructionCount(const dreg_t* args,
     JValue* pResult)
 {
     ArrayObject* countArray = (ArrayObject*) args[0];
@@ -453,7 +453,7 @@ static void Dalvik_dalvik_system_VMDebug_getInstructionCount(const u4* args,
  *
  * Reset the instruction count array.
  */
-static void Dalvik_dalvik_system_VMDebug_resetInstructionCount(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_resetInstructionCount(const dreg_t* args,
     JValue* pResult)
 {
     sched_yield();
@@ -466,7 +466,7 @@ static void Dalvik_dalvik_system_VMDebug_resetInstructionCount(const u4* args,
  *
  * Dump the list of loaded classes.
  */
-static void Dalvik_dalvik_system_VMDebug_printLoadedClasses(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_printLoadedClasses(const dreg_t* args,
     JValue* pResult)
 {
     int flags = args[0];
@@ -481,7 +481,7 @@ static void Dalvik_dalvik_system_VMDebug_printLoadedClasses(const u4* args,
  *
  * Return the number of loaded classes
  */
-static void Dalvik_dalvik_system_VMDebug_getLoadedClassCount(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_getLoadedClassCount(const dreg_t* args,
     JValue* pResult)
 {
     int count;
@@ -497,7 +497,7 @@ static void Dalvik_dalvik_system_VMDebug_getLoadedClassCount(const u4* args,
  * Returns the thread-specific CPU-time clock value for the current thread,
  * or -1 if the feature isn't supported.
  */
-static void Dalvik_dalvik_system_VMDebug_threadCpuTimeNanos(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_threadCpuTimeNanos(const dreg_t* args,
     JValue* pResult)
 {
     jlong result;
@@ -519,7 +519,7 @@ static void Dalvik_dalvik_system_VMDebug_threadCpuTimeNanos(const u4* args,
  * Cause "hprof" data to be dumped.  We can throw an IOException if an
  * error occurs during file handling.
  */
-static void Dalvik_dalvik_system_VMDebug_dumpHprofData(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_dumpHprofData(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* fileNameStr = (StringObject*) args[0];
@@ -573,7 +573,7 @@ static void Dalvik_dalvik_system_VMDebug_dumpHprofData(const u4* args,
  *
  * Cause "hprof" data to be computed and sent directly to DDMS.
  */
-static void Dalvik_dalvik_system_VMDebug_dumpHprofDataDdms(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_dumpHprofDataDdms(const dreg_t* args,
     JValue* pResult)
 {
     int result;
@@ -606,7 +606,7 @@ static void Dalvik_dalvik_system_VMDebug_dumpHprofDataDdms(const u4* args,
  *
  * (Uncomment logs in dvmGetExpandedRegisterMap0() to gather stats.)
  */
-static void Dalvik_dalvik_system_VMDebug_cacheRegisterMap(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_cacheRegisterMap(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* classAndMethodDescStr = (StringObject*) args[0];
@@ -703,7 +703,7 @@ bail:
 /*
  * static void dumpReferenceTables()
  */
-static void Dalvik_dalvik_system_VMDebug_dumpReferenceTables(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_dumpReferenceTables(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -726,7 +726,7 @@ static void Dalvik_dalvik_system_VMDebug_dumpReferenceTables(const u4* args,
  * (Might want to restrict this to debuggable processes as a security
  * measure, or check SecurityManager.checkExit().)
  */
-static void Dalvik_dalvik_system_VMDebug_crash(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_crash(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -743,7 +743,7 @@ static void Dalvik_dalvik_system_VMDebug_crash(const u4* args,
  * Provide a hook for gdb to hang to so that the VM can be stopped when
  * user-tagged source locations are being executed.
  */
-static void Dalvik_dalvik_system_VMDebug_infopoint(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_infopoint(const dreg_t* args,
     JValue* pResult)
 {
     gDvm.nativeDebuggerActive = true;
@@ -754,7 +754,7 @@ static void Dalvik_dalvik_system_VMDebug_infopoint(const u4* args,
     RETURN_VOID();
 }
 
-static void Dalvik_dalvik_system_VMDebug_countInstancesOfClass(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_countInstancesOfClass(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*)args[0];
@@ -774,7 +774,7 @@ static void Dalvik_dalvik_system_VMDebug_countInstancesOfClass(const u4* args,
 /*
  * public static native void getHeapSpaceStats(long[] data)
  */
-static void Dalvik_dalvik_system_VMDebug_getHeapSpaceStats(const u4* args,
+static void Dalvik_dalvik_system_VMDebug_getHeapSpaceStats(const dreg_t* args,
     JValue* pResult)
 {
     ArrayObject* dataArray = (ArrayObject*) args[0];
diff --git a/vm/native/dalvik_system_VMRuntime.cpp b/vm/native/dalvik_system_VMRuntime.cpp
index 4123426..c942e50 100644
--- a/vm/native/dalvik_system_VMRuntime.cpp
+++ b/vm/native/dalvik_system_VMRuntime.cpp
@@ -36,7 +36,7 @@
  * between zero and one.
  */
 static void Dalvik_dalvik_system_VMRuntime_getTargetHeapUtilization(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
 
@@ -52,7 +52,7 @@ static void Dalvik_dalvik_system_VMRuntime_getTargetHeapUtilization(
  * Note that this is NOT static.
  */
 static void Dalvik_dalvik_system_VMRuntime_nativeSetTargetHeapUtilization(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     dvmSetTargetHeapUtilization(dvmU4ToFloat(args[1]));
 
@@ -65,7 +65,7 @@ static void Dalvik_dalvik_system_VMRuntime_nativeSetTargetHeapUtilization(
  * Callback function from the framework to indicate that an app has gone
  * through the startup phase and it is time to enable the JIT compiler.
  */
-static void Dalvik_dalvik_system_VMRuntime_startJitCompilation(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_startJitCompilation(const dreg_t* args,
     JValue* pResult)
 {
 #if defined(WITH_JIT)
@@ -85,7 +85,7 @@ static void Dalvik_dalvik_system_VMRuntime_startJitCompilation(const u4* args,
  * permanently disable the JIT compiler. Currently only the system server uses
  * this interface when it detects system-wide safe mode is enabled.
  */
-static void Dalvik_dalvik_system_VMRuntime_disableJitCompilation(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_disableJitCompilation(const dreg_t* args,
     JValue* pResult)
 {
 #if defined(WITH_JIT)
@@ -96,7 +96,7 @@ static void Dalvik_dalvik_system_VMRuntime_disableJitCompilation(const u4* args,
     RETURN_VOID();
 }
 
-static void Dalvik_dalvik_system_VMRuntime_newNonMovableArray(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_newNonMovableArray(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* elementClass = (ClassObject*) args[1];
@@ -127,7 +127,7 @@ static void Dalvik_dalvik_system_VMRuntime_newNonMovableArray(const u4* args,
     RETURN_PTR(newArray);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_addressOf(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_addressOf(const dreg_t* args,
     JValue* pResult)
 {
     ArrayObject* array = (ArrayObject*) args[1];
@@ -140,7 +140,7 @@ static void Dalvik_dalvik_system_VMRuntime_addressOf(const u4* args,
     RETURN_LONG(result);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_clearGrowthLimit(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_clearGrowthLimit(const dreg_t* args,
     JValue* pResult)
 {
     dvmClearGrowthLimit();
@@ -148,12 +148,12 @@ static void Dalvik_dalvik_system_VMRuntime_clearGrowthLimit(const u4* args,
 }
 
 static void Dalvik_dalvik_system_VMRuntime_isDebuggerActive(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     RETURN_BOOLEAN(gDvm.debuggerActive || gDvm.nativeDebuggerActive);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_properties(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_properties(const dreg_t* args,
     JValue* pResult)
 {
     ArrayObject* result = dvmCreateStringArray(*gDvm.properties);
@@ -168,19 +168,19 @@ static void returnCString(JValue* pResult, const char* s)
     RETURN_PTR(result);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_bootClassPath(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_bootClassPath(const dreg_t* args,
     JValue* pResult)
 {
     returnCString(pResult, gDvm.bootClassPathStr);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_classPath(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_classPath(const dreg_t* args,
     JValue* pResult)
 {
     returnCString(pResult, gDvm.classPathStr);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_vmVersion(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_vmVersion(const dreg_t* args,
     JValue* pResult)
 {
     char buf[64];
@@ -189,13 +189,13 @@ static void Dalvik_dalvik_system_VMRuntime_vmVersion(const u4* args,
     returnCString(pResult, buf);
 }
 
-static void Dalvik_dalvik_system_VMRuntime_vmLibrary(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_vmLibrary(const dreg_t* args,
     JValue* pResult)
 {
     returnCString(pResult, "libdvm.so");
 }
 
-static void Dalvik_dalvik_system_VMRuntime_setTargetSdkVersion(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_setTargetSdkVersion(const dreg_t* args,
     JValue* pResult)
 {
     // This is the target SDK version of the app we're about to run.
@@ -214,7 +214,7 @@ static void Dalvik_dalvik_system_VMRuntime_setTargetSdkVersion(const u4* args,
     RETURN_VOID();
 }
 
-static void Dalvik_dalvik_system_VMRuntime_registerNativeAllocation(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_registerNativeAllocation(const dreg_t* args,
                                                                     JValue* pResult)
 {
   int bytes = args[1];
@@ -226,7 +226,7 @@ static void Dalvik_dalvik_system_VMRuntime_registerNativeAllocation(const u4* ar
   RETURN_VOID();
 }
 
-static void Dalvik_dalvik_system_VMRuntime_registerNativeFree(const u4* args,
+static void Dalvik_dalvik_system_VMRuntime_registerNativeFree(const dreg_t* args,
                                                               JValue* pResult)
 {
   int bytes = args[1];
@@ -443,7 +443,7 @@ static void preloadDexCachesStatsFilled(DexCacheStats* filled) {
     }
 }
 
-static void Dalvik_dalvik_system_VMRuntime_preloadDexCaches(const u4* args, JValue* pResult)
+static void Dalvik_dalvik_system_VMRuntime_preloadDexCaches(const dreg_t* args, JValue* pResult)
 {
     if (!kPreloadDexCachesEnabled) {
         return;
diff --git a/vm/native/dalvik_system_VMStack.cpp b/vm/native/dalvik_system_VMStack.cpp
index db627ef..abdee62 100644
--- a/vm/native/dalvik_system_VMStack.cpp
+++ b/vm/native/dalvik_system_VMStack.cpp
@@ -26,7 +26,7 @@
  *
  * Return the defining class loader of the caller's caller.
  */
-static void Dalvik_dalvik_system_VMStack_getCallingClassLoader(const u4* args,
+static void Dalvik_dalvik_system_VMStack_getCallingClassLoader(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz =
@@ -44,7 +44,7 @@ static void Dalvik_dalvik_system_VMStack_getCallingClassLoader(const u4* args,
  *
  * Returns the class of the caller's caller's caller.
  */
-static void Dalvik_dalvik_system_VMStack_getStackClass2(const u4* args,
+static void Dalvik_dalvik_system_VMStack_getStackClass2(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz =
@@ -62,7 +62,7 @@ static void Dalvik_dalvik_system_VMStack_getStackClass2(const u4* args,
  * first two and all reflection methods.  If "stopAtPrivileged" is set,
  * stop shortly after we encounter a privileged class.
  */
-static void Dalvik_dalvik_system_VMStack_getClasses(const u4* args,
+static void Dalvik_dalvik_system_VMStack_getClasses(const dreg_t* args,
     JValue* pResult)
 {
     /* note "maxSize" is unsigned, so -1 turns into a very large value */
@@ -177,7 +177,7 @@ static int* getTraceBuf(Object* targetThreadObj, size_t* pStackDepth)
  * Retrieve the stack trace of the specified thread and return it as an
  * array of StackTraceElement.  Returns NULL on failure.
  */
-static void Dalvik_dalvik_system_VMStack_getThreadStackTrace(const u4* args,
+static void Dalvik_dalvik_system_VMStack_getThreadStackTrace(const dreg_t* args,
     JValue* pResult)
 {
     Object* targetThreadObj = (Object*) args[0];
@@ -201,7 +201,7 @@ static void Dalvik_dalvik_system_VMStack_getThreadStackTrace(const u4* args,
  * Retrieve a partial stack trace of the specified thread and return
  * the number of frames filled.  Returns 0 on failure.
  */
-static void Dalvik_dalvik_system_VMStack_fillStackTraceElements(const u4* args,
+static void Dalvik_dalvik_system_VMStack_fillStackTraceElements(const dreg_t* args,
     JValue* pResult)
 {
     Object* targetThreadObj = (Object*) args[0];
diff --git a/vm/native/dalvik_system_Zygote.cpp b/vm/native/dalvik_system_Zygote.cpp
index e2b618b..a234c5f 100644
--- a/vm/native/dalvik_system_Zygote.cpp
+++ b/vm/native/dalvik_system_Zygote.cpp
@@ -316,7 +316,7 @@ static int mountEmulatedStorage(uid_t uid, u4 mountMode) {
 }
 
 /* native public static int fork(); */
-static void Dalvik_dalvik_system_Zygote_fork(const u4* args, JValue* pResult)
+static void Dalvik_dalvik_system_Zygote_fork(const dreg_t* args, JValue* pResult)
 {
     pid_t pid;
 
@@ -485,7 +485,7 @@ static bool needsNoRandomizeWorkaround() {
 /*
  * Utility routine to fork zygote and specialize the child process.
  */
-static pid_t forkAndSpecializeCommon(const u4* args, bool isSystemServer)
+static pid_t forkAndSpecializeCommon(const dreg_t* args, bool isSystemServer)
 {
     pid_t pid;
 
@@ -684,7 +684,7 @@ static pid_t forkAndSpecializeCommon(const u4* args, bool isSystemServer)
  *     int[] gids, int debugFlags, int[][] rlimits, int mountExternal,
  *     String seInfo, String niceName);
  */
-static void Dalvik_dalvik_system_Zygote_forkAndSpecialize(const u4* args,
+static void Dalvik_dalvik_system_Zygote_forkAndSpecialize(const dreg_t* args,
     JValue* pResult)
 {
     pid_t pid;
@@ -700,7 +700,7 @@ static void Dalvik_dalvik_system_Zygote_forkAndSpecialize(const u4* args,
  *     long permittedCapabilities, long effectiveCapabilities);
  */
 static void Dalvik_dalvik_system_Zygote_forkSystemServer(
-        const u4* args, JValue* pResult)
+        const dreg_t* args, JValue* pResult)
 {
     pid_t pid;
     pid = forkAndSpecializeCommon(args, true);
diff --git a/vm/native/java_lang_Class.cpp b/vm/native/java_lang_Class.cpp
index 9b3b2f0..a075302 100644
--- a/vm/native/java_lang_Class.cpp
+++ b/vm/native/java_lang_Class.cpp
@@ -28,7 +28,7 @@
  * called from <clinit> in javac-generated classes that use the Java
  * programming language "assert" keyword.
  */
-static void Dalvik_java_lang_Class_desiredAssertionStatus(const u4* args,
+static void Dalvik_java_lang_Class_desiredAssertionStatus(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* thisPtr = (ClassObject*) args[0];
@@ -120,7 +120,7 @@ static void Dalvik_java_lang_Class_desiredAssertionStatus(const u4* args,
  *
  * "name" is in "binary name" format, e.g. "dalvik.system.Debug$1".
  */
-static void Dalvik_java_lang_Class_classForName(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Class_classForName(const dreg_t* args, JValue* pResult)
 {
     StringObject* nameObj = (StringObject*) args[0];
     bool initialize = (args[1] != 0);
@@ -134,7 +134,7 @@ static void Dalvik_java_lang_Class_classForName(const u4* args, JValue* pResult)
  *
  * Return the class' defining class loader.
  */
-static void Dalvik_java_lang_Class_getClassLoader(const u4* args,
+static void Dalvik_java_lang_Class_getClassLoader(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -148,7 +148,7 @@ static void Dalvik_java_lang_Class_getClassLoader(const u4* args,
  * If this is an array type, return the class of the elements; otherwise
  * return NULL.
  */
-static void Dalvik_java_lang_Class_getComponentType(const u4* args,
+static void Dalvik_java_lang_Class_getComponentType(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* thisPtr = (ClassObject*) args[0];
@@ -176,7 +176,7 @@ static void Dalvik_java_lang_Class_getComponentType(const u4* args,
  * If "publicOnly" is set, we strip out any classes that don't have "public"
  * access.
  */
-static void Dalvik_java_lang_Class_getDeclaredClasses(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredClasses(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -227,7 +227,7 @@ static void Dalvik_java_lang_Class_getDeclaredClasses(const u4* args,
  * static Constructor[] getDeclaredConstructors(Class clazz, boolean publicOnly)
  *     throws SecurityException
  */
-static void Dalvik_java_lang_Class_getDeclaredConstructors(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredConstructors(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -243,7 +243,7 @@ static void Dalvik_java_lang_Class_getDeclaredConstructors(const u4* args,
 /*
  * static Field[] getDeclaredFields(Class klass, boolean publicOnly)
  */
-static void Dalvik_java_lang_Class_getDeclaredFields(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredFields(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -259,7 +259,7 @@ static void Dalvik_java_lang_Class_getDeclaredFields(const u4* args,
 /*
  * static Field getDeclaredField(Class klass, String name)
  */
-static void Dalvik_java_lang_Class_getDeclaredField(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredField(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -273,7 +273,7 @@ static void Dalvik_java_lang_Class_getDeclaredField(const u4* args,
  * static Method[] getDeclaredMethods(Class clazz, boolean publicOnly)
  *     throws SecurityException
  */
-static void Dalvik_java_lang_Class_getDeclaredMethods(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredMethods(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -291,7 +291,7 @@ static void Dalvik_java_lang_Class_getDeclaredMethods(const u4* args,
  *     Class clazz, String name, Class[] args);
  */
 static void Dalvik_java_lang_Class_getDeclaredConstructorOrMethod(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
     StringObject* nameObj = (StringObject*) args[1];
@@ -308,7 +308,7 @@ static void Dalvik_java_lang_Class_getDeclaredConstructorOrMethod(
 /*
  * Class[] getInterfaces()
  */
-static void Dalvik_java_lang_Class_getInterfaces(const u4* args,
+static void Dalvik_java_lang_Class_getInterfaces(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -328,7 +328,7 @@ static void Dalvik_java_lang_Class_getInterfaces(const u4* args,
  * and this is an inner class, we return the access flags from the inner class
  * attribute.
  */
-static void Dalvik_java_lang_Class_getModifiers(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Class_getModifiers(const dreg_t* args, JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
     bool ignoreInner = args[1];
@@ -359,7 +359,7 @@ static void Dalvik_java_lang_Class_getModifiers(const u4* args, JValue* pResult)
  * between "L" and ";" but with dots rather than slashes (so "java.lang.String"
  * but "[Ljava.lang.String;"). Madness.
  */
-static void Dalvik_java_lang_Class_getNameNative(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Class_getNameNative(const dreg_t* args, JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
     const char* descriptor = clazz->descriptor;
@@ -416,7 +416,7 @@ static void Dalvik_java_lang_Class_getNameNative(const u4* args, JValue* pResult
  *
  * For an array, return the java/lang/Object ClassObject.
  */
-static void Dalvik_java_lang_Class_getSuperclass(const u4* args,
+static void Dalvik_java_lang_Class_getSuperclass(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -433,7 +433,7 @@ static void Dalvik_java_lang_Class_getSuperclass(const u4* args,
  * Determine if this class is either the same as, or is a superclass or
  * superinterface of, the class specified in the "cls" parameter.
  */
-static void Dalvik_java_lang_Class_isAssignableFrom(const u4* args,
+static void Dalvik_java_lang_Class_isAssignableFrom(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* thisPtr = (ClassObject*) args[0];
@@ -451,7 +451,7 @@ static void Dalvik_java_lang_Class_isAssignableFrom(const u4* args,
  *
  * Dynamic equivalent of Java programming language "instanceof".
  */
-static void Dalvik_java_lang_Class_isInstance(const u4* args,
+static void Dalvik_java_lang_Class_isInstance(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* thisPtr = (ClassObject*) args[0];
@@ -465,7 +465,7 @@ static void Dalvik_java_lang_Class_isInstance(const u4* args,
 /*
  * public boolean isInterface()
  */
-static void Dalvik_java_lang_Class_isInterface(const u4* args,
+static void Dalvik_java_lang_Class_isInterface(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* thisPtr = (ClassObject*) args[0];
@@ -476,7 +476,7 @@ static void Dalvik_java_lang_Class_isInterface(const u4* args,
 /*
  * public boolean isPrimitive()
  */
-static void Dalvik_java_lang_Class_isPrimitive(const u4* args,
+static void Dalvik_java_lang_Class_isPrimitive(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* thisPtr = (ClassObject*) args[0];
@@ -489,7 +489,7 @@ static void Dalvik_java_lang_Class_isPrimitive(const u4* args,
  *
  * Create a new instance of this class.
  */
-static void Dalvik_java_lang_Class_newInstance(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Class_newInstance(const dreg_t* args, JValue* pResult)
 {
     Thread* self = dvmThreadSelf();
     ClassObject* clazz = (ClassObject*) args[0];
@@ -566,7 +566,7 @@ static void Dalvik_java_lang_Class_newInstance(const u4* args, JValue* pResult)
  *
  * Returns the signature annotation array.
  */
-static void Dalvik_java_lang_Class_getSignatureAnnotation(const u4* args,
+static void Dalvik_java_lang_Class_getSignatureAnnotation(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -581,7 +581,7 @@ static void Dalvik_java_lang_Class_getSignatureAnnotation(const u4* args,
  *
  * Get the class that encloses this class (if any).
  */
-static void Dalvik_java_lang_Class_getDeclaringClass(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaringClass(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -596,7 +596,7 @@ static void Dalvik_java_lang_Class_getDeclaringClass(const u4* args,
  *
  * Get the class that encloses this class (if any).
  */
-static void Dalvik_java_lang_Class_getEnclosingClass(const u4* args,
+static void Dalvik_java_lang_Class_getEnclosingClass(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -611,7 +611,7 @@ static void Dalvik_java_lang_Class_getEnclosingClass(const u4* args,
  *
  * Get the constructor that encloses this class (if any).
  */
-static void Dalvik_java_lang_Class_getEnclosingConstructor(const u4* args,
+static void Dalvik_java_lang_Class_getEnclosingConstructor(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -632,7 +632,7 @@ static void Dalvik_java_lang_Class_getEnclosingConstructor(const u4* args,
  *
  * Get the method that encloses this class (if any).
  */
-static void Dalvik_java_lang_Class_getEnclosingMethod(const u4* args,
+static void Dalvik_java_lang_Class_getEnclosingMethod(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -649,7 +649,7 @@ static void Dalvik_java_lang_Class_getEnclosingMethod(const u4* args,
 }
 
 #if 0
-static void Dalvik_java_lang_Class_getGenericInterfaces(const u4* args,
+static void Dalvik_java_lang_Class_getGenericInterfaces(const dreg_t* args,
     JValue* pResult)
 {
     dvmThrowUnsupportedOperationException("native method not implemented");
@@ -657,7 +657,7 @@ static void Dalvik_java_lang_Class_getGenericInterfaces(const u4* args,
     RETURN_PTR(NULL);
 }
 
-static void Dalvik_java_lang_Class_getGenericSuperclass(const u4* args,
+static void Dalvik_java_lang_Class_getGenericSuperclass(const dreg_t* args,
     JValue* pResult)
 {
     dvmThrowUnsupportedOperationException("native method not implemented");
@@ -665,7 +665,7 @@ static void Dalvik_java_lang_Class_getGenericSuperclass(const u4* args,
     RETURN_PTR(NULL);
 }
 
-static void Dalvik_java_lang_Class_getTypeParameters(const u4* args,
+static void Dalvik_java_lang_Class_getTypeParameters(const dreg_t* args,
     JValue* pResult)
 {
     dvmThrowUnsupportedOperationException("native method not implemented");
@@ -679,7 +679,7 @@ static void Dalvik_java_lang_Class_getTypeParameters(const u4* args,
  *
  * Returns true if this is an "anonymous" class.
  */
-static void Dalvik_java_lang_Class_isAnonymousClass(const u4* args,
+static void Dalvik_java_lang_Class_isAnonymousClass(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -703,7 +703,7 @@ static void Dalvik_java_lang_Class_isAnonymousClass(const u4* args,
  *
  * Return the annotations declared on this class.
  */
-static void Dalvik_java_lang_Class_getDeclaredAnnotations(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredAnnotations(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -716,7 +716,7 @@ static void Dalvik_java_lang_Class_getDeclaredAnnotations(const u4* args,
 /*
  * private Annotation getDeclaredAnnotation(Class annotationClass)
  */
-static void Dalvik_java_lang_Class_getDeclaredAnnotation(const u4* args,
+static void Dalvik_java_lang_Class_getDeclaredAnnotation(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -728,7 +728,7 @@ static void Dalvik_java_lang_Class_getDeclaredAnnotation(const u4* args,
 /*
  * private boolean isDeclaredAnnotationPresent(Class annotationClass);
  */
-static void Dalvik_java_lang_Class_isDeclaredAnnotationPresent(const u4* args,
+static void Dalvik_java_lang_Class_isDeclaredAnnotationPresent(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -742,7 +742,7 @@ static void Dalvik_java_lang_Class_isDeclaredAnnotationPresent(const u4* args,
  *
  * Returns the simple name of a member class or local class, or null otherwise.
  */
-static void Dalvik_java_lang_Class_getInnerClassName(const u4* args,
+static void Dalvik_java_lang_Class_getInnerClassName(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
diff --git a/vm/native/java_lang_Double.cpp b/vm/native/java_lang_Double.cpp
index b019c8c..c97543e 100644
--- a/vm/native/java_lang_Double.cpp
+++ b/vm/native/java_lang_Double.cpp
@@ -17,17 +17,17 @@
 #include "Dalvik.h"
 #include "native/InternalNativePriv.h"
 
-static void Double_doubleToLongBits(const u4* args, JValue* pResult)
+static void Double_doubleToLongBits(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangDouble_doubleToLongBits);
 }
 
-static void Double_doubleToRawLongBits(const u4* args, JValue* pResult)
+static void Double_doubleToRawLongBits(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangDouble_doubleToRawLongBits);
 }
 
-static void Double_longBitsToDouble(const u4* args, JValue* pResult)
+static void Double_longBitsToDouble(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangDouble_longBitsToDouble);
 }
diff --git a/vm/native/java_lang_Float.cpp b/vm/native/java_lang_Float.cpp
index e99e4aa..b3c0dfc 100644
--- a/vm/native/java_lang_Float.cpp
+++ b/vm/native/java_lang_Float.cpp
@@ -17,17 +17,17 @@
 #include "Dalvik.h"
 #include "native/InternalNativePriv.h"
 
-static void Float_floatToIntBits(const u4* args, JValue* pResult)
+static void Float_floatToIntBits(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangFloat_floatToIntBits);
 }
 
-static void Float_floatToRawIntBits(const u4* args, JValue* pResult)
+static void Float_floatToRawIntBits(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangFloat_floatToRawIntBits);
 }
 
-static void Float_intBitsToFloat(const u4* args, JValue* pResult)
+static void Float_intBitsToFloat(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangFloat_intBitsToFloat);
 }
diff --git a/vm/native/java_lang_Math.cpp b/vm/native/java_lang_Math.cpp
index 7c17242..311000d 100644
--- a/vm/native/java_lang_Math.cpp
+++ b/vm/native/java_lang_Math.cpp
@@ -17,47 +17,47 @@
 #include "Dalvik.h"
 #include "native/InternalNativePriv.h"
 
-static void Math_absD(const u4* args, JValue* pResult)
+static void Math_absD(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_abs_double);
 }
 
-static void Math_absF(const u4* args, JValue* pResult)
+static void Math_absF(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_abs_float);
 }
 
-static void Math_absI(const u4* args, JValue* pResult)
+static void Math_absI(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_abs_int);
 }
 
-static void Math_absJ(const u4* args, JValue* pResult)
+static void Math_absJ(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_abs_long);
 }
 
-static void Math_cos(const u4* args, JValue* pResult)
+static void Math_cos(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_cos);
 }
 
-static void Math_maxI(const u4* args, JValue* pResult)
+static void Math_maxI(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_max_int);
 }
 
-static void Math_minI(const u4* args, JValue* pResult)
+static void Math_minI(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_min_int);
 }
 
-static void Math_sin(const u4* args, JValue* pResult)
+static void Math_sin(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_sin);
 }
 
-static void Math_sqrt(const u4* args, JValue* pResult)
+static void Math_sqrt(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangMath_sqrt);
 }
diff --git a/vm/native/java_lang_Object.cpp b/vm/native/java_lang_Object.cpp
index 12f701f..63edf27 100644
--- a/vm/native/java_lang_Object.cpp
+++ b/vm/native/java_lang_Object.cpp
@@ -26,7 +26,7 @@
  *
  * Implements most of Object.clone().
  */
-static void Dalvik_java_lang_Object_internalClone(const u4* args,
+static void Dalvik_java_lang_Object_internalClone(const dreg_t* args,
     JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
@@ -38,7 +38,7 @@ static void Dalvik_java_lang_Object_internalClone(const u4* args,
 /*
  * public int hashCode()
  */
-static void Dalvik_java_lang_Object_hashCode(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Object_hashCode(const dreg_t* args, JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
     RETURN_INT(dvmIdentityHashCode(thisPtr));
@@ -47,7 +47,7 @@ static void Dalvik_java_lang_Object_hashCode(const u4* args, JValue* pResult)
 /*
  * public Class getClass()
  */
-static void Dalvik_java_lang_Object_getClass(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Object_getClass(const dreg_t* args, JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
 
@@ -60,7 +60,7 @@ static void Dalvik_java_lang_Object_getClass(const u4* args, JValue* pResult)
  * NOTE: we declare this as a full DalvikBridgeFunc, rather than a
  * DalvikNativeFunc, because we really want to avoid the "self" lookup.
  */
-static void Dalvik_java_lang_Object_notify(const u4* args, JValue* pResult,
+static void Dalvik_java_lang_Object_notify(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self)
 {
     Object* thisPtr = (Object*) args[0];
@@ -72,7 +72,7 @@ static void Dalvik_java_lang_Object_notify(const u4* args, JValue* pResult,
 /*
  * public void notifyAll()
  */
-static void Dalvik_java_lang_Object_notifyAll(const u4* args, JValue* pResult,
+static void Dalvik_java_lang_Object_notifyAll(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self)
 {
     Object* thisPtr = (Object*) args[0];
@@ -84,7 +84,7 @@ static void Dalvik_java_lang_Object_notifyAll(const u4* args, JValue* pResult,
 /*
  * public void wait(long ms, int ns) throws InterruptedException
  */
-static void Dalvik_java_lang_Object_wait(const u4* args, JValue* pResult,
+static void Dalvik_java_lang_Object_wait(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self)
 {
     Object* thisPtr = (Object*) args[0];
diff --git a/vm/native/java_lang_Runtime.cpp b/vm/native/java_lang_Runtime.cpp
index 2d1c4fe..b829860 100644
--- a/vm/native/java_lang_Runtime.cpp
+++ b/vm/native/java_lang_Runtime.cpp
@@ -29,7 +29,7 @@
  *
  * Initiate a gc.
  */
-static void Dalvik_java_lang_Runtime_gc(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Runtime_gc(const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
 
@@ -43,7 +43,7 @@ static void Dalvik_java_lang_Runtime_gc(const u4* args, JValue* pResult)
  * Runtime.exit() calls this after doing shutdown processing.  Runtime.halt()
  * uses this as well.
  */
-static void Dalvik_java_lang_Runtime_nativeExit(const u4* args,
+static void Dalvik_java_lang_Runtime_nativeExit(const dreg_t* args,
     JValue* pResult)
 {
     int status = args[0];
@@ -67,7 +67,7 @@ static void Dalvik_java_lang_Runtime_nativeExit(const u4* args,
  * JNI-compatible methods. Returns null on success, or a failure
  * message on failure.
  */
-static void Dalvik_java_lang_Runtime_nativeLoad(const u4* args,
+static void Dalvik_java_lang_Runtime_nativeLoad(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* fileNameObj = (StringObject*) args[0];
@@ -109,7 +109,7 @@ static void Dalvik_java_lang_Runtime_nativeLoad(const u4* args,
  *
  * Returns GC heap max memory in bytes.
  */
-static void Dalvik_java_lang_Runtime_maxMemory(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_Runtime_maxMemory(const dreg_t* args, JValue* pResult)
 {
     RETURN_LONG(dvmGetHeapDebugInfo(kVirtualHeapMaximumSize));
 }
@@ -119,7 +119,7 @@ static void Dalvik_java_lang_Runtime_maxMemory(const u4* args, JValue* pResult)
  *
  * Returns GC heap total memory in bytes.
  */
-static void Dalvik_java_lang_Runtime_totalMemory(const u4* args,
+static void Dalvik_java_lang_Runtime_totalMemory(const dreg_t* args,
     JValue* pResult)
 {
     RETURN_LONG(dvmGetHeapDebugInfo(kVirtualHeapSize));
@@ -130,7 +130,7 @@ static void Dalvik_java_lang_Runtime_totalMemory(const u4* args,
  *
  * Returns GC heap free memory in bytes.
  */
-static void Dalvik_java_lang_Runtime_freeMemory(const u4* args,
+static void Dalvik_java_lang_Runtime_freeMemory(const dreg_t* args,
     JValue* pResult)
 {
     size_t size = dvmGetHeapDebugInfo(kVirtualHeapSize);
diff --git a/vm/native/java_lang_String.cpp b/vm/native/java_lang_String.cpp
index 38f9e31..9cc4f89 100644
--- a/vm/native/java_lang_String.cpp
+++ b/vm/native/java_lang_String.cpp
@@ -20,39 +20,39 @@
 #include "Dalvik.h"
 #include "native/InternalNativePriv.h"
 
-static void String_charAt(const u4* args, JValue* pResult)
+static void String_charAt(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangString_charAt);
 }
 
-static void String_compareTo(const u4* args, JValue* pResult)
+static void String_compareTo(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangString_compareTo);
 }
 
-static void String_equals(const u4* args, JValue* pResult)
+static void String_equals(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangString_equals);
 }
 
-static void String_fastIndexOf(const u4* args, JValue* pResult)
+static void String_fastIndexOf(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangString_fastIndexOf_II);
 }
 
-static void String_intern(const u4* args, JValue* pResult)
+static void String_intern(const dreg_t* args, JValue* pResult)
 {
     StringObject* str = (StringObject*) args[0];
     StringObject* interned = dvmLookupInternedString(str);
     RETURN_PTR(interned);
 }
 
-static void String_isEmpty(const u4* args, JValue* pResult)
+static void String_isEmpty(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangString_isEmpty);
 }
 
-static void String_length(const u4* args, JValue* pResult)
+static void String_length(const dreg_t* args, JValue* pResult)
 {
     MAKE_INTRINSIC_TRAMPOLINE(javaLangString_length);
 }
diff --git a/vm/native/java_lang_System.cpp b/vm/native/java_lang_System.cpp
index 1b07f85..3255717 100644
--- a/vm/native/java_lang_System.cpp
+++ b/vm/native/java_lang_System.cpp
@@ -185,7 +185,7 @@ static void memmove_words(void* dest, const void* src, size_t n) {
  * The description of this function is long, and describes a multitude
  * of checks and exceptions.
  */
-static void Dalvik_java_lang_System_arraycopy(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_System_arraycopy(const dreg_t* args, JValue* pResult)
 {
     ArrayObject* srcArray = (ArrayObject*) args[0];
     int srcPos = args[1];
@@ -366,7 +366,7 @@ static void Dalvik_java_lang_System_arraycopy(const u4* args, JValue* pResult)
  * method would return for "x", even if "x"s class
  * overrides hashCode().
  */
-static void Dalvik_java_lang_System_identityHashCode(const u4* args,
+static void Dalvik_java_lang_System_identityHashCode(const dreg_t* args,
     JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
diff --git a/vm/native/java_lang_Throwable.cpp b/vm/native/java_lang_Throwable.cpp
index 6e3ab6c..8a9aff3 100644
--- a/vm/native/java_lang_Throwable.cpp
+++ b/vm/native/java_lang_Throwable.cpp
@@ -24,7 +24,7 @@
 /*
  * private static Object nativeFillInStackTrace()
  */
-static void Dalvik_java_lang_Throwable_nativeFillInStackTrace(const u4* args,
+static void Dalvik_java_lang_Throwable_nativeFillInStackTrace(const dreg_t* args,
     JValue* pResult)
 {
     Object* stackState = NULL;
@@ -41,7 +41,7 @@ static void Dalvik_java_lang_Throwable_nativeFillInStackTrace(const u4* args,
  * The "stackState" argument must be the value returned by an earlier call to
  * nativeFillInStackTrace().
  */
-static void Dalvik_java_lang_Throwable_nativeGetStackTrace(const u4* args,
+static void Dalvik_java_lang_Throwable_nativeGetStackTrace(const dreg_t* args,
     JValue* pResult)
 {
     Object* stackState = (Object*) args[0];
diff --git a/vm/native/java_lang_VMClassLoader.cpp b/vm/native/java_lang_VMClassLoader.cpp
index eeff688..8c7a541 100644
--- a/vm/native/java_lang_VMClassLoader.cpp
+++ b/vm/native/java_lang_VMClassLoader.cpp
@@ -28,7 +28,7 @@
  *
  * Convert an array of bytes to a Class object.
  */
-static void Dalvik_java_lang_VMClassLoader_defineClass(const u4* args,
+static void Dalvik_java_lang_VMClassLoader_defineClass(const dreg_t* args,
     JValue* pResult)
 {
     Object* loader = (Object*) args[0];
@@ -56,7 +56,7 @@ static void Dalvik_java_lang_VMClassLoader_defineClass(const u4* args,
  * Convert an array of bytes to a Class object. Deprecated version of
  * previous method, lacks name parameter.
  */
-static void Dalvik_java_lang_VMClassLoader_defineClass2(const u4* args,
+static void Dalvik_java_lang_VMClassLoader_defineClass2(const dreg_t* args,
     JValue* pResult)
 {
     Object* loader = (Object*) args[0];
@@ -75,7 +75,7 @@ static void Dalvik_java_lang_VMClassLoader_defineClass2(const u4* args,
 /*
  * static Class findLoadedClass(ClassLoader cl, String name)
  */
-static void Dalvik_java_lang_VMClassLoader_findLoadedClass(const u4* args,
+static void Dalvik_java_lang_VMClassLoader_findLoadedClass(const dreg_t* args,
     JValue* pResult)
 {
     Object* loader = (Object*) args[0];
@@ -114,7 +114,7 @@ bail:
  *
  * Get the number of entries in the boot class path.
  */
-static void Dalvik_java_lang_VMClassLoader_getBootClassPathSize(const u4* args,
+static void Dalvik_java_lang_VMClassLoader_getBootClassPathSize(const dreg_t* args,
     JValue* pResult)
 {
     int count = dvmGetBootPathSize();
@@ -129,7 +129,7 @@ static void Dalvik_java_lang_VMClassLoader_getBootClassPathSize(const u4* args,
  * This mimics the previous VM interface, since we're sharing class libraries.
  */
 static void Dalvik_java_lang_VMClassLoader_getBootClassPathResource(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     StringObject* nameObj = (StringObject*) args[0];
     StringObject* result;
@@ -149,7 +149,7 @@ static void Dalvik_java_lang_VMClassLoader_getBootClassPathResource(
 /*
  * static final Class getPrimitiveClass(char prim_type)
  */
-static void Dalvik_java_lang_VMClassLoader_getPrimitiveClass(const u4* args,
+static void Dalvik_java_lang_VMClassLoader_getPrimitiveClass(const dreg_t* args,
     JValue* pResult)
 {
     int primType = args[0];
@@ -168,7 +168,7 @@ static void Dalvik_java_lang_VMClassLoader_getPrimitiveClass(const u4* args,
  *
  * "name" is in "binary name" format, e.g. "dalvik.system.Debug$1".
  */
-static void Dalvik_java_lang_VMClassLoader_loadClass(const u4* args,
+static void Dalvik_java_lang_VMClassLoader_loadClass(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* nameObj = (StringObject*) args[0];
diff --git a/vm/native/java_lang_VMThread.cpp b/vm/native/java_lang_VMThread.cpp
index 635b726..b7f00b5 100644
--- a/vm/native/java_lang_VMThread.cpp
+++ b/vm/native/java_lang_VMThread.cpp
@@ -28,7 +28,7 @@
  *
  * Throws an exception on failure.
  */
-static void Dalvik_java_lang_VMThread_create(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_VMThread_create(const dreg_t* args, JValue* pResult)
 {
     Object* threadObj = (Object*) args[0];
     s8 stackSize = GET_ARG_LONG(args, 1);
@@ -41,7 +41,7 @@ static void Dalvik_java_lang_VMThread_create(const u4* args, JValue* pResult)
 /*
  * static Thread currentThread()
  */
-static void Dalvik_java_lang_VMThread_currentThread(const u4* args,
+static void Dalvik_java_lang_VMThread_currentThread(const dreg_t* args,
     JValue* pResult)
 {
     UNUSED_PARAMETER(args);
@@ -55,7 +55,7 @@ static void Dalvik_java_lang_VMThread_currentThread(const u4* args,
  * Gets the Thread status. Result is in VM terms, has to be mapped to
  * Thread.State by interpreted code.
  */
-static void Dalvik_java_lang_VMThread_getStatus(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_VMThread_getStatus(const dreg_t* args, JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
     Thread* thread;
@@ -78,7 +78,7 @@ static void Dalvik_java_lang_VMThread_getStatus(const u4* args, JValue* pResult)
  * Returns whether the current thread has a monitor lock on the specific
  * object.
  */
-static void Dalvik_java_lang_VMThread_holdsLock(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_VMThread_holdsLock(const dreg_t* args, JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
     Object* object = (Object*) args[1];
@@ -102,7 +102,7 @@ static void Dalvik_java_lang_VMThread_holdsLock(const u4* args, JValue* pResult)
  *
  * Interrupt a thread that is waiting (or is about to wait) on a monitor.
  */
-static void Dalvik_java_lang_VMThread_interrupt(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_VMThread_interrupt(const dreg_t* args, JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
     Thread* thread;
@@ -120,7 +120,7 @@ static void Dalvik_java_lang_VMThread_interrupt(const u4* args, JValue* pResult)
  *
  * Determine if the current thread has been interrupted.  Clears the flag.
  */
-static void Dalvik_java_lang_VMThread_interrupted(const u4* args,
+static void Dalvik_java_lang_VMThread_interrupted(const dreg_t* args,
     JValue* pResult)
 {
     Thread* self = dvmThreadSelf();
@@ -139,7 +139,7 @@ static void Dalvik_java_lang_VMThread_interrupted(const u4* args,
  * Determine if the specified thread has been interrupted.  Does not clear
  * the flag.
  */
-static void Dalvik_java_lang_VMThread_isInterrupted(const u4* args,
+static void Dalvik_java_lang_VMThread_isInterrupted(const dreg_t* args,
     JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
@@ -162,7 +162,7 @@ static void Dalvik_java_lang_VMThread_isInterrupted(const u4* args,
  *
  * The name of the target thread has changed.  We may need to alert DDMS.
  */
-static void Dalvik_java_lang_VMThread_nameChanged(const u4* args,
+static void Dalvik_java_lang_VMThread_nameChanged(const dreg_t* args,
     JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
@@ -192,7 +192,7 @@ static void Dalvik_java_lang_VMThread_nameChanged(const u4* args,
  * from Thread.MIN_PRIORITY to Thread.MAX_PRIORITY (1-10), with "normal"
  * threads at Thread.NORM_PRIORITY (5).
  */
-static void Dalvik_java_lang_VMThread_setPriority(const u4* args,
+static void Dalvik_java_lang_VMThread_setPriority(const dreg_t* args,
     JValue* pResult)
 {
     Object* thisPtr = (Object*) args[0];
@@ -212,7 +212,7 @@ static void Dalvik_java_lang_VMThread_setPriority(const u4* args,
 /*
  * static void sleep(long msec, int nsec)
  */
-static void Dalvik_java_lang_VMThread_sleep(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_VMThread_sleep(const dreg_t* args, JValue* pResult)
 {
     dvmThreadSleep(GET_ARG_LONG(args,0), args[2]);
     RETURN_VOID();
@@ -226,7 +226,7 @@ static void Dalvik_java_lang_VMThread_sleep(const u4* args, JValue* pResult)
  * The exact behavior is poorly defined.  Some discussion here:
  *   http://www.cs.umd.edu/~pugh/java/memoryModel/archive/0944.html
  */
-static void Dalvik_java_lang_VMThread_yield(const u4* args, JValue* pResult)
+static void Dalvik_java_lang_VMThread_yield(const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
 
diff --git a/vm/native/java_lang_reflect_AccessibleObject.cpp b/vm/native/java_lang_reflect_AccessibleObject.cpp
index 46a1357..49b1bb0 100644
--- a/vm/native/java_lang_reflect_AccessibleObject.cpp
+++ b/vm/native/java_lang_reflect_AccessibleObject.cpp
@@ -28,7 +28,7 @@
  * Class.getSignatureAnnotation(), but available to java.lang.reflect.
  */
 static void Dalvik_java_lang_reflect_AccessibleObject_getClassSignatureAnnotation(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
     ArrayObject* arr = dvmGetClassSignatureAnnotation(clazz);
diff --git a/vm/native/java_lang_reflect_Array.cpp b/vm/native/java_lang_reflect_Array.cpp
index dbe823d..4da54c7 100644
--- a/vm/native/java_lang_reflect_Array.cpp
+++ b/vm/native/java_lang_reflect_Array.cpp
@@ -27,7 +27,7 @@
  *
  * Create a one-dimensional array of Objects.
  */
-static void Dalvik_java_lang_reflect_Array_createObjectArray(const u4* args,
+static void Dalvik_java_lang_reflect_Array_createObjectArray(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* elementClass = (ClassObject*) args[0];
@@ -63,7 +63,7 @@ static void Dalvik_java_lang_reflect_Array_createObjectArray(const u4* args,
  * subtract pieces off.  Besides, we want to start with the outermost
  * piece and work our way in.
  */
-static void Dalvik_java_lang_reflect_Array_createMultiArray(const u4* args,
+static void Dalvik_java_lang_reflect_Array_createMultiArray(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* elementClass = (ClassObject*) args[0];
diff --git a/vm/native/java_lang_reflect_Constructor.cpp b/vm/native/java_lang_reflect_Constructor.cpp
index 166dbf5..d7f8e6f 100644
--- a/vm/native/java_lang_reflect_Constructor.cpp
+++ b/vm/native/java_lang_reflect_Constructor.cpp
@@ -32,7 +32,7 @@
  * with an interface, array, or primitive class.
  */
 static void Dalvik_java_lang_reflect_Constructor_constructNative(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     // ignore thisPtr in args[0]
     ArrayObject* argList = (ArrayObject*) args[1];
diff --git a/vm/native/java_lang_reflect_Field.cpp b/vm/native/java_lang_reflect_Field.cpp
index dac784b..aff1e98 100644
--- a/vm/native/java_lang_reflect_Field.cpp
+++ b/vm/native/java_lang_reflect_Field.cpp
@@ -433,7 +433,7 @@ static void setFieldValue(Field* field, Object* obj, const JValue* value)
 /*
  * public int getFieldModifiers(Class declaringClass, int slot)
  */
-static void Dalvik_java_lang_reflect_Field_getFieldModifiers(const u4* args,
+static void Dalvik_java_lang_reflect_Field_getFieldModifiers(const dreg_t* args,
     JValue* pResult)
 {
     /* ignore thisPtr in args[0] */
@@ -451,7 +451,7 @@ static void Dalvik_java_lang_reflect_Field_getFieldModifiers(const u4* args,
  *
  * Primitive types need to be boxed.
  */
-static void Dalvik_java_lang_reflect_Field_getField(const u4* args,
+static void Dalvik_java_lang_reflect_Field_getField(const dreg_t* args,
     JValue* pResult)
 {
     /* ignore thisPtr in args[0] */
@@ -486,7 +486,7 @@ static void Dalvik_java_lang_reflect_Field_getField(const u4* args,
  * When assigning into a primitive field we will automatically extract
  * the value from box types.
  */
-static void Dalvik_java_lang_reflect_Field_setField(const u4* args,
+static void Dalvik_java_lang_reflect_Field_setField(const dreg_t* args,
     JValue* pResult)
 {
     /* ignore thisPtr in args[0] */
@@ -519,7 +519,7 @@ static void Dalvik_java_lang_reflect_Field_setField(const u4* args,
  * private double getIField(Object o, Class declaringClass,
  *     Class type, int slot, boolean noAccessCheck, char descriptor)
  */
-static void Dalvik_java_lang_reflect_Field_getPrimitiveField(const u4* args,
+static void Dalvik_java_lang_reflect_Field_getPrimitiveField(const dreg_t* args,
     JValue* pResult)
 {
     /* ignore thisPtr in args[0] */
@@ -559,7 +559,7 @@ static void Dalvik_java_lang_reflect_Field_getPrimitiveField(const u4* args,
  * private void setIField(Object o, Class declaringClass,
  *     Class type, int slot, boolean noAccessCheck, char descriptor, int value)
  */
-static void Dalvik_java_lang_reflect_Field_setPrimitiveField(const u4* args,
+static void Dalvik_java_lang_reflect_Field_setPrimitiveField(const dreg_t* args,
     JValue* pResult)
 {
     /* ignore thisPtr in args[0] */
@@ -603,7 +603,7 @@ static void Dalvik_java_lang_reflect_Field_setPrimitiveField(const u4* args,
  * Return the annotations declared for this field.
  */
 static void Dalvik_java_lang_reflect_Field_getDeclaredAnnotations(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ClassObject* declaringClass = (ClassObject*) args[0];
     int slot = args[1];
@@ -621,7 +621,7 @@ static void Dalvik_java_lang_reflect_Field_getDeclaredAnnotations(
  * static Annotation getAnnotation(
  *         Class declaringClass, int slot, Class annotationType);
  */
-static void Dalvik_java_lang_reflect_Field_getAnnotation(const u4* args,
+static void Dalvik_java_lang_reflect_Field_getAnnotation(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -636,7 +636,7 @@ static void Dalvik_java_lang_reflect_Field_getAnnotation(const u4* args,
  * static boolean isAnnotationPresent(
  *         Class declaringClass, int slot, Class annotationType);
  */
-static void Dalvik_java_lang_reflect_Field_isAnnotationPresent(const u4* args,
+static void Dalvik_java_lang_reflect_Field_isAnnotationPresent(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -652,7 +652,7 @@ static void Dalvik_java_lang_reflect_Field_isAnnotationPresent(const u4* args,
  *
  * Returns the signature annotation.
  */
-static void Dalvik_java_lang_reflect_Field_getSignatureAnnotation(const u4* args,
+static void Dalvik_java_lang_reflect_Field_getSignatureAnnotation(const dreg_t* args,
     JValue* pResult)
 {
     /* ignore thisPtr in args[0] */
diff --git a/vm/native/java_lang_reflect_Method.cpp b/vm/native/java_lang_reflect_Method.cpp
index 33d98c5..ba0c783 100644
--- a/vm/native/java_lang_reflect_Method.cpp
+++ b/vm/native/java_lang_reflect_Method.cpp
@@ -27,7 +27,7 @@
  * (Not sure why the access flags weren't stored in the class along with
  * everything else.  Not sure why this isn't static.)
  */
-static void Dalvik_java_lang_reflect_Method_getMethodModifiers(const u4* args,
+static void Dalvik_java_lang_reflect_Method_getMethodModifiers(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* declaringClass = (ClassObject*) args[0];
@@ -44,7 +44,7 @@ static void Dalvik_java_lang_reflect_Method_getMethodModifiers(const u4* args,
  *
  * Invoke a static or virtual method via reflection.
  */
-static void Dalvik_java_lang_reflect_Method_invokeNative(const u4* args,
+static void Dalvik_java_lang_reflect_Method_invokeNative(const dreg_t* args,
     JValue* pResult)
 {
     // ignore thisPtr in args[0]
@@ -118,7 +118,7 @@ init_failed:
  * Return the annotations declared for this method.
  */
 static void Dalvik_java_lang_reflect_Method_getDeclaredAnnotations(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ClassObject* declaringClass = (ClassObject*) args[0];
     int slot = args[1];
@@ -136,7 +136,7 @@ static void Dalvik_java_lang_reflect_Method_getDeclaredAnnotations(
  * static Annotation getAnnotation(
  *         Class declaringClass, int slot, Class annotationType);
  */
-static void Dalvik_java_lang_reflect_Method_getAnnotation(const u4* args,
+static void Dalvik_java_lang_reflect_Method_getAnnotation(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -151,7 +151,7 @@ static void Dalvik_java_lang_reflect_Method_getAnnotation(const u4* args,
  * static boolean isAnnotationPresent(
  *         Class declaringClass, int slot, Class annotationType);
  */
-static void Dalvik_java_lang_reflect_Method_isAnnotationPresent(const u4* args,
+static void Dalvik_java_lang_reflect_Method_isAnnotationPresent(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -168,7 +168,7 @@ static void Dalvik_java_lang_reflect_Method_isAnnotationPresent(const u4* args,
  * Return the annotations declared for this method's parameters.
  */
 static void Dalvik_java_lang_reflect_Method_getParameterAnnotations(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ClassObject* declaringClass = (ClassObject*) args[0];
     int slot = args[1];
@@ -188,7 +188,7 @@ static void Dalvik_java_lang_reflect_Method_getParameterAnnotations(
  * Return the default value for the annotation member represented by
  * this Method instance.  Returns NULL if none is defined.
  */
-static void Dalvik_java_lang_reflect_Method_getDefaultValue(const u4* args,
+static void Dalvik_java_lang_reflect_Method_getDefaultValue(const dreg_t* args,
     JValue* pResult)
 {
     // ignore thisPtr in args[0]
@@ -214,7 +214,7 @@ static void Dalvik_java_lang_reflect_Method_getDefaultValue(const u4* args,
  * Returns the signature annotation.
  */
 static void Dalvik_java_lang_reflect_Method_getSignatureAnnotation(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ClassObject* declaringClass = (ClassObject*) args[0];
     int slot = args[1];
diff --git a/vm/native/java_lang_reflect_Proxy.cpp b/vm/native/java_lang_reflect_Proxy.cpp
index da1232c..0770e9d 100644
--- a/vm/native/java_lang_reflect_Proxy.cpp
+++ b/vm/native/java_lang_reflect_Proxy.cpp
@@ -28,7 +28,7 @@
  * Generate a proxy class with the specified characteristics.  Throws an
  * exception on error.
  */
-static void Dalvik_java_lang_reflect_Proxy_generateProxy(const u4* args,
+static void Dalvik_java_lang_reflect_Proxy_generateProxy(const dreg_t* args,
     JValue* pResult)
 {
     StringObject* str = (StringObject*) args[0];
diff --git a/vm/native/java_util_concurrent_atomic_AtomicLong.cpp b/vm/native/java_util_concurrent_atomic_AtomicLong.cpp
index eb1d0de..c242f6e 100644
--- a/vm/native/java_util_concurrent_atomic_AtomicLong.cpp
+++ b/vm/native/java_util_concurrent_atomic_AtomicLong.cpp
@@ -25,7 +25,7 @@
  * private static native boolean VMSupportsCS8();
  */
 static void Dalvik_java_util_concurrent_atomic_AtomicLong_VMSupportsCS8(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
     RETURN_BOOLEAN(1);
diff --git a/vm/native/org_apache_harmony_dalvik_NativeTestTarget.cpp b/vm/native/org_apache_harmony_dalvik_NativeTestTarget.cpp
index ccc9467..f0cc16e 100644
--- a/vm/native/org_apache_harmony_dalvik_NativeTestTarget.cpp
+++ b/vm/native/org_apache_harmony_dalvik_NativeTestTarget.cpp
@@ -27,7 +27,7 @@
  * For benchmarks, a do-nothing internal method with no arguments.
  */
 static void Dalvik_org_apache_harmony_dalvik_NativeTestTarget_emptyInternalMethod(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
 
diff --git a/vm/native/org_apache_harmony_dalvik_ddmc_DdmServer.cpp b/vm/native/org_apache_harmony_dalvik_ddmc_DdmServer.cpp
index 570d469..f84f2d7 100644
--- a/vm/native/org_apache_harmony_dalvik_ddmc_DdmServer.cpp
+++ b/vm/native/org_apache_harmony_dalvik_ddmc_DdmServer.cpp
@@ -28,7 +28,7 @@
  * Send a DDM chunk to the server.
  */
 static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmServer_nativeSendChunk(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     int type = args[0];
     ArrayObject* data = (ArrayObject*) args[1];
diff --git a/vm/native/org_apache_harmony_dalvik_ddmc_DdmVmInternal.cpp b/vm/native/org_apache_harmony_dalvik_ddmc_DdmVmInternal.cpp
index 558b2ff..0071d0d 100644
--- a/vm/native/org_apache_harmony_dalvik_ddmc_DdmVmInternal.cpp
+++ b/vm/native/org_apache_harmony_dalvik_ddmc_DdmVmInternal.cpp
@@ -27,7 +27,7 @@
  * Enable DDM thread notifications.
  */
 static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_threadNotify(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     bool enable = (args[0] != 0);
 
@@ -42,7 +42,7 @@ static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_threadNotify(
  * Get a buffer full of thread info.
  */
 static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_getThreadStats(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
 
@@ -57,7 +57,7 @@ static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_getThreadStats(
  * Enable DDM heap notifications.
  */
 static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_heapInfoNotify(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     int when = args[0];
     bool ret;
@@ -73,7 +73,7 @@ static void Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_heapInfoNotify(
  */
 static void
     Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_heapSegmentNotify(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     int  when   = args[0];        // 0=never (off), 1=during GC
     int  what   = args[1];        // 0=merged objects, 1=distinct objects
@@ -92,7 +92,7 @@ static void
  */
 static void
     Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_getStackTraceById(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     u4 threadId = args[0];
     ArrayObject* trace;
@@ -108,7 +108,7 @@ static void
  */
 static void
     Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_enableRecentAllocations(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     bool enable = (args[0] != 0);
 
@@ -126,7 +126,7 @@ static void
  */
 static void
     Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_getRecentAllocationStatus(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     UNUSED_PARAMETER(args);
     RETURN_BOOLEAN(gDvm.allocRecords != NULL);
@@ -139,7 +139,7 @@ static void
  */
 static void
     Dalvik_org_apache_harmony_dalvik_ddmc_DdmVmInternal_getRecentAllocations(
-    const u4* args, JValue* pResult)
+    const dreg_t* args, JValue* pResult)
 {
     ArrayObject* data;
 
diff --git a/vm/native/sun_misc_Unsafe.cpp b/vm/native/sun_misc_Unsafe.cpp
index db8493b..630908d 100644
--- a/vm/native/sun_misc_Unsafe.cpp
+++ b/vm/native/sun_misc_Unsafe.cpp
@@ -24,7 +24,7 @@
 /*
  * private static native long objectFieldOffset0(Field field);
  */
-static void Dalvik_sun_misc_Unsafe_objectFieldOffset0(const u4* args,
+static void Dalvik_sun_misc_Unsafe_objectFieldOffset0(const dreg_t* args,
     JValue* pResult)
 {
     Object* fieldObject = (Object*) args[0];
@@ -37,7 +37,7 @@ static void Dalvik_sun_misc_Unsafe_objectFieldOffset0(const u4* args,
 /*
  * private static native int arrayBaseOffset0(Class clazz);
  */
-static void Dalvik_sun_misc_Unsafe_arrayBaseOffset0(const u4* args,
+static void Dalvik_sun_misc_Unsafe_arrayBaseOffset0(const dreg_t* args,
     JValue* pResult)
 {
     // The base offset is not type-dependent in this vm.
@@ -48,7 +48,7 @@ static void Dalvik_sun_misc_Unsafe_arrayBaseOffset0(const u4* args,
 /*
  * private static native int arrayIndexScale0(Class clazz);
  */
-static void Dalvik_sun_misc_Unsafe_arrayIndexScale0(const u4* args,
+static void Dalvik_sun_misc_Unsafe_arrayIndexScale0(const dreg_t* args,
     JValue* pResult)
 {
     ClassObject* clazz = (ClassObject*) args[0];
@@ -59,7 +59,7 @@ static void Dalvik_sun_misc_Unsafe_arrayIndexScale0(const u4* args,
  * public native boolean compareAndSwapInt(Object obj, long offset,
  *         int expectedValue, int newValue);
  */
-static void Dalvik_sun_misc_Unsafe_compareAndSwapInt(const u4* args,
+static void Dalvik_sun_misc_Unsafe_compareAndSwapInt(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -79,7 +79,7 @@ static void Dalvik_sun_misc_Unsafe_compareAndSwapInt(const u4* args,
  * public native boolean compareAndSwapLong(Object obj, long offset,
  *         long expectedValue, long newValue);
  */
-static void Dalvik_sun_misc_Unsafe_compareAndSwapLong(const u4* args,
+static void Dalvik_sun_misc_Unsafe_compareAndSwapLong(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -100,7 +100,7 @@ static void Dalvik_sun_misc_Unsafe_compareAndSwapLong(const u4* args,
  * public native boolean compareAndSwapObject(Object obj, long offset,
  *         Object expectedValue, Object newValue);
  */
-static void Dalvik_sun_misc_Unsafe_compareAndSwapObject(const u4* args,
+static void Dalvik_sun_misc_Unsafe_compareAndSwapObject(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -108,11 +108,10 @@ static void Dalvik_sun_misc_Unsafe_compareAndSwapObject(const u4* args,
     s8 offset = GET_ARG_LONG(args, 2);
     Object* expectedValue = (Object*) args[4];
     Object* newValue = (Object*) args[5];
-    int32_t* address = (int32_t*) (((u1*) obj) + offset);
+    Object** address = (Object**) (((u1*) obj) + offset);
 
-    // Note: android_atomic_cmpxchg() returns 0 on success, not failure.
-    int result = android_atomic_release_cas((int32_t) expectedValue,
-            (int32_t) newValue, address);
+    // Note: returns 0 on success via __sync (opposite of android CAS).
+    int result = !__sync_bool_compare_and_swap(address, expectedValue, newValue);
     dvmWriteBarrierField(obj, address);
     RETURN_BOOLEAN(result == 0);
 }
@@ -120,7 +119,7 @@ static void Dalvik_sun_misc_Unsafe_compareAndSwapObject(const u4* args,
 /*
  * public native int getIntVolatile(Object obj, long offset);
  */
-static void Dalvik_sun_misc_Unsafe_getIntVolatile(const u4* args,
+static void Dalvik_sun_misc_Unsafe_getIntVolatile(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -135,7 +134,7 @@ static void Dalvik_sun_misc_Unsafe_getIntVolatile(const u4* args,
 /*
  * public native void putIntVolatile(Object obj, long offset, int newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putIntVolatile(const u4* args,
+static void Dalvik_sun_misc_Unsafe_putIntVolatile(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -151,7 +150,7 @@ static void Dalvik_sun_misc_Unsafe_putIntVolatile(const u4* args,
 /*
  * public native long getLongVolatile(Object obj, long offset);
  */
-static void Dalvik_sun_misc_Unsafe_getLongVolatile(const u4* args,
+static void Dalvik_sun_misc_Unsafe_getLongVolatile(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -166,7 +165,7 @@ static void Dalvik_sun_misc_Unsafe_getLongVolatile(const u4* args,
 /*
  * public native void putLongVolatile(Object obj, long offset, long newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putLongVolatile(const u4* args,
+static void Dalvik_sun_misc_Unsafe_putLongVolatile(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -183,31 +182,34 @@ static void Dalvik_sun_misc_Unsafe_putLongVolatile(const u4* args,
 /*
  * public native Object getObjectVolatile(Object obj, long offset);
  */
-static void Dalvik_sun_misc_Unsafe_getObjectVolatile(const u4* args,
+static void Dalvik_sun_misc_Unsafe_getObjectVolatile(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
     s8 offset = GET_ARG_LONG(args, 2);
-    volatile int32_t* address = (volatile int32_t*) (((u1*) obj) + offset);
-
-    RETURN_PTR((Object*) android_atomic_acquire_load(address));
+    Object* volatile* address = (Object* volatile*) (((u1*) obj) + offset);
+    Object* val = *address;
+    ANDROID_MEMBAR_FULL();
+    RETURN_PTR(val);
 }
 
 /*
  * public native void putObjectVolatile(Object obj, long offset,
  *         Object newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putObjectVolatile(const u4* args,
+static void Dalvik_sun_misc_Unsafe_putObjectVolatile(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
     s8 offset = GET_ARG_LONG(args, 2);
     Object* value = (Object*) args[4];
-    volatile int32_t* address = (volatile int32_t*) (((u1*) obj) + offset);
+    Object* volatile* address = (Object* volatile*) (((u1*) obj) + offset);
 
-    android_atomic_release_store((int32_t)value, address);
+    ANDROID_MEMBAR_STORE();
+    *address = value;
+    ANDROID_MEMBAR_FULL();
     dvmWriteBarrierField(obj, (void *)address);
     RETURN_VOID();
 }
@@ -215,7 +217,7 @@ static void Dalvik_sun_misc_Unsafe_putObjectVolatile(const u4* args,
 /*
  * public native int getInt(Object obj, long offset);
  */
-static void Dalvik_sun_misc_Unsafe_getInt(const u4* args, JValue* pResult)
+static void Dalvik_sun_misc_Unsafe_getInt(const dreg_t* args, JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
@@ -228,7 +230,7 @@ static void Dalvik_sun_misc_Unsafe_getInt(const u4* args, JValue* pResult)
 /*
  * public native void putInt(Object obj, long offset, int newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putInt(const u4* args, JValue* pResult)
+static void Dalvik_sun_misc_Unsafe_putInt(const dreg_t* args, JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
@@ -243,7 +245,7 @@ static void Dalvik_sun_misc_Unsafe_putInt(const u4* args, JValue* pResult)
 /*
  * public native void putOrderedInt(Object obj, long offset, int newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putOrderedInt(const u4* args,
+static void Dalvik_sun_misc_Unsafe_putOrderedInt(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -260,7 +262,7 @@ static void Dalvik_sun_misc_Unsafe_putOrderedInt(const u4* args,
 /*
  * public native long getLong(Object obj, long offset);
  */
-static void Dalvik_sun_misc_Unsafe_getLong(const u4* args, JValue* pResult)
+static void Dalvik_sun_misc_Unsafe_getLong(const dreg_t* args, JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
@@ -273,7 +275,7 @@ static void Dalvik_sun_misc_Unsafe_getLong(const u4* args, JValue* pResult)
 /*
  * public native void putLong(Object obj, long offset, long newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putLong(const u4* args, JValue* pResult)
+static void Dalvik_sun_misc_Unsafe_putLong(const dreg_t* args, JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
@@ -288,7 +290,7 @@ static void Dalvik_sun_misc_Unsafe_putLong(const u4* args, JValue* pResult)
 /*
  * public native void putOrderedLong(Object obj, long offset, long newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putOrderedLong(const u4* args,
+static void Dalvik_sun_misc_Unsafe_putOrderedLong(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
@@ -305,7 +307,7 @@ static void Dalvik_sun_misc_Unsafe_putOrderedLong(const u4* args,
 /*
  * public native Object getObject(Object obj, long offset);
  */
-static void Dalvik_sun_misc_Unsafe_getObject(const u4* args, JValue* pResult)
+static void Dalvik_sun_misc_Unsafe_getObject(const dreg_t* args, JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
@@ -318,7 +320,7 @@ static void Dalvik_sun_misc_Unsafe_getObject(const u4* args, JValue* pResult)
 /*
  * public native void putObject(Object obj, long offset, Object newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putObject(const u4* args, JValue* pResult)
+static void Dalvik_sun_misc_Unsafe_putObject(const dreg_t* args, JValue* pResult)
 {
     // We ignore the this pointer in args[0].
     Object* obj = (Object*) args[1];
@@ -335,7 +337,7 @@ static void Dalvik_sun_misc_Unsafe_putObject(const u4* args, JValue* pResult)
  * public native void putOrderedObject(Object obj, long offset,
  *      Object newValue);
  */
-static void Dalvik_sun_misc_Unsafe_putOrderedObject(const u4* args,
+static void Dalvik_sun_misc_Unsafe_putOrderedObject(const dreg_t* args,
     JValue* pResult)
 {
     // We ignore the this pointer in args[0].
diff --git a/vm/oo/Array.cpp b/vm/oo/Array.cpp
index a03b295..ead0745 100644
--- a/vm/oo/Array.cpp
+++ b/vm/oo/Array.cpp
@@ -45,7 +45,7 @@ static ArrayObject* allocArray(ClassObject* arrayClass, size_t length,
     assert(elemWidth > 0);
     assert(elemWidth <= 8);
     assert((elemWidth & (elemWidth - 1)) == 0);
-    size_t elementShift = sizeof(size_t) * CHAR_BIT - 1 - CLZ(elemWidth);
+    size_t elementShift = sizeof(unsigned int) * CHAR_BIT - 1 - CLZ(elemWidth);
     size_t elementSize = length << elementShift;
     size_t headerSize = OFFSETOF_MEMBER(ArrayObject, contents);
     size_t totalSize = elementSize + headerSize;
diff --git a/vm/oo/Object.h b/vm/oo/Object.h
index 92438ba..5083f60 100644
--- a/vm/oo/Object.h
+++ b/vm/oo/Object.h
@@ -22,6 +22,7 @@
 #define DALVIK_OO_OBJECT_H_
 
 #include <stddef.h>
+#include "RegSlot.h"
 #include "Atomic.h"
 
 /* fwd decl */
@@ -48,9 +49,9 @@ struct RegisterMap;
  * arguments passed in, but some functions only care about the first two.
  * Passing extra arguments to a C function is (mostly) harmless.
  */
-typedef void (*DalvikBridgeFunc)(const u4* args, JValue* pResult,
+typedef void (*DalvikBridgeFunc)(const dreg_t* args, JValue* pResult,
     const Method* method, struct Thread* self);
-typedef void (*DalvikNativeFunc)(const u4* args, JValue* pResult);
+typedef void (*DalvikNativeFunc)(const dreg_t* args, JValue* pResult);
 
 
 /* vm-internal access flags and related definitions */
@@ -145,7 +146,8 @@ enum ClassStatus {
  */
 #define CLASS_WALK_SUPER ((unsigned int)(3))
 #define CLASS_SMALLEST_OFFSET (sizeof(struct Object))
-#define CLASS_BITS_PER_WORD (sizeof(unsigned long int) * 8)
+/* refOffsets is u4 (32-bit), so always use 32-bit width here */
+#define CLASS_BITS_PER_WORD 32
 #define CLASS_OFFSET_ALIGNMENT 4
 #define CLASS_HIGH_BIT ((unsigned int)1 << (CLASS_BITS_PER_WORD - 1))
 /*
@@ -208,8 +210,13 @@ struct Object {
     /*
      * A word containing either a "thin" lock or a "fat" monitor.  See
      * the comments in Sync.c for a description of its layout.
+     * On 64-bit, must be pointer-width to hold Monitor* in fat locks.
      */
+#if __SIZEOF_POINTER__ > 4
+    volatile uintptr_t lock;
+#else
     u4              lock;
+#endif
 };
 
 /*
diff --git a/vm/oo/ObjectInlines.h b/vm/oo/ObjectInlines.h
index eb2e962..ab53589 100644
--- a/vm/oo/ObjectInlines.h
+++ b/vm/oo/ObjectInlines.h
@@ -119,7 +119,13 @@ INLINE double dvmGetFieldDoubleVolatile(const Object* obj, int offset) {
 }
 INLINE Object* dvmGetFieldObjectVolatile(const Object* obj, int offset) {
     Object** ptr = &((JValue*)BYTE_OFFSET(obj, offset))->l;
+#if __SIZEOF_POINTER__ > 4
+    Object* val = *ptr;
+    ANDROID_MEMBAR_FULL();
+    return val;
+#else
     return (Object*)android_atomic_acquire_load((int32_t*)ptr);
+#endif
 }
 
 INLINE void dvmSetFieldBoolean(Object* obj, int offset, bool val) {
@@ -275,7 +281,13 @@ INLINE double dvmGetStaticFieldDoubleVolatile(const StaticField* sfield) {
 }
 INLINE Object* dvmGetStaticFieldObjectVolatile(const StaticField* sfield) {
     Object* const* ptr = &(sfield->value.l);
+#if __SIZEOF_POINTER__ > 4
+    Object* val = *ptr;
+    ANDROID_MEMBAR_FULL();
+    return val;
+#else
     return (Object*)android_atomic_acquire_load((int32_t*)ptr);
+#endif
 }
 
 INLINE void dvmSetStaticFieldBoolean(StaticField* sfield, bool val) {
diff --git a/vm/reflect/Annotation.cpp b/vm/reflect/Annotation.cpp
index 942027c..bbd58e9 100644
--- a/vm/reflect/Annotation.cpp
+++ b/vm/reflect/Annotation.cpp
@@ -1288,7 +1288,7 @@ bool dvmIsClassAnnotationPresent(const ClassObject* clazz,
 {
     const DexAnnotationSetItem* pAnnoSet = findAnnotationSetForClass(clazz);
     if (pAnnoSet == NULL) {
-        return NULL;
+        return false;
     }
     const DexAnnotationItem* pAnnoItem = getAnnotationItemFromAnnotationSet(
             clazz, pAnnoSet, kDexVisibilityRuntime, annotationClazz);
@@ -1743,7 +1743,7 @@ bool dvmIsMethodAnnotationPresent(const ClassObject* clazz,
 {
     const DexAnnotationSetItem* pAnnoSet = findAnnotationSetForMethod(method);
     if (pAnnoSet == NULL) {
-        return NULL;
+        return false;
     }
     const DexAnnotationItem* pAnnoItem = getAnnotationItemFromAnnotationSet(
             clazz, pAnnoSet, kDexVisibilityRuntime, annotationClazz);
@@ -2053,7 +2053,7 @@ bool dvmIsFieldAnnotationPresent(const ClassObject* clazz,
 {
     const DexAnnotationSetItem* pAnnoSet = findAnnotationSetForField(field);
     if (pAnnoSet == NULL) {
-        return NULL;
+        return false;
     }
     const DexAnnotationItem* pAnnoItem = getAnnotationItemFromAnnotationSet(
             clazz, pAnnoSet, kDexVisibilityRuntime, annotationClazz);
diff --git a/vm/reflect/Proxy.cpp b/vm/reflect/Proxy.cpp
index 57d32e7..fc29623 100644
--- a/vm/reflect/Proxy.cpp
+++ b/vm/reflect/Proxy.cpp
@@ -39,9 +39,9 @@ static void updateExceptionClassList(const Method* method, PointerSet* throws);
 static void createConstructor(ClassObject* clazz, Method* meth);
 static void createHandlerMethod(ClassObject* clazz, Method* dstMeth,
     const Method* srcMeth);
-static void proxyConstructor(const u4* args, JValue* pResult,
+static void proxyConstructor(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self);
-static void proxyInvoker(const u4* args, JValue* pResult,
+static void proxyInvoker(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self);
 static bool mustWrapException(const Method* method, const Object* throwable);
 
@@ -787,7 +787,7 @@ static void createHandlerMethod(ClassObject* clazz, Method* dstMeth,
  *
  * On failure, returns with an appropriate exception raised.
  */
-static ArrayObject* boxMethodArgs(const Method* method, const u4* args)
+static ArrayObject* boxMethodArgs(const Method* method, const dreg_t* args)
 {
     const char* desc = &method->shorty[1]; // [0] is the return type.
 
@@ -848,7 +848,7 @@ static ArrayObject* boxMethodArgs(const Method* method, const u4* args)
  * This is the constructor for a generated proxy object.  All we need to
  * do is stuff "handler" into "h".
  */
-static void proxyConstructor(const u4* args, JValue* pResult,
+static void proxyConstructor(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self)
 {
     Object* obj = (Object*) args[0];
@@ -867,7 +867,7 @@ static void proxyConstructor(const u4* args, JValue* pResult,
  * a new Object[] array, make the call, and unbox the return value if
  * necessary.
  */
-static void proxyInvoker(const u4* args, JValue* pResult,
+static void proxyInvoker(const dreg_t* args, JValue* pResult,
     const Method* method, Thread* self)
 {
     Object* thisObj = (Object*) args[0];
diff --git a/vm/reflect/Reflect.cpp b/vm/reflect/Reflect.cpp
index 9cb9fc7..9e21232 100644
--- a/vm/reflect/Reflect.cpp
+++ b/vm/reflect/Reflect.cpp
@@ -1038,7 +1038,8 @@ int dvmConvertArgument(DataObject* arg, ClassObject* type, s4* destPtr)
     } else {
         /* verify object is compatible */
         if ((arg == NULL) || dvmInstanceof(arg->clazz, type)) {
-            *destPtr = (s4) arg;
+            /* 64-bit fix: store full pointer into dreg_t-sized slot */
+            *(uintptr_t*)destPtr = (uintptr_t) arg;
             retVal = 1;
         } else {
             LOGVV("Arg %p (%s) not compatible with %s",
