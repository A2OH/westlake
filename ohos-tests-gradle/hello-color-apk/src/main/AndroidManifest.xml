<?xml version="1.0" encoding="utf-8"?>
<!--
  E12 smoke-test APK. No resources, no theme, no permissions. MainActivity
  builds a ColorView in onCreate and exposes it via a public field so the
  in-process launcher can grab the view, call its draw(canvas) on a
  SoftwareCanvas, and feed the BGRA buffer to the in-process DRM presenter.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:label="HelloColorApk"
                 android:debuggable="true"
                 android:hasCode="true">
        <activity android:name=".MainActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
