@Entry
@Component
struct Index {
  build() {
    Column() {
      // The host intentionally has no visible content. Android owns the app UI.
    }
    .width('100%')
    .height('100%')
    .backgroundColor('#00000000')
  }
}
