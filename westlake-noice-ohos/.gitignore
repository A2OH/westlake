# Large / un-rebuildable binaries are intentionally NOT committed.
# Their md5s + provenance are in REPRODUCE.md §11 and ARTIFACT-INVENTORY.txt.
liboh_android_runtime.so
liboh_adapter_bridge.so
libhwui.so
*.apk
*.jar
boot*.art
boot*.oat
boot*.vdex
*.oat
*.art
*.vdex
# allow the small committed blobs explicitly
!ca-store/cacerts.tgz
!scripts/SmaliAssemble.java
!native-libs/libv4force.so
!native-tls/libtlsjni.so
!native-tls/tlsjni-extra.dex
# clean-WSL reproducibility package: committed prebuilt fixed binaries
# (md5-suffixed so they don't match the *.jar/*.so patterns; force-added).
# See MANIFEST.md + REPRODUCE-CLEAN-WSL.md. framework.jar + boot image are NOT
# committed (size) — pull with collect-artifacts.sh.
!prebuilt-jars/
!prebuilt-native/
!sslsockets-fix/
